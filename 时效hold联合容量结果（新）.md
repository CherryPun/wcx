# 时效 hold 联合容量结果（新）

## 1. 做法

**hold 规则**（`时效hold（新）.py`）：若 V5 Top1 是显著掉量业务（`cost_chg ≤ -20%` 且近7天成本基数 ≥ 1000），
则**维持当前业务、不做切换**；否则保持原 Top1。输出 `v5_node_recommendations_hold（新）.csv` 交 V6 容量分配。

参数与前置一致：窗口 `2026-08-10~09-09`；V6 用 `0.70/14/7/0.8`；复用同一 raw。

## 2. 三方案对比（V6 全切溢出）

| 口径 | 基线(V5) | 时效折减 | **时效 hold** |
|---|---:|---:|---:|
| 全切溢出业务数 | 4 | 3 | **2** |
| **合计溢出(Gbps)** | **4,955** | 5,139 | **2,006（↓59.5%）** |
| 1382680008 字节CDN专线 | 1,957 | 3,562 | 2,006 |
| 10000224 字节CDN专线V4 | 1,634 | 1,574 | 0 |
| 10000096 字节架构专线V3 | 1,364 | 0 | 0 |
| 10000041 B站Web加速 | 0 | 3 | 0 |
| 10000244 B站专线-盒子V2 | 0 | 0 | 0 |
| 拦截节点 | 11 | 9 | 10 |

- hold 触发：**1,146/2,288 节点（50.1%）维持当前业务**。
- `10000244` 三方案均为 overflow 0、规划切入 7~8 个节点，**始终不是容量约束对象**。

## 3. 结论

1. **hold 规则优于"折减换下一个"**：总溢出 4,955 → 2,006 Gbps（↓59.5%），且消掉了 `10000096`、`10000224`、`10000041` 三个溢出业务；
2. **残留 `1382680008 字节CDN专线`（≈2,006 Gbps）是需求本身超容量**，只能靠容量层拦截/扩容，**时效规则无法消除**；
3. `10000244 B站专线-盒子V2` 与容量无关（三方案 overflow 均为 0），其"扎堆"属排序/准确性问题，且在当前 V5 下仅 82 节点。

## 4. 建议

- **时效层采用 hold 规则**：掉量业务不给"切换背书"，既不额外加压其它业务，也符合"候选/参考、不自动切"的定位；
- `1382680008` 的溢出交由**容量层拦截/扩容**处理；
- 后续需要补：hold 后的**离线命中/收益复核**（本实验只评估了容量溢出，未重算 hit@k / regret）。

## 5. 限制

- 本实验只比较"全切溢出"与拦截节点数，**未重算命中类指标**；hold 会降低切换量，可能影响 hit@k，需补评估；
- 数据截至 `2026-09-09`，需滚动重算。

> 本文件为实验记录，不代表最终方案；明细 CSV 为本地产物，不入库。
