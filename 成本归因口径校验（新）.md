# 成本归因口径校验（新）

**数据**：桌面 `七牛CDN掉量成本归因修正_研发操作数据_2026-09-06_vs_2026-09-05.xlsx`（研发口径）；
以及 `七牛CDN下钻业务掉量毛利影响及成本分摊调整_2026-09-13_vs_2026-09-12.xlsx`（毛利下钻）。

**工具**：`wcx2实验代码（新）/成本归因复算（新）.py`（11 项对平校验，全 PASS 才退出码 0）。

## 1. 研发归因规则（已从原始列复现，误差 0）

```
机房级建议调账额 = max(0, 昨日机房总成本 × 前日CDN成本占比 − 昨日CDN成本)
业务项追加成本   = 机房建议调账额 × (该项计费带宽下降G / 机房内CDN下降G合计)
调整后CDN成本    = 昨日CDN成本 + 建议调账额
调整后其他业务成本 = 昨日其他业务成本 − 建议调账额
总账约束         = 各机房调账明细合计 0（不改机房总成本、不改大盘）
```

**对平结果（11/11 PASS）**：机房级建议额公式、调整后 CDN 成本/毛利、调整后其他业务成本、
业务项分摊权重/追加成本/调整后成本/毛利、研发明细合计=0、每机房合计=0、节点级 CDN追加+其他冲回=0。

## 2. 影响量级（09-06 vs 09-05）

| 指标 | 值 |
|---|---:|
| 受影响机房数 | 48 |
| CDN 计费带宽下降 | 488.72 G |
| 建议转回 CDN 承担成本 | **32,347.26 元** |
| 调整前 CDN 毛利 | −17,612.11 元 |
| 调整后 CDN 毛利 | **−49,959.37 元（恶化约 2.8 倍）** |

另一日（09-13 vs 09-12，下钻业务合计）：掉量 −534.4 G，当前模型毛利影响 −6,793 元，
**修正后口径 −73,853 元（差 67,060 元，≈ 低估 10.9 倍）**。

## 3. 关键判据：修正是否改变排序（影响温和）

> ⚠️ **更正**：初版此处误用 `04_CDN追加承担_节点级` 的 `调整后_毛利金额` 列（**整列 1,528 行全空**），
> 只统计到 03 表的 1,052 行，得出"负占比 42.7%→17.9%、25% 符号翻转、Spearman 0.69"的虚高结论，已作废。
> 下表为 03+04 全量（n=2,580）用恒等式 `毛利金额 − 成本调整金额` 重算。

在节点×业务粒度计算「单位建设带宽毛利」（分母 节点建设带宽Gbps）：

| 指标 | 修正前 | 修正后 |
|---|---:|---:|
| 单G毛利为负的行占比 | 44.0% | 50.2% |
| 符号翻转行占比 | — | **11.4%** |
| 单G毛利 Spearman 秩相关 | — | **0.8831** |
| 业务级 平均单G毛利 秩相关 | — | 0.9298 |

→ 修正是**温和影响**（业务级排序基本不变），不是"重排"。

## 4. 结论

1. **现平台/矿主标签在"掉量分摊"情形下系统性失真**——CDN 少摊成本、其他业务多摊成本；
   量级上当前口径把 CDN 掉量的毛利冲击**低估约 10 倍**（−6,793 vs −73,853 元）。
   此结论来自表格自身汇总 + 公式 11/11 对平，**不受 §3 更正影响**。
2. **但该修正只改"业务间成本归属"，总账恒为 0** → **节点级总利润不变**；
   且全量重算显示它**对可分性/排序只有温和影响**（残差 −4.5%，业务级秩相关 0.93）。
   - 因此它**能纠业务级成本归属水平**，但**不能证明提升节点级可预测性**；
   - **不能救"节点级"平台预测**（节点总盘子未变，节点层噪声依旧，详见 `标签可分性诊断（新）.md`）。
3. 推论：平台模型若重做，**合理粒度是"业务/机房级"**；坚持节点级单位利润预测大概率仍是低 R²。

## 5. 局限与下一步

**局限**：仅 2 个日对；仅七牛CDN相关机房/业务；仅在"掉量"触发下反向标定；口径为毛利（收入−成本），
与 RJ「单位平台利润」需映射；nodeId 为哈希，需与 wcx `node_id` 对照。

**下一步（按性价比）**
1. 用 wcx 现有 `platform_unit_profit` 与两表节点级重算值做**差异分布**（判系统偏差方向/幅度）；
2. 用表 A `05_节点级明细` 做**方差分解**（业务固定效应 / 机房固定效应 / 残差），判定标签噪声占比；
3. 若②显示业务层可解释度高 → 向财务/RJ 要 **08-10~09-09 全窗口**同字段（收入/成本/计费带宽/节点建设带宽/调账），
   按本规则重建 V3 标签并双跑对照；
4. 若②显示残差仍主导 → 平台改为**业务级预测 + 节点级区间/观察**，停止节点级点估计投入。
