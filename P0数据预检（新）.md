# P0 数据预检（新）

## 1. 目的
为后续改进（平台重做 / 需求曲线 / 凹目标）确认数据是否够用，并给出准入门禁。

## 2. 已拉取
- 源：Superset `test.node_day_ops_wide_full`；**窗口扩到 `2026-03-16 ~ 2026-09-09`（178 天）**；
- 聚合口径（按 业务×日）：`nodes=COUNT(DISTINCT nodeId)`、`supply_mbps=ΣbuildBandwidth`、`traffic_bps=Σpeak95`、`cost=Σcost_finalAmount`、`rev=Σrevenue_finalAmount`；
- 结果：**21,360 行、185 业务、178 天**（按 6 个月分批拉取，规避服务端 10,000 行截断）。
- 本地产物：`业务日供给流量（新）.csv`（不入库）。

## 3. 字段与规模核查
- 关键业务覆盖良好：`10000096 / 10000183 / 10000244 / 10000079` 均 **178 天**、供给 CV 0.27~0.43、流量缺失 0；
- 门禁（天数≥60 且 供给CV≥0.2 且 流量缺失<50%）**通过 72 个业务**，满足拟合需求曲线的样本量要求。

## 4. 限制（需在校验时注意）
- 本聚合为**全部节点**（未限大节点）；如需大节点口径，需按节点前缀/画像过滤后重算；
- **七牛虚拟业务未做 node-day 去重**（可能重复计数）；
- `traffic_bps` 为 bps，转 Mbps 需 /1e6；
- 供给与需求存在**内生性**（供给常随需求走），曲线可能偏平 → 需用池内/省际差异或断点辅助。

## 5. 结论与准入
- **P3（需求曲线）数据条件已满足**（178 天、72 业务过门禁）；
- **P2（平台重做）**：标签与定价特征已有，待归因口径确认；
- **P5（凹目标）**：数据齐备，规模需控制（先业务层、K≤5）。
- 下一步进入 **P1 评估体系**。
