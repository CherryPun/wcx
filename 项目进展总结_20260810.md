# 多业务节点最佳业务推荐项目进展

更新时间：2026-08-10

## 当前状态

已完成真实数据重建、节点级训练/测试切分、多随机种子验证，以及严格的节点固有属性特征重训。

当前收益预测模型只使用节点上线前可确定的固有属性：地域、运营商、设备、硬件容量、部署配置和名义网络配置。历史在线时长、前一天在线率、带宽利用率、运行中 CPU/内存/磁盘使用率、RTT、丢包、重传、SMART、ZFS 和业务流量不进入收益预测模型。

CPU、内存、磁盘使用率、重传、丢包、SMART、ZFS 等上线后观测指标仍保留在节点属性和健康契约中，只用于业务健康准入：不合格业务阻断，指标缺失转人工审核。

## 数据口径

- 数据表：`test.node_day_ops_wide_full`
- 数据范围：`2026-03-16` 至 `2026-08-04`
- 节点筛选：`customerId > 0`，至少运行 3 个不同业务，业务首次出现日期至少分布在 3 天，且至少存在一条 `state='online'` 记录
- 业务窗口：每个节点-业务从首次出现日开始计算 7 个自然日
- 目标一：7 天累计成本最高业务，作为矿主最佳业务
- 目标二：7 天累计收入减累计成本最高业务，作为运营最佳业务
- 合格节点：26,623 个
- 节点-业务结果：294,836 行
- 候选业务：85 个
- 节点级切分：训练 21,298 个，测试 5,325 个；实际有效评估节点分别为 21,291 和 5,323 个
- 当前数据使用节点 ID 后缀 `00` 至 `0c` 的稳定样本，约为全量的 5.08%，不是全量节点推断

## 最终模型字段

增强模型包含：

- 45 个固有类别属性：地域、运营商、设备型号、硬件型号、架构、NAT、拨号配置、机房、部署配置等
- 17 个固有数值属性：名义带宽、CPU 核心数、内存总量、总盘/HDD/SSD/系统盘容量，以及 `node_join` 中的硬件容量字段
- 12 个业务与固有容量的交互字段：业务与名义带宽、CPU 核数、内存容量、磁盘容量的交互

核心基线包含 20 个类别属性和同一组 17 个固有容量数值属性；增强版增加部署、机房、拨号和节点配置属性。

数值缺失只使用训练集统计量填补，并增加缺失标记；类别缺失作为独立类别。动态观测字段不以缺失填补的方式进入模型。

## 模型参数

- 推荐分类器：`ExtraTreesClassifier`
- `n_estimators=200`
- `max_depth=18`
- `min_samples_leaf=5`
- `max_features='sqrt'`
- `n_jobs=-1`
- 辅助金额模型：`Ridge(alpha=10.0, solver='lsqr')`
- 金额目标变换：有符号对数变换
- 验证种子：`42, 52, 62, 72, 82`

## 最新测试效果

严格固有属性增强模型测试集：

- 矿主最佳业务命中率：17.96%
- 运营最佳业务命中率：18.35%
- 相对核心固有属性基线：矿主 +0.17 个百分点，`p=0.06497`；运营 +0.04 个百分点，`p=0.90405`
- 新增固有属性相对核心基线暂不能认定有显著增益
- 相对健康约束固定基线的提升仍具有回测显著性，但不代表因果效果或线上 A/B 结果

辅助金额模型测试集：

- 成本 R²：`-0.2986`
- 利润 R²：`-0.1664`
- 当前金额预测无效，业务推荐以分类命中率为主

## 健康数据问题

- SMART 各字段缺失率约 99.59% 至 100%
- ZFS 池状态、数据集读写缺失率均为 100%
- Prometheus 重传覆盖率约 8.71%
- 当前自动通过节点为 0；测试集中 4,086 个节点需人工检查，1,237 个节点无可行健康业务
- 缺失的 SMART/ZFS 不会被当作正常值自动放行

## 主要产物

- `rebuild_multibusiness_model.py`：重建、特征配置、训练和评估脚本
- `多业务节点最佳业务重建报告.md`：最新完整报告
- `multibusiness_metrics.json`：机器可读指标、字段边界和健康规则
- `multibusiness_nodes.csv`：节点属性快照及健康观测字段
- `multibusiness_outcomes.csv`：节点-业务 7 天成本、收入和利润结果
- `multibusiness_recommendations.csv`：训练集和测试集推荐结果及阻断理由
- `multibusiness_eligible_pairs.csv`：符合条件的节点-业务首次出现明细
- `multibusiness_prometheus_probe.json`：Prometheus 指标探查结果
- `multibusiness_feature_comparison_pre_day.json`：核心/增强固有属性对比
- `multibusiness_multiseed_validation_pre_day.json`：五种子节点级验证结果

## 重要限制

1. 现有结果是历史回测，不是因果验证，也没有线上 A/B 实验。
2. 7 天窗口中有大量节点-业务记录不足 7 个观测日，缺失日不能解释为真实业务运行状态。
3. 当前 VM 接口没有与节点上线日前一天对齐的 SMART/ZFS 历史时序，因此这些指标只能做当前健康审核。
4. 健康阈值是可审计的初始规则，尚未得到业务方的业务-资源要求映射验证。
