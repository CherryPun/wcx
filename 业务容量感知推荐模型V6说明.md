# 大节点业务容量感知推荐模型 V6.2 说明

## 1. 文档目标

V5 回答“这个节点在当前画像下更适合哪些业务”，但没有回答“这些业务还能承接多少建设带宽”。
如果把所有节点都交给同一个 Top1，客户业务量级可能无法支撑新增带宽，最终表现为利用率下降。

V6 将这两个问题拆开：

1. V5 继续预测每个节点对各主流业务的矿主单位收益与平台单位利润。
2. V6.2 根据最近一个月的建设带宽、95 峰值流量和利用率，估计四层容量上限。
3. V6.2 在业务、原运营商、调度类型、目标运营商和省份容量上限内对 V5 Top3 做批量约束分配。
4. 输出人工规划方案，不自动修改 NiuLink 现网业务。

这不是用“配额”人为压低高收益业务。只有历史流量证据表明业务量级不足时，容量约束才会阻止继续增加建设带宽。

## 2. 当前真实数据范围

本轮运行时间窗口：

```text
2026-08-02 至 2026-09-01，共 31 天
```

范围与 V5 一致：

- 训练和容量统计对象为大节点候选集。
- 业务候选仅包含主流业务白名单。
- 七牛虚拟业务统一归并为 `七牛CDN-ZJ月95`，规范业务 ID 为 `10000280`。
- 一个节点同一天只允许一个干净的主流逻辑业务。
- 全天存在多个并行业务、主流与非主流重叠、财务归属不清的节点日被剔除。
- 专线和汇聚暂不作为硬准入限制。

本轮数据规模：

| 项目 | 数量 |
| --- | ---: |
| Superset 原始行 | 505,615 |
| 干净节点日 | 279,442 |
| 有效容量流量节点日 | 276,652 |
| 容量流量可用率 | 99.00% |
| 进入容量统计的主流业务 | 26 |
| 容量证据充分业务 | 26 |
| 当前报告节点 | 6,933 |

## 3. 数据源与字段

容量事实来自 Superset `node_day_ops_wide_full`，核心字段如下：

| 字段 | 单位或取值 | 用途 |
| --- | --- | --- |
| `nodeId` | 节点 ID | 节点日主键 |
| `day` | 日期 | 时间窗口、日级事实 |
| `customerId` | 业务 ID | 业务归属及虚拟业务映射 |
| `state` | `online` | 在线业务判定 |
| `stage` | `inService` | 服务中业务判定 |
| `buildBandwidth` | Mbps | 建设带宽和收益率分母 |
| `peak95` | bps | 首选 95 峰值流量 |
| `analyzePeak95` | bps | `peak95` 缺失时的第二流量来源 |
| `peak95Ratio` | 百分数 | 前两项缺失时重建流量 |
| `cost_finalAmount` | 元/天 | 矿主最终收益 |
| `revenue_finalAmount` | 元/天 | 平台收入，减矿主收益后为平台利润 |
| `scheduleISPs` | 运营商列表 | 调度目标运营商；空取节点原运营商，非空只取原始顺序第一个 |
| `transProvRate` | `0` 或 `100` | `0` 为本省，`100` 为出省 |
| `province`、`isp` | 文本 | 区域容量池 |

V5 节点适配输入仍然只使用已确认的十项画像：省份、运营商、调度类型、CPU 档位、NAT 类型、
内存档位、磁盘档位、IPv6 能力、带宽档位、最新一次整体压测丢包满意度。业务流量只进入 V6
容量层，不进入 V5 节点收益预测，避免把业务上线后的结果泄漏到节点画像。

## 4. 节点日流量口径

### 4.1 取值优先级

每条被归属到当天逻辑业务的记录按以下顺序取流量：

```text
peak95
-> analyzePeak95
-> buildBandwidth * peak95Ratio / 100
-> 缺失
```

`peak95` 和 `analyzePeak95` 从 bps 转换成 Mbps。`peak95Ratio` 是百分数，不是 0 到 1 的小数。

### 4.2 异常值处理

若某个测量值满足以下任一条件，则该来源无效并尝试下一来源：

- 小于 0。
- 对应单节点利用率超过 200%。
- `peak95Ratio` 不在 `0~200` 之间。
- 建设带宽缺失或小于等于 0。

200% 是数据质量保护上限，不是业务目标利用率。被回退的异常行保留在审计字段
`capacity_peak95_outlier_rows` 中。

### 4.3 七牛虚拟业务去重

七牛同一节点同一天可能出现多个虚拟业务 ID，并且这些行的 `peak95Ratio` 相同。如果直接求和，
会将同一份节点流量放大多倍。V6 的规则是：

```text
同节点 + 同一天 + 同一规范业务，只取优先级最高来源中的一个最大有效值，不对虚拟行求和。
```

本轮七牛统计：

| 项目 | 数量 |
| --- | ---: |
| 七牛干净节点日 | 64,462 |
| 有效容量流量节点日 | 62,487 |
| 流量可用率 | 96.94% |
| `peak95Ratio` 重建 | 61,698 |
| 直接 `peak95` | 782 |
| `analyzePeak95` | 7 |
| 重复值冲突节点日 | 0 |

## 5. 业务日容量事实

先计算节点日利用率：

```text
node_utilization = effective_peak95_mbps / buildBandwidth
```

再按业务和日期聚合：

```text
active_build_bw = 所有干净节点日建设带宽之和
observed_build_bw = 有有效流量节点日的建设带宽之和
observed_peak95 = 有效节点日 95 峰值流量之和
bandwidth_coverage = observed_build_bw / active_build_bw
observed_utilization = observed_peak95 / observed_build_bw
estimated_total_peak95 = observed_utilization * active_build_bw
```

不能把缺少流量的节点当成 0 流量，否则会系统性压低利用率。V6 先在有观测带宽上计算利用率，
再按带宽覆盖率扩展到完整建设带宽。

每个业务日同时输出：

- 节点数、建设带宽、流量观测节点数和观测带宽。
- 整体利用率、节点利用率 P25/P50/P75。
- 节点利用率低于 50% 的占比。
- `peak95Ratio` 重建占比。
- 矿主收益、平台收入、平台利润及单位建设带宽收益。

## 6. 容量池层级与回退

V6.2 建立并约束四个容量层级：

1. `business`：业务整体。
2. `business_isp`：业务 + 节点原运营商。
3. `business_isp_schedule`：业务 + 节点原运营商 + 调度类型 + 目标运营商。
4. `business_province_isp_schedule`：业务 + 省份 + 节点原运营商 + 调度类型 + 目标运营商。

一个候选节点会同时消耗它命中的全部证据充分容量池。例如浙江移动节点、异网出省且 `scheduleISPs`
首个值为电信，拟交付业务 A 时，
会同时占用：

```text
业务 A
业务 A + 移动
业务 A + 移动 + 异网出省 + 电信
业务 A + 浙江 + 移动 + 异网出省 + 电信
```

四层中任何一个生效容量池无法承接该节点完整建设带宽时，该候选都不能被选择。

细粒度组合容易出现小样本，因此采用层级证据回退：

```text
业务整体、业务+原运营商：新增候选的必需证据层
业务+原运营商+调度+目标运营商：证据充分时加入硬约束，否则回退到业务+原运营商
业务+省份+原运营商+调度+目标运营商：证据充分时加入硬约束，否则回退到上一有效父层
```

回退不是忽略容量，而是避免用不足 7 个有效日的小样本直接把正常候选误判为不可交付。

迁出当前业务和迁入目标业务使用不同的路由依据：当前业务从 `current_business_day` 当天的干净容量事实
确定原容量池，目标业务按节点最新画像确定目标容量池。原运营商与目标运营商分字段保存，
`移动 -> 电信` 和 `移动 -> 联通` 不会共享同一个细分容量池。

本轮容量池覆盖：

| 层级 | 容量池 | 证据充分 | 当前有负载 |
| --- | ---: | ---: | ---: |
| 业务整体 | 26 | 26 | 23 |
| 业务 + 原运营商 | 70 | 69 | 63 |
| 业务 + 原运营商 + 调度 + 目标运营商 | 273 | 240 | 226 |
| 业务 + 省份 + 原运营商 + 调度 + 目标运营商 | 1,512 | 1,119 | 1,038 |

## 7. 容量预测

### 7.1 有效容量日

业务日必须同时满足：

- 带宽观测覆盖率至少 80%。
- 整体利用率在 `0~200%`。
- 估算总 95 峰值流量非空。

一个容量池至少有 7 个有效容量日才作为硬约束。对于新增候选，业务整体和业务+运营商两层都必须
有充分证据；更细层按上一节规则回退。本轮 26 个业务整体容量池均满足。

### 7.2 流量预测与建设带宽上限

默认使用最近 14 天有效业务日的估算总流量 P75：

```text
forecast_peak95 = P75(recent estimated_total_peak95)
target_utilization = 70%
raw_capacity_ceiling = forecast_peak95 / target_utilization
allocatable_headroom = max(raw_capacity_ceiling - current_build_bw, 0)
oversupply = max(current_build_bw - raw_capacity_ceiling, 0)
```

选择 P75 而不是最大值，是为了保留需求波动余量，同时避免单日异常峰值永久抬高容量。
目标利用率 70% 是当前 V6 的可配置规划参数，不是模型训练得到的常数。

### 7.3 不强制迁出现存节点

当任一容量池当前建设带宽已经超过原始容量上限，V6.2 不会为了让数学约束可行而强制迁出节点：

```text
operational_capacity_ceiling = max(current_build_bw, raw_capacity_ceiling)
```

因此超配业务不再新增，但允许保留现状；只有其他节点真实迁出后才形成新的可用空间。

## 8. 带宽、节点数与利用率相关性

每个业务输出以下 31 天历史诊断：

- 建设带宽与利用率 Pearson 相关系数。
- 建设带宽与利用率 Spearman 相关系数。
- 节点数与利用率 Pearson 相关系数。
- 建设带宽与总流量 Pearson 相关系数。
- 每增加 1 Gbps 建设带宽对应的历史边际 95 峰值流量斜率。

部分本轮结果：

| 业务 | 带宽-利用率 Pearson | 带宽-流量 Pearson | 边际流量/建设带宽 |
| --- | ---: | ---: | ---: |
| 腾讯专线 | -0.868 | 0.805 | 0.279 |
| 优酷汇聚 | -0.797 | 0.943 | 0.516 |
| 七牛CDN-ZJ月95 | -0.769 | 0.622 | 0.284 |
| B站Web盒子 | -0.731 | 0.265 | 0.055 |
| 快手严选 | -0.707 | 0.659 | 0.388 |
| B站专线-盒子V2 | -0.292 | 0.806 | 0.783 |

负相关表示在这 31 天里建设带宽增加时利用率倾向下降，符合“业务量跟不上带宽”的现象；但它可能同时
受到日期、商务调量、区域结构和节点质量变化影响，所以只能作为风险诊断，不能解释为增加带宽必然导致下降。

## 9. 容量约束分配算法

### 9.1 可进入批量规划的节点

节点必须同时满足：

- 当前业务已知且在 V5 主流候选集。
- 当前业务不是 V5 Top1。
- V5 动作为“谨慎人工复核”或“优先人工复核”。
- 候选位于 V5 Top3。
- 候选置信度至少为 `medium`。
- 候选平台单位利润点估计大于 0。
- 候选综合分比当前业务至少高 0.03。
- 候选业务整体和业务+运营商容量证据充分。
- 候选命中的更细容量池有充分证据时，也必须满足其容量上限。

当前业务未知的节点不能直接加入容量优化，因为无法可靠地从原业务释放建设带宽。

### 9.2 优化目标与约束

对每个可规划节点建立“保持当前业务、Top1、Top2、Top3”离散选项：

```text
最大化 Σ(节点所选业务综合分 - 节点当前业务综合分)

约束：
每个节点恰好选择一个业务
每个证据充分的业务整体容量池规划后建设带宽 <= operational_capacity_ceiling
每个证据充分的业务+运营商容量池规划后建设带宽 <= operational_capacity_ceiling
每个证据充分的业务+原运营商+调度+目标运营商容量池规划后建设带宽 <= operational_capacity_ceiling
每个证据充分的业务+省份+原运营商+调度+目标运营商容量池规划后建设带宽 <= operational_capacity_ceiling
一个节点只能交付一个业务
```

安装 SciPy 时使用 `scipy.optimize.milp` 求解 0/1 整数规划。没有 SciPy 时使用确定性的分数增益贪心
分配：按增益从高到低遍历节点，Top1 容量不足时尝试 Top2、Top3，否则保持当前业务。两条路径都经过
四层容量不超限完整性检查。当前生产运行使用 MILP；贪心路径是依赖缺失时的可解释降级方案，不能保证
与全局最优方案完全一致。

## 10. 本轮真实规划结果

### 10.1 无容量约束的 Top1 风险

假设将当前业务可识别的节点都切到 V5 Top1，会有 4 个业务超过可操作容量上限：

| 业务 | 无约束 Top1 负载 Gbps | 溢出 Gbps |
| --- | ---: | ---: |
| 字节架构专线V3 | 3,589.900 | 1,594.794 |
| B站专线-盒子 | 2,567.130 | 323.517 |
| 优酷专线 | 566.226 | 115.811 |
| B站专线-mp | 604.187 | 25.028 |

合计无约束溢出约 `2,059.15 Gbps`。这说明仅靠节点适配 Top1 会产生明显的业务集中度与利用率风险。

### 10.2 当前容量状态

| 状态 | 业务数 |
| --- | ---: |
| 仍有容量 | 12 |
| 接近目标 | 6 |
| 建设带宽偏多 | 8 |
| 证据不足 | 0 |

当前建设带宽明显高于预测容量的业务包括 B站盒子、腾讯汇聚P2P、腾讯专线、快手严选、百度小度、
百度XCDN、B站Web盒子和快手直播。这些业务当前不再接收新增带宽，但 V6 不要求自动迁出。

### 10.3 约束后规划

| 项目 | 数量 |
| --- | ---: |
| 当前报告节点 | 6,933 |
| 满足 V5 和容量前置门槛 | 462 |
| 建议人工复核切换 | 335 |
| 其中采用 Top1 / Top2 / Top3 | 307 / 17 / 11 |
| 保持当前业务 | 4,703 |
| 当前业务未知或不在候选集，暂不分配 | 1,895 |
| 有证据容量池约束后超限 | 0 / 1,454 |

与仅约束业务整体的旧方案相比，四层约束使 155 个节点的最终方案发生变化：127 个节点由切换改为保持
当前业务，28 个节点改走其他候选。B站专线-盒子V2 的规划切入数从 142 降至 79，
B站专线-盒子从 75 降至 25，说明业务整体尚有空间时，局部原运营商、调度目标或省份池仍可能已满。

335 个规划切换中，194 个约束到“业务+省份+原运营商+调度+目标运营商”，103 个因最细层证据不足而约束到
“业务+原运营商+调度+目标运营商”，38 个回退到“业务+原运营商”。本轮使用 `scipy.optimize.milp` 求解，HiGHS 返回
`Optimal`，全部 1,454 个证据充分容量池的规划溢出为 0。

这 335 条仍只是“建议人工复核”，不是可以直接执行的切换清单。V5 平台利润时间外 R² 较低，且大量预测区间
跨 0，整个推荐系统当前仍禁止自动切换。

### 10.4 节点级容量报告

节点报告覆盖全部 6,933 个节点，并将容量决策拆成可筛选类别：

| 节点类别 | 数量 |
| --- | ---: |
| 建议切换 Top1 | 307 |
| 建议切换 Top2 | 17 |
| 建议切换 Top3 | 11 |
| 容量约束保留 | 127 |
| 当前已是 Top1 | 886 |
| 其他保持当前 | 3,690 |
| 当前业务未知或不在候选集 | 1,895 |

127 个容量约束保留节点均定位到了具体瓶颈池：省份+原运营商+调度+目标运营商 70 个、
原运营商+调度+目标运营商 45 个、原运营商 12 个。节点报告同时输出节点带宽、当前业务、V5 Top1、容量规划业务、采用排名、综合分增益、
最紧容量池、规划后余量和容量诊断。

## 11. 产物

默认输出目录：

```text
recent_month_large_mainstream_v6_capacity/
```

主要文件：

| 文件 | 内容 |
| --- | --- |
| `v6_node_day_capacity_facts.csv` | 干净节点日容量事实 |
| `v6_node_day_capacity_audit.csv` | 节点日流量来源和清洗审计 |
| `v6_business_capacity_daily.csv` | 业务整体日容量 |
| `v6_capacity_pool_daily.csv` | 四层容量池日事实 |
| `v6_capacity_pool_summary.csv` | 四层容量上限、证据、规划负载和约束审计 |
| `v6_business_capacity_summary.csv` | 业务容量上限、余量和状态 |
| `v6_business_capacity_correlations.csv` | 带宽、节点数、流量、利用率相关性 |
| `v6_capacity_aware_node_allocations.csv` | 节点级容量约束规划 |
| `v6_capacity_node_report.csv` | 全量节点容量诊断，包含瓶颈层级和容量余量 |
| `v6_capacity_review_nodes.csv` | 建议人工复核切换节点清单 |
| `v6_capacity_blocked_nodes.csv` | 满足收益门槛但被容量约束保留的节点清单 |
| `v6_capacity_node_report.json` | 节点报告 JSON 数据 |
| `v6_capacity_allocation_summary.csv` | 业务级无约束与约束后负载对比 |
| `v6_capacity_report.html` | 前端交互报告 |
| `v6_node_report.html` | 节点级筛选、瓶颈诊断和分页报告 |
| `v6_capacity_summary.json` | 本轮口径和摘要 |

## 12. 运行与校验

完整刷新 Superset 数据并生成报告：

```bash
python3 -m venv .venv
.venv/bin/pip install -r requirements-v5.txt

.venv/bin/python v6_capacity_aware_allocation.py build \
  --output-dir recent_month_large_mainstream_v6_capacity
```

复用已下载的原始数据：

```bash
.venv/bin/python v6_capacity_aware_allocation.py build \
  --raw-input recent_month_large_mainstream_v6_capacity/node_day_business_capacity_raw_20260802_20260901.csv \
  --output-dir recent_month_large_mainstream_v6_capacity
```

完整性检查：

```bash
.venv/bin/python v6_capacity_aware_allocation.py check \
  --output-dir recent_month_large_mainstream_v6_capacity

.venv/bin/python -m unittest -v \
  test_v3_daily_business_training.py \
  test_v6_capacity_aware_allocation.py
```

本轮在完整依赖环境执行了全仓 71 项测试，全部通过且没有跳过。安装 `requirements-v5.txt` 后会使用
MILP 全局优化；不安装 SciPy 时自动使用无额外依赖的确定性分配器。

## 13. 当前边界与下一步

1. 当前容量预测是最近 14 天流量 P75，不包含客户正式需求单、合同上限和未来活动流量计划。
2. 四层容量已作为分层硬约束，但省份细分池只有 74.0% 证据充分，其余组合会回退到父层。
3. 31 天相关性样本较短，且存在时间趋势和商务调量混杂，不能当因果关系。
4. `peak95Ratio` 重建提高了七牛覆盖率，但仍应持续与 NiuLink 页面展示值抽样核对。
5. 当前业务未知的 1,406 个节点需要先补齐权威快照，才能进入可释放容量的全局优化。
6. V5 平台利润预测仍是自动化的主要短板；V6 解决“能否承接”，不解决“收益预测是否足够可信”。
7. 后续应接入业务需求单或客户容量预算，形成“历史流量上限 + 商务需求上限”的双重容量约束。
