# wcx2 迭代收尾汇总（新）

## 1. 方向校正（本阶段最重要的反思）

起点是"B站扎堆"，我们一度钻进了容量/时效/评估（护栏），产出多为诊断与否决——
**已校正回"在 RJ 同等权限下做模型优化迭代"**，并保留了容量/时效作为约束与工具。

## 2. 采用配置（已固化为默认）

```text
V5_RECENCY_HALF_LIFE = 7       # 近端时效权重（半衰期 7 天）
V5_RECENCY_WEIGHT_CAP = 2.0    # 权重上限（单线程确定性重扫后的平衡点；原 1.5 作废）
V5_NTHREAD = 1                 # 确定性运行
```

指标（单线程确定性；同窗口 `2026-08-10~09-09`）：

| 指标 | RJ 基线 | 采用配置（HL7+cap2.0） | 变化 |
|---|---:|---:|---|
| 矿主 RMSE | 0.27810 | **0.20020** | -28% |
| 矿主 R² | 0.01965 | **0.02532** | ↑ |
| 平台 RMSE | 0.24255 | **0.19225** | -21% |
| 平台 R² | 0.00218 | **0.00690** | ↑ |
| Top1 集中度 max / HHI | 0.5044 / 0.3550 | **0.4178 / 0.2520** | ↓ |
| V6 全切溢出（同点全量 current） | — | **4,139 Gbps / 4 业务**（切换 206、拦截 87） |

> 早期"cap1.5、集中度 0.3955/0.2454、Top1 9.88%"等数字来自**非确定性运行**，已作废；以本表为准。

## 3. 实验裁决

| 尝试 | 结果 |
|---|---|
| 近端时效权重（HL7+cap2.0，单线程确定性） | ✅ 采用（准确率↑且集中度↓） |
| 量趋势特征 | ❌ 拟合升但集中度恶化 |
| 业务平衡权重 | ❌ RMSE/集中度恶化 |
| 分业务校准 | ❌ Top1/Top3 恶化 |
| 外层/逐节点需求折减 | ❌ 转移超限 |
| 迭代拥堵定价（路线1） | ❌ 不收敛（振荡） |
| 时间线模拟（混合规则） | ✅ 有效：无溢出、给出可切上限/hold；顺序<总量 |
| 四层池时间线 | ✅ 更严：可切 1,477→**1,004（-32%）** |
| 时效 hold（掉量不切） | ⚠️ 降切换/溢出，但命中优势是定义使然，未采作默认 |
| V6 求解器 | ✅ 已含累计容量约束（无需改） |

## 4. 关键诊断结论

1. **B站 10000244 不是容量问题**：V6 官方口径仍有容量、不拦截；在 V5 下仅约 4% Top1；
2. **真正超容是字节系**（基线：1382680008/10000224/10000096）；
3. **召回不是瓶颈**（真优 99.67% 在候选集）；**排序是瓶颈**（真优进 Top1 2.05%）；
4. **92.8% 的 Top1 未命中是被"超容业务"挤掉**；剔除后 Top1 2.05%→**25.77%**；
5. **收益预测 R²≈0、可信度 low** → "是否最佳业务"仍无法离线证明。

## 5. 采用配置容量清单

- 规划切换 **159**（Top1 133/Top2 14/Top3 12）、容量拦截 **91**、planned 溢出 **0**；
- 全切诊断溢出业务：10000096(2,184)、10000183(442)、10000069(159)、10000041(136) Gbps。

## 6. 口径与数据

- 数据窗口 `2026-08-10~09-09`，jarvis `20260910`；大节点=专线/汇聚、非 IDC；主流 allowlist；
- **七牛定义以 RJ canonical `10000280（七牛CDN-ZJ月95）` 为准**；wcx 旧口径 `FQCDNV/10000292` 视为历史。

## 7. 边界与未决

- 可信度 low、**不自动切**；hit 为**观察性口径**，非因果；
- 结论基于本次窗口，数据更新需重验；
- 未做：与 V5 同口径的 OPE/regret 正式评估、七牛权威绑定、线上 A/B（需权限，仅能设计）；
- 默认值已使 wcx2 不等于 RJ 原分支（有意为之）。

## 8. wcx2 交付件索引（均带「（新）」）

- 方案/口径：`wcx2迭代方案（新）.md`、`数据口径与清单（新）.md`、`采用配置（新）.md`、本文件
- 诊断：`P2基线度量（新）.md`、`P2召回审计（新）.md`、`P2错因分桶（新）.md`
- 容量：`P1采用配置清单（新）.md`、`实验与历史记录（新）.md`、`10000244容量核查（新）.md`
- 时效：`掉量时效核查（新）.md`、`实验与历史记录（新）.md`、`实验与历史记录（新）.md`
- 模型：`实验与历史记录（新）.md`、`实验与历史记录（新）.md`、`实验与历史记录（新）.md`
- 可视化：`节点推荐与分布（新）.html`（**RJ v4 报告模板四页**：模型概览 / 节点推荐 / 训练数据 / 集中度，含中文名与预测金额；数据 `节点推荐与分布_data（新）.js`；生成脚本在 `wcx2实验代码（新）/`）
- 实验脚本（一次性）：`wcx2实验代码（新）/`
- 代码改动：`v5_hybrid_recommendation.py`（时效权重默认开 + 上限；趋势/平衡/校准 env 开关默认关）

> 已合并/删除的旧文档不在索引内（其结论已并入上列"实验/否决"文档）。

## 9. 下一步（建议）

1. **把采用配置纳入正式复核**（用 `P1采用配置清单（新）.md` 出人工复核清单）；
2. 若继续提"最优"：做**排序/目标层联合设计**（当前后处理全部失败）或**OPE/regret + 七牛绑定**；
3. 数据更新后滚动重跑并重验采用配置。

> 本文件为阶段收尾索引；细节以各（新）文档为准。
