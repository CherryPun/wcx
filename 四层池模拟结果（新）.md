# 四层池模拟结果（新）

## 1. 做法

在业务层时间线模拟基础上，把容量约束扩展到 **V6 四层池**：
`业务 / 业务+运营商 / 业务+运营商+调度+目标运营商 / 业务+省份+运营商+调度+目标运营商`；
逐节点分配（混合规则：紧迫度→增益密度→随机），**消费/释放四层余量**；仅用"证据充分"池。

## 2. 结果

| 口径 | 切换 | hold | max占比 | HHI |
|---|---:|---:|---:|---:|
| 业务层（对照） | 1,477 | 811 | — | — |
| **四层池** | **1,004** | **1,284** | 0.3088 | 0.1901 |

切换目标分布（四层，前 8）：
`10000183` 310、`10000096` 240、`10000280` 147、`10000244` 72、`10000304` 59、`10000079` 56、`10000065` 52、`1381526443` 37。

## 3. 结论

1. **细分池显著更紧**：可切换由 1,477 → **1,004（-32%）**，hold 升到 **1,284（56%）**；
2. 说明**"业务层有余量" ≠ "细分池有余量"**——真实可切上限必须看四层；
3. 大业务仍被钉住：`10000183` 310、`10000096` 240（远低于推荐 532/956）；
4. **无溢出（按构造）**；顺序影响仍小于总量约束（业务层规则对比已证）；
5. **决策建议**：对外可切量/批次应以**四层口径**为准，业务层口径会高估。

## 4. 限制

- 混合规则单一 seed；未做多 seed 分布；
- 依赖弱模型（R²≈0.025）与容量估计；未做因果验证。

> 脚本：`wcx2实验代码（新）/四层池模拟（新）.py`；结果文本本地产物。
