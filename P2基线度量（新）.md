# P2 基线度量（新）

## 1. 目的

在 P1–P6 之前建立"当前离最优有多远"的基线：模型准确率、可信度、容量稀释，以及**离线能否度量 regret**。

## 2. V5 模型准确率（时间外，RJ 官方摘要）

| 目标 | R² | RMSE | MAE | 90% 区间覆盖 |
|---|---:|---:|---:|---:|
| 矿主单位收益 | **0.0217** | 0.2778 | 0.0302 | 0.888 |
| 平台单位利润 | **-0.0027** | 0.2431 | 0.0240 | 0.906 |

- 选型：矿主=xgboost_standard，平台=xgboost_propensity；
- 冷启动更差（矿主 R²≈0.009，平台 R²<0）。

**结论：收益预测的解释力接近 0**（矿主 R²≈0.02、平台 R²<0），冷启动更弱。

## 3. V5 可信度（官方）

```text
level = low；automated_switching_allowed = false
低可信节点占比 = 90.3%
Top1 平台利润 90% 区间跨零占比 = 83.0%
不通过原因：平台时间外 R²<0.10、低可信>50%、区间跨零>50%
动作分布：暂不推荐执行 2,175 / 谨慎人工复核 113
```

## 4. 推荐集中度与"当前 vs 推荐"

- V5 Top1 集中度：10000096 48.9%、1382680008 22.6%、10000224 19.4%、**10000244 仅 3.6%（82 节点）**；
- current business 对比（快照 1,658 节点）：`current_not_top1` 1,619、`already_top1` 31、`current_unknown` 630、`not_in_candidate` 8；**严格复核切换候选仅 1 个**。

## 5. 排序模型命中（V3 日粒度，V2 测试口径）

| 指标 | 值 |
|---|---:|
| hit@1 | **11.42%** |
| hit@3 | **14.27%** |
| nDCG@3 | 13.00% |
| observed_regret | **0.0（退化）** |
| Top1 分布 | 10000244=45,600（主导） |

- `observed_regret=0` 是**退化**：V3 日粒度每组只有一个观测业务，无法算"相对最优的后悔"。

## 6. 容量稀释（业务层，全切 Top1）

| 业务 | 全切负载/上限(Mbps) | 稀释因子 | 需扩容(Gbps) |
|---|---|---:|---:|
| 1382680008 字节CDN专线 | 3,353,726 / 1,396,795 | 0.42 | ≈1,957 |
| 10000224 字节CDN专线V4 | 1,749,796 / 115,603 | 0.07 | ≈1,634 |
| 10000096 字节架构专线V3 | 3,242,258 / 1,878,728 | 0.58 | ≈1,364 |
| 10000041 B站Web盒子 | 35,040 / 18,603 | 0.53 | ≈16 |

- **稀释因子 = 上限/负载**：<1 表示全切时该业务单节点实际收益会被摊薄（如 10000224 只剩 7% 的可分摊空间）。

## 7. 结论（P2）

1. **当前收益预测解释力≈0、可信度 low、90% 节点低置信** → "该节点跑最佳业务"目前**无从谈起**；
2. **离线无法度量 regret**（观测业务单一，退化）→ 度量必须依赖**反事实/在线 A/B**；
3. **排序未把稀释计入**（稀释因子 0.07~0.58）→ "超容仍选它"的比较当前不可信；
4. 因此 P2 的交付是"**把度量立起来**"，而非已有最优性结论。

## 8. 下一步（进入 P7/排序专项的前置）

- 用**反事实/在线小流量**建立 regret 度量；
- 把**稀释感知收益**纳入排序目标；
- 以"可行集 regret + hit@k + 容量溢出"为主指标。
