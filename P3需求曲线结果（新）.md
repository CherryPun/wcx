# P3 需求曲线结果（新）

## 1. 做法

用 178 天（2026-03-16~09-09）、主流 allowlist 的"业务×日 供给 S / 流量 T"，
拟合饱和曲线 `T = D·(1 − exp(−S/S0))`；门禁：天数≥60、供给 CV≥0.2；
稳定性：前/后两半分别拟合，比较 `D` 差异（≤20% 视为稳定）。

## 2. 结果

- 通过门禁业务 **25 个**；拟合 **R²≥0.3 的 25 个**（拟合优度看似很好）；
- **但 D 参数稳定者仅 1/25**：前后两半的 `D` 相差数量级（例：`1380460970` 8.1e6 vs 1.6e3；`10000088` 1.15e9 vs 4.0e6）；
- 出现明显不合理值：`10000244` 拟合 `D≈1.45e10 Mbps`（14.5 Tbps），属外推失真。

## 3. 结论：**当前数据不支持"拟合饱和曲线"**（门禁不通过）

原因：
1. **不可辨识**：供给 S 的取值范围相对 `S0` 偏窄，数据大多落在近似线性段 → `D` 与 `S0` 存在强替换关系，换半段数据即漂移；
2. **时间趋势干扰**：半年内业务量整体涨跌，跨半年拟合把"时间趋势"误当"供给饱和"；
3. **单日噪声 + 极值**：峰值抖动大，`curve_fit` 易被末端极值主导。

## 4. 替代方案（建议改用）

1. **稳健需求上限**：不做曲线拟合，直接用**近端窗口的流量分位**作为上限，例如
   `D_b = 近 30 天 T 的 P90/P95`（或与 V6 现有 `ceiling = 近14天P75/0.7` 对齐）；
   —— 这正是 P4"简单上限"可直接复用的口径。
2. **经验边际（替代 `v_k`）**：把供给分位分桶，统计每桶内 **`ΔT/ΔS`（边际流量）**，
   用**等渗回归**强制递减 → `v_k ≈ 单位收益 × (ΔT/ΔS)_k`；不依赖 `D/S0` 的联合估计。
3. **分窗口滚动**：在**较短窗口（如 30~60 天）**内拟合/分桶，避免跨期趋势污染；只对"期限内利用率接近上限"的业务启用边际递减。

## 5. 对后续的影响
- **P4（简单上限）**：可直接用"近端分位需求上限"（与 V6 容量上限口径一致），**无需拟合曲线**；
- **P5（凹目标）**：用**经验边际 + 等渗**生成递减单价 `v_k`，而不是从 `D/S0` 推导。

> 脚本：`wcx2实验代码（新）/P3需求曲线（新）.py`；参数表 `P3需求曲线参数（新）.csv`（本地产物，不入库）。
