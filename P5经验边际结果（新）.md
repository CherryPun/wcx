# P5 经验边际结果（新）

## 1. 做法

用 178 天历史，按业务把供给分 5 桶，桶内拟合 `ΔT/ΔS`（边际流量），并用**等渗**强制递减；
再按"业务**当前**建设带宽所处桶"取边际因子 `factor = slope(当前段)/slope(首段)`，
折减候选分后输出 V6 推荐文件。

## 2. 结果

- 可算业务 **38 个**；
- **`factor < 1` 的业务：0 个** —— 即**当前负荷都还落在边际最高（首段）**，没有进入饱和区；
- 因此折减后推荐**与原来完全一致**（无效果）。

## 3. 结论（负 + 有价值的信息）

1. **按"当前负荷"做经验边际折减：无影响** → 说明**各业务当前仍未接近需求上限**；
2. 若改按"**全切 Top1 的未来负荷**"取因子，则等价于之前的**静态折减** —— 已验证会**转移超限**，不可取；
3. 因此：**要体现边际递减，必须在求解器目标里做（P5 的分段线性凹目标），而不是在排序前折减分数**；
4. 同时，P3 已证**曲线参数不可辨识**，分段点只能靠**经验分位**（本脚本的分桶法可复用），不能靠 `D/S0`。

## 4. 与 P4 的呼应
- P4（把上限收到需求）→ 更差（容量损失 36.6%→66.8%）；
- P5（当前负荷边际折减）→ 无影响；
- 两者共同说明：**现行 `P75/0.7` 口径 + 排序，已接近"需求约束下的可行解"**；
  进一步提升要靠**求解器内凹目标**或**平台/收益模型本身**，而非外层调整。

> 脚本：`wcx2实验代码（新）/P5经验边际（新）.py`；输出推荐文件与文本为本地/临时产物。
