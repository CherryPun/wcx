# 最终交付说明：矿主优先 · 相似节点候选 · 诚实参考推荐（v1 主版）

## 1. 定位

给矿主节点推荐业务：候选只来自**相似节点真实跑过的业务**，按**矿主预测 7 天结算**为主序排序，平台利润与容量作软约束。输出 Top1~3 及预测金额，定位为**候选/参考，不承诺“最赚”**。

## 2. 主版结论（时间外验证，新节点=较晚上线、训练未见）

- 测试节点 1035：真实最优业务进 Top1 **35.9%**、进 Top3 **65.4%**
- Top1 平台利润为正比例 **100%**
- 金额（矿主结算）预测 WAPE≈0.82、R²≈0.44（主要短板）

对比：不限制候选（全池打分）时命中≈0，限定到相似节点真实业务后命中 Top1≈36%。方向经诊断验证成立。

## 3. 方法流程

```
输入 node_id
 → 节点画像（nodes_attr_filled.csv）
 → 相似节点召回（画像余弦 Top15）
 → 候选 = 相似节点真实跑过且训练支持度达标的业务（阈值见 stage_e2e 配置）∪ 该节点自身满7天账
 → XGB 预测 cost/profit（direct；训练于按引入日时间切分的早期节点）
 → 排序：pred_cost 主序 + 平台利润<=0 软降权 + 容量超限软降权
 → 输出 Top1~3：业务、预测矿主结算、预测平台利润
```

## 4. 有效性尺子说明

- “命中”尺子 = 该节点历史被分配且结算最高的业务是否进入推荐（候选质量参考），不作为反事实收益证明。
- 有效性验证采用时间外切分（训练未见过测试节点），避免因“训练见过相似节点”造成效果虚高。

## 5. 关键实验证据

- 时间外切分后：全池打分命中≈0 → 证明需候选收窄。
- 相似节点候选后：Top1≈36%、Top3≈65%。
- 双塔召回在随机切分下有效，但时间外冷启动未证明；主版用相似节点召回替代，更简单可解释。
- 带宽辅助分：仅 17/1035 测试节点有自身账数据，开/关几乎无差别（Top1 只变 1 个）→ 保留但注明仅对存量候选生效。
- 金额误差改进拆解：neighbor 特征无效（WAPE 反升 0.84→0.90）；WAPE 改善主要来自 log1p（0.77），代价 R² 下降（0.31）。主版保持 direct（命中优先）。

## 6. 交付文件清单

| 文件 | 说明 |
|---|---|
| `02_main_recommendation/final_top3_e2e.csv` | v1 主版：5171 节点 Top1~3 + 矿主结算/平台利润 |
| `03_variant_ablation/final_top3_e2e_v2.csv` | v2（log1p）对照版 |
| `02_main_recommendation/e2e_metrics.json` | 主版指标 |
| `03_variant_ablation/e2e_v2_metrics.json` | v1/v2 对比 |
| `03_variant_ablation/bw_aux_ab_metrics.json` | 带宽辅助分开/关对比 |
| `03_variant_ablation/cost_improve_metrics.json` | 三变体误差拆解 |
| `06_html/全节点推荐_最终版.html` | 主版全节点推荐总览（含业务名 + “当前在跑(近7天实际)”对照列） |
| `方案定稿报告_矿主优先候选推荐.md` | 设计定稿 |
| 本文件 | 最终交付说明 |

## 7. 局限（须随交付说明）

1. 金额预测 WAPE≈0.82，绝对值仅供排序与参考，不承诺结算精度。
2. “命中”尺子是“真实被分配且结算最高”的业务进不进推荐，属候选质量参考，不是反事实收益证明。
3. 冷启动无真实“未跑业务收益”标签，“更赚”为相似节点统计推断。
4. 容量为近 7 天流量近似，非客户真实容量。
5. 样本：约 5% 节点抽样、满 7 天账、7 天累计口径（非日单位带宽）。
6. **可达量假设（重要）**：模型预测的输入不含“该节点平时实际带宽利用率/可达量”，预测按“业务在相似节点的典型给量”口径。
   因此对当前给量不足/空载/刚切换的节点，预测结算可能明显高于实际到手（例：某节点当前跑腾讯汇聚P2P 近7天实际结算 209 元、利润 −143.67，而模型对同业务预测 7 天结算 3,236 元）。
   推荐应作为“可争取收益”的上限参考，实际到手取决于接入后调度/客户实际派量；落地前建议增加可达量/利用率校准层。
