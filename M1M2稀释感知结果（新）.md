# M1+M2 稀释感知结果（新）

## 1. 做法

- **M1 稀释感知**：对 V5 候选 Top1~3，按"业务 / 业务+运营商 / 业务+运营商+调度"池的**余量**折减分数：
  `adjusted = score × min(1, 池余量 / 本节点建设带宽)`；池余量 ≤0 → 该候选不可行（分数 0）。
- **M2 降档/hold**：取折减后最高分；若当前业务分更高或所有候选不可行 → **维持当前（hold）**。
- 输出 `v5_node_recommendations_m1m2（新）.csv`，交 V6 分配。

## 2. 结果

| 口径 | 基线(V5) | M1+M2 |
|---|---:|---:|
| hold 节点（维持当前） | — | **35（1.5%）** |
| 其中目标池全不可行 | — | 25 |
| 全切溢出业务数 | 4 | **3** |
| 全切溢出总量(Gbps) | **4,955** | **4,573（-7.7%）** |

Top1 分布变化：

| 业务 | 基线 | M1+M2 |
|---|---:|---:|
| 10000096 字节架构专线V3 | 1,118 | 1,245 |
| 1382680008 字节CDN专线 | 516 | 479 |
| 10000224 字节CDN专线V4 | 445 | 303 |
| 10000244 B站专线-盒子V2 | 82 | 149 |

## 3. 关键结论（诚实的局限）

1. **M1 的"逐节点折减"效果有限**：因为每个节点都拿**同一份池余量**去折减，**没有建模"多个节点同时占用同一池"的累计效应** → 池余量被反复使用，折减偏松（所以 10000096 反而略升）。
2. **真正做稀释需要"累计占位"**，而这正是 **V6 solver 已经做的**（边分配边扣减容量池）；因此：
   - **M1 应该实现在 solver 内部**（候选分数随累计占用变化），而不是在 solver 之前对每个节点独立折减；
   - **M2 的 hold 是有效的**，但本数据下仅 25 个节点"目标池全不可行"，所以只 hold 35 个 → 影响小。
3. 本实验的净效果仅 **-7.7% 溢出**，不足以解决字节系超限；**不能替代 solver 的累计约束**。

## 4. 相对基线的优化与不足

- 优化：把"不可行候选"显式置 0 并允许 hold，排序与可行性更一致；溢出略降。
- 不足：**没有解决"累计占用"**，因此大量节点仍会共同涌向同一超容池；`10000244` 因余量大反而略升（属合理：它确实有容量）。

## 5. 下一步（真正的 M1）

- 在 **V6 `optimize_allocations` 内**实现"随累计占用递减的候选分数"（capacity-aware score），
  让排在前面的节点优先占用池余量、后续节点自动降档/ hold；
- 验收：全切溢出下降且**不转移**、planned 溢出 0、hold 比例合理。

> 本文件为实验记录；明细 CSV 为本地产物，不入库。
