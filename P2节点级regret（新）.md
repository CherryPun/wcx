# P2 节点级 regret（新）

## 1. 口径

以"节点在整个窗口内**实际跑过业务的加权单位收益**"为参考集：
- `hit@k`：推荐 Top1/Top3 是否落在该参考集；
- `coverage`：Top1 命中参考集的比例（未命中不计 regret）；
- `regret`：命中时 `(参考集最优 − Top1 实际) / |参考集最优|`。

观察性度量，**非因果**；用于同一口径下的相对比较。

## 2. 结果（2,146 个有观测节点）

| 口径 | hit@1 | hit@3 | 覆盖 | regret |
|---|---:|---:|---:|---:|
| V5 基线 | 5.08% | 40.77% | 5.08% | 0.142 |
| hold | **56.69%** | 74.08% | 56.69% | 0.133 |
| M1M2 | 6.57% | 69.70% | 6.57% | 0.137 |

## 3. 关键解读（重要）

1. **hold 的 hit/覆盖高是"定义使然"**：hold 把 Top1 设为"最近在跑的业务"，
   而该业务通常就在"节点实际跑过集合"里 → 覆盖被机械抬高（56.69%）。**不能据此认为 hold 更好**。
2. **基线覆盖只有 5.08%**：因为推荐本质是**推荐"该节点没怎么跑过的新业务"**，
   而本参考集只含"该节点自己跑过的业务" → **天然惩罚换业务推荐**。
3. **条件 regret 三者接近（0.13~0.14）**：一旦推荐落在该节点跑过的集合里，
   约比"该节点历史最优"低 13~14%——差异不大，说明**问题主要在"选没选到"（覆盖/候选），而非命中后的排序精度**。
4. 结论：**该类"与自身历史比"的离线 regret 不能评价"换业务是否最优"**——需要**反事实/在线 A/B**。

## 4. 对方案的启示

- 当前离线指标**系统性偏向"维持现状"**（hold），对"应切换的新业务"不公平；
- 因此 P1–P6 内的评估无法回答"是否最佳业务"，必须补：
  - **稀释感知的期望收益**（换业务后的真实到手）；
  - **反事实/在线小流量 A/B**（唯一因果证明）；
- 在拿到上述之前，任何"提升命中/降低 regret"的离线数字都需谨慎对待。

## 5. 产物

- 脚本 `P2节点级regret（新）.py`；明细文本本地产物，不入库。
