# 分析报告（新）

本文件合并原多份分析文档（数据口径、容量清单、10000244 核查、P2 诊断、时间线/规则/四层池、掉量核查、P0/P1 预检），仅保留结论与关键数字。

## 1. 数据与口径
- 窗口 `2026-08-10 ~ 09-09`（需求曲线预检另拉到 `2026-03-16 ~ 09-09`，178 天）；
- 大节点 = 专线/汇聚、**非 IDC**；业务 = 主流 allowlist（50 个，观测 36）；
- 七牛虚拟统一到 canonical **`10000280`（七牛CDN-ZJ月95）**；wcx 旧口径 `FQCDNV/10000292` 为历史；
- 时间外 80/20：训练 11,972 / 测试 2,994；评测覆盖画像交集 14,966。

## 2. 容量（采用配置，同点全量 current）
| 指标 | 值 |
|---|---:|
| 规划切换 / 容量拦截 | 206 / 87 |
| planned 溢出 | 0 |
| 全切溢出（诊断） | **4,139 Gbps / 4 业务** |
| 分配级容量损失（P1） | **36.6%** |
| 稀释后期望分值 | 0.5671（名义 0.8413，-32.6%） |

全切溢出业务：`10000096` 3,764、`10000183` 349、`10000069` 22、`10000041` 3 Gbps。
- 上限口径 = 近端流量 P75 ÷ 0.7；**收紧到"需求本身"更差**（损失 36.6%→66.8%）。

## 3. 10000244（B站专线-盒子V2）
- RJ V6 官方口径：**仍有容量、不拦截**（上限 1,503,428 / 当前 1,009,358 / 富余 494,070 Mbps）；
- V5 下 Top1 占比仅 **4.2%**；其"扎堆/掉量"属排序与时效议题，非容量问题。

## 4. 排序诊断（P2）
- **召回非瓶颈**：真优在候选集 **99.67%**；
- **排序是瓶颈**：真优进 Top1 **2.05%**、Top3 30.62%（观察口径）；
- **92.8% 的未命中是被"超容业务"挤掉**；剔除超容业务后 Top1 升至 **25.77%**。

## 5. 时序与规则（可执行性）
- **时间线模拟**（容量消耗/释放）：业务层切换 **1,477** / hold 811；**四层池** 切换 **1,004** / hold 1,284（**-32%**）；
- **规则对比**（同数据只换顺序）：value 1,482 / urgency 1,477 / history 1,552 / random 1,482（切换量相近，max占比 0.238~0.273）；
- **结论**：顺序主要决定"谁拿到"，**可切总量由需求决定**；需求不足必须扩容/客户加量。

## 6. 掉量核查（近7天 vs 前7天 cost）
- `10000096` -38.5%、`10000224` -93.2%、`10000183` -29.3%、`10000244` -7.8%；
- 采用配置的时效权重据此缓解了历史外推（10000244 占比 11.8%→4.2%）。

## 7. 需求侧预检（P0/P1）
- 178 天、185 业务；门禁（天数≥60、供给 CV≥0.2）过 **72 个**；
- 曲线拟合 R² 高但 `D` **不可辨识**（仅 1/25 稳定）→ 不做曲线，改用经验分位/经验边际（结论见 `实验与历史记录（新）.md`）。

## 8. 样本口径与一致性（可复跑检查）

- **清洗守恒（V3 审计）**：audit 364,291 节点日 = clean **281,461** + excluded **82,830**；
  excluded 构成：无主流在跑 57,155、非主流混跑 23,074、未归属财务行 1,423、跨省标记无效 738、
  **多业务 340**、零财务产出 100；**clean 节点日集合 == outcomes 节点日集合（差集 0）**。
- **"一天一个干净业务"**：clean 行 canonical 业务数恒为 1（违例 0）、无虚拟绑定歧义；
  **多业务节点日 340 个被整日剔除**（V5 语义，不做份额拆解）。
- **七牛归并**：clean 中 `selected_business=10000280` 的行 canonical 恒为 `10000280`（65,742 行）。
- **复跑方式**：`wcx2实验代码（新）/一致性检查（新）.py`（含 NaN 陷阱 fixture 自检；全 PASS 退出码 0）。

> ⚠️ **指标口径（重要）**：V5/V6 现有 R²/RMSE 为**全样本**口径，含建设带宽低至 50 Mbps 的节点，
> 小分母极端值主导 SSE。按带宽分层重算：**bw≥500 Mbps** 时矿主 R²=0.237、平台 R²=0.0363，
> 与 RJ 文档 §10.3 同量级（详见 `指标口径对账（新）.md`）。**评估须声明带宽下限。**
>
> ⚠️ **压测特征**：本轮回填覆盖率 **0.0** → 指标**不含**静态压测泄漏；
> 若启用（RJ 口径"最新一次 netbench"复用整窗）才会引入未来信息风险（RJ §5.7）。

## 9. 上线门槛看板（RJ §17 九条）

工具：`wcx2实验代码（新）/门槛看板（新）.py`（默认读采用配置输出目录）。当前结果：

| 门槛 | 判定 | 数值 |
|---|---|---|
| 训练核心画像覆盖率 ≥95% | ✅ | **corenum/memtotal/totaldisksize/ipv6 均 100%**（源=historical-profiles，V5 加载时补入训练帧；此前看板读磁盘 pairs 造成 0% 误报，已修正） |
| 压测特征历史 as-of 对齐 | ❌ | 覆盖 0%（本轮未启用） |
| 矿主时间外 R² ≥0.10 | ✅ | **0.267**（默认口径：bw≥500 + 评估 winsorize；全样本 0.025） |
| 平台时间外 R² ≥0.10 | ❌ | **0.037**（默认口径；全样本 0.007） |
| 冷启动平台 R² ≥0 | ❌ | −0.0183 |
| 90% 区间覆盖 85%~95% | ✅ | 矿主 0.896 / 平台 0.905 |
| 低置信度节点占比 <50% | ❌ | **75.6%**（confidence 标签=low：1730/2288；此前 0.0% 系看板把文本列数值化，已修正） |
| Top1 平台区间跨零 <50% | ❌ | **99.0%** |
| 滚动回测多窗稳定 | ✅ | **8 窗**（默认口径）；矿主 R² 最差 0.261、平台 0.027 |
| 严格切换候选小流量前瞻 | 待定 | 需权限/组织决策 |

**合计：PASS 4 / FAIL 5 / 待定 1 → 仍未达上线门槛，只能作人工参考。**（修正：低置信度 75.6% 为 FAIL；画像覆盖为 PASS。剩余 FAIL 中 3 条指向平台标签弱，需成本归因/结算口径）

口径：默认评估口径 = --min-build-bandwidth 500 --eval-winsorize 1（见 指标口径对账（新）.md、w6复盘（新）.md）。

## 10. 业务级可信度与降级（R6）

工具：`wcx2实验代码（新）/业务级可信度（新）.py`（读 `v5_business_training_metrics.csv`，按 test 支持/双目标 R²/区间覆盖判定）。

- 19 个候选业务中 **12 个判为"证据不足"**（支持 <100、双目标 R² 同时为负、或 90% 覆盖越界）：
  `10000041`(矿 R² −9.75)、`10000088`、`10000304`?*（见脚本输出）*、`10000244`（覆盖 0.842）、`10000003`、`1382649737` 等；
- 产出 `业务阻止清单（新）.txt`（本地产物，可复跑生成）；
- **已在 V5 接入**：`V5_BUSINESS_BLOCKLIST` 指向清单后，命中业务不再给出可执行 Top1。
  实测（w2 窗）：动作分布由 `暂不推荐 2077 / 谨慎复核 174 / 优先复核 37` 变为
  `暂不推荐 2261 / 谨慎复核 27 / 优先复核 0`，共 **2,242 个节点被降级**。
- 该策略**偏保守**（阈值 支持≥100、覆盖 0.85–0.95），阈值可按运营容忍度调整。


## 更正记录（Corrigenda，2026-09-15）

1. 现行默认（以代码为准）：V5_RECENCY_HALF_LIFE=7、V5_RECENCY_WEIGHT_CAP=2.0、V5_NTHREAD=1、--min-build-bandwidth 500、--eval-winsorize 1；说明书 §7.6 仅写连续运行段权重，未记载这些默认。
2. 平台择模赢家：采用产物的 selected_sources.platform_unit_profit = xgboost_propensity（非说明书 §10.3 所述 V4 分层基线）。
3. 画像口径：corenum/memtotal/totaldisksize/ipv6 训练帧覆盖 100%，但来源是最新快照（v4.load_training_pairs 未做 profile_day <= sample_day）→ 覆盖高、非 as-of 对齐；说明书 P0 的约 15% 为 RJ 旧口径。
4. 压测 as-of：已回填 178 天按日压测（snapshots/bench_*.csv），但当前训练路径仍 static（metadata: static latest snapshot reused for every historical day；coverage 0.0）→ as-of 尚未在训练中生效。
5. 用词：hit@1 不得称准确率（采用 hit@1≈4.5%，观察性口径）。
6. 严格切换候选=0 的性质：pipeline_config.json 的 quote 段未传 --current-business，采用跑 current 全 unknown → 0 切换是输入缺失的机械结果，不能作为规则严格的证据。
