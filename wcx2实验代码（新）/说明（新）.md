# wcx2 实验代码（新）说明

> 交付整理后，本目录**只保留报表生成器**；其余一次性探索脚本（M1/M2、P0–P6、时效、容量核查等）已按"仅交付有意义文件"原则移除，其结论与关键数字已固化到仓库根文档（`分析报告（新）.md`、`实验与历史记录（新）.md`）。

## 保留文件

| 脚本 | 用途 |
|---|---|
| `生成RJ模板报告（新）.py` | 由 V5 报告产物重跑 `节点推荐与分布（新）.html` + `_data（新）.js`（RJ v4 四页模板）。用法：`python "生成RJ模板报告（新）.py" [V5输出目录]`，默认取 `recent_month_large_mainstream_v5_sw_7c2.0（新）`；需先按 `采用配置（新）.md` 跑一次 V5 build 生成该目录的报告产物。 |
| `成本归因复算（新）.py` | 复算并校验研发「七牛CDN掉量成本归因」规则（11 项对平校验，全 PASS 才退出码 0）。输入为桌面两张 xlsx，产出结论见 `成本归因口径校验（新）.md`。 |
| `标签可分性诊断（新）.py` | 对单位建设带宽毛利做方差分解（业务/机房/残差），判定平台标签可预测粒度。产出见 `标签可分性诊断（新）.md`。 |

## 采用的模型改动

改动在仓库根 `v5_hybrid_recommendation.py`（参数化、可移植）：
- `V5_RECENCY_HALF_LIFE`（默认 7）
- `V5_RECENCY_WEIGHT_CAP`（默认 2.0）
- `V5_NTHREAD`（默认 1，确定性）
- 实验开关 `V5_TREND` / `V5_BUSINESS_BALANCE` / `V5_CALIB` / `V5_PLATFORM_FEATURES`（默认**全关**）

复现走仓库 README 正式流程 + `采用配置（新）.md`，不要依赖本目录脚本。
