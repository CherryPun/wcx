# B站专线-盒子V2（10000244）大节点容量核查

## 1. 为什么做这份核查

上周结论里，`10000244 B站专线-盒子V2` 被模型大量推荐为 Top1，但其近期业务量在下滑。
如果业务量已经不足以支撑新增节点，继续把它推荐给大量节点，就会出现“模型看起来对、现网无法兑现”的情况。

因此本次只回答三件事（不改模型、只对齐口径）：

```text
1）10000244 在大节点（专线/汇聚，排除机房）上的量是涨还是跌；
2）按 V6 容量口径，它当前还能承接多少建设带宽；
3）当前是否已经超限。
```

数据口径：`test.node_day_ops_wide_full`，数据截至 `2026-09-09`；节点大节点判定使用 `jarvis.node_analysis_data`（分区 `20260910`）。

## 2. 口径与方法

大节点过滤（与 `scan_current_non_idc_large_mismatches.py` 一致）：

```text
state = online 且 stage = inService
且 supplysidedeliverytype ∉ {机房, 小盒子}
且 nodedeliverytype ∉ {droid, h618, aml, n1, openwrt}
且满足其一：supplysidedeliverytype ∈ {汇聚, 专线}
        或 resourcetype ∈ {aggregation, dedicated}
        或 max(bandwidth, actualbandwidth) ≥ 100
        或 (max(bandwidth, actualbandwidth) ≥ 1000 且 corenum ≥ 8)
```

业务容量口径（与 `v6_capacity_aware_allocation.py` 一致）：

```text
单节点日流量回退：peak95 → analyzePeak95 → buildBandwidth × peak95Ratio / 100
                （要求 0 ≤ 值 ≤ buildBandwidth × 2.0）
业务当日合计流量 ≈ 观测节点平均利用率 × 业务全部节点建设带宽
容量上限 = 最近 14 个有效日合计流量的 P75 ÷ 目标利用率 0.70（覆盖率 ≥ 0.80）
超限 = 当前建设带宽 > 容量上限
```

## 3. 结果

### 3.1 大节点子集周趋势（专线+汇聚，排除机房）

| 周 | est 峰值(Mbps) | 节点数 |
|---|---:|---:|
| 2026-07-19 | 55,100 | 57 |
| 2026-07-26 | 69,303 | 71 |
| 2026-08-02 | 63,728 | 76 |
| 2026-08-09 | 65,449 | 76 |
| 2026-08-16 | 73,150 | 79 |
| 2026-08-23 | 76,063 | 86 |
| 2026-08-30 | 77,776 | 86 |
| 2026-09-06 | 68,587 | 88 |
| 2026-09-13（残周） | 66,302 | 96 |

- 峰值日：`2026-08-26`，est ≈ 98,226 Mbps（87 个节点）。
- 最新日：`2026-09-09`，est ≈ 78,214 Mbps（95 个节点）。
- **峰值 → 最新：约 -20.4%**；节点数同期从 87 增到 95（**是量下滑，不是节点退出**）。

### 3.2 当前容量与超限（as_of 2026-09-09）

| 口径 | 容量上限(Mbps) | 当前建设带宽(Mbps) | 结果 |
|---|---:|---:|---|
| 大节点子集（专线+汇聚，排机房） | 110,203 | 122,384 | **过建 12,181** |
| 业务整体（全部交付类型，含小盒子/机房/未知） | 123,288 | 126,704 | 过建 3,416 |

结论：**按大节点口径，10000244 当前建设带宽已经超过容量上限约 12.2 Gbps**，容量层不再给它新增空间；把它继续大批量推荐为 Top1 与现网容量不符。

### 3.3 与上周“掉量大”的对应

- 上周“掉量大”的观察，在大节点子集上对应为“**峰值 → 最新约 -20.4%**”；
- 业务整体（含小盒子）14 日均值口径只到约 -12.7%，被小盒子部分稀释；
- “大”与否是主观判断，本报告只给可核对的量纲与时间窗。

## 4. 限制

- 数据截至 `2026-09-09`，之后的走势未知；
- `jarvis.node_analysis_data` 只覆盖到 10000244 的 1,432/2,427 个节点，其余 995 个节点属性缺失（按 RJ 规则不计入大节点）；
- 容量为规划口径，不改变任何现网交付；
- 本核查不涉及 V5 排序结果，未计算“全部切换会超多少 Gbps”。

## 5. 下一步

- 数据过 `2026-09-09` 后回拉，确认下滑是否延续；
- 将本核查接入 V6 容量层，对 `10000244` 的推荐做容量拦截；
- 如需“全切超限 Gbps”结论，需先跑通 V5 → V6 全链。

> 数据文件：`10000244大节点容量核查周表（新）.csv`

## 勘误（2026-09-10，以 RJ V6 官方口径为准）

本文件第 3.2 节“大节点子集过建 12,181 Mbps”使用的是**本仓库自算的 jarvis 覆盖子集（1,279 节点）与近似分母**；
改用 RJ V6 官方容量链路在最新数据（窗口 `2026-08-10`~`09-09`，候选集来自近月大节点发现）重算后：

- `10000244` 业务整体：容量上限 **1,503,428 Mbps**、当前建设带宽 **1,009,358 Mbps**、**富余 494,070 Mbps**，状态 **仍有容量**，**未超限**。

因此**以 `容量层核查_10000244（新）.md` 的 RJ V6 官方结论为准**；本文件第 3.2 节的“过建”结论作废，仅保留周趋势（第 3.1 节）作为业务量变化的参考。

