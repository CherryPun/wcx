# 时效重排联合容量结果（新）

## 1. 本次执行

1. **温和时效重排**：`时效重排（新）.py`（`FLOOR=0.85`，仅对 `cost_chg ≤ -20%` 的业务折减），输出
   `v5_node_recommendations_rerank（新）.csv`；
2. **接 V6 容量分配**：`v6_capacity_aware_allocation.py build` 用重排后的推荐，复用同一 raw 与参数（0.70/14/7/0.8）。

## 2. Top1 分布变化（重排本身）

| 业务 | 重排前 | 重排后 |
|---|---:|---:|
| 10000096 字节架构专线V3（-38.5%） | 1,118 | **68** |
| 1382680008 字节CDN专线 | 516 | **1,244** |
| 10000224 字节CDN专线V4（-93.2%） | 445 | 426 |
| 10000244 B站专线-盒子V2（-7.8%） | 82 | **170** |

Top1 改变 1,103 个（48.2%）。

## 3. 接 V6 后：全切溢出对比

| 口径 | 基线（V5 原样） | 时效重排（温和） |
|---|---:|---:|
| 全切溢出业务数 | **4** | 3 |
| 合计溢出 | **4,955 Gbps** | **5,139 Gbps（↑）** |
| 1382680008 字节CDN专线 | 1,957 Gbps | **3,562 Gbps（↑↑）** |
| 10000224 字节CDN专线V4 | 1,634 Gbps | 1,574 Gbps |
| 10000096 字节架构专线V3 | 1,364 Gbps | 0（被压出溢出） |
| 10000041 B站Web加速 | ≈0 | 3 Gbps |
| 10000244 | overflow 0 | overflow 0 |
| 拦截节点 | 11 | 9 |

## 4. 结论

- **时效重排把 `10000096` 的溢出消掉了，但代价是把负载推向 `1382680008`，导致总溢出反而上升（4,955 → 5,139 Gbps）。**
- 即**"掉量折减 + 换下一个候选"这个思路不成立**：总量需求超过供给时，换对象只是转移超限。
- `10000244` 两次都是 overflow 0、规划切入 7 个节点，**再次确认它不是容量约束对象**。

## 5. 建议的替代做法

1. **"掉量即不背书切换"（hold 规则）**：若模型 Top1 是显著掉量业务，则维持当前业务（不切），而不是切到 Top2/3。
   预期：不额外给其它业务加压，直接减少无效切换。
2. **容量耦合**：把时效折减放进 V6 候选分数后再由 solver 统一分配（而非先重排再交 solver），让容量约束在排序时即生效。
3. **训练侧近端加权**：从样本权重体现时效，避免对最终分数做硬折减。
4. 任何方案验收都要看**总溢出是否下降 + 是否转移到其他超限业务**，不能只看 Top1 分布。

## 6. 下一步

- 先试 **hold 规则**（改动小、可解释），接 V6 比较总溢出；
- 若仍不降，再考虑训练侧近端加权或容量耦合排序。

> 本文件为实验记录，不代表最终方案；明细 CSV 为本地产物，不入库。
