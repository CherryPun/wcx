# 成本归因与标签诊断（新）

**数据**：桌面 `七牛CDN掉量成本归因修正_研发操作数据_2026-09-06_vs_2026-09-05.xlsx`、
`七牛CDN下钻业务掉量毛利影响及成本分摊调整_2026-09-13_vs_2026-09-12.xlsx`。
**工具**：`wcx2实验代码（新）/成本归因复算（新）.py`（11 项对平校验）、`标签可分性诊断（新）.py`（方差分解 + 安慰剂）。

## 1. 研发归因规则（已从原始列复现，11/11 对平，误差 0）

```
机房级建议调账额   = max(0, 昨日机房总成本 × 前日CDN成本占比 − 昨日CDN成本)
业务项追加成本     = 机房建议调账额 × (该项计费带宽下降G / 机房内CDN下降G合计)
调整后CDN成本      = 昨日CDN成本 + 建议调账额
调整后其他业务成本 = 昨日其他业务成本 − 建议调账额
总账约束           = 各机房调账明细合计 0（不改机房总成本、不改大盘）
```

## 2. 量级（09-06 vs 09-05；另一日 09-13 vs 09-12）

| 指标 | 值 |
|---|---:|
| 受影响机房数 | 48 |
| CDN 计费带宽下降 | 488.72 G |
| 建议转回 CDN 承担成本 | **32,347.26 元** |
| 调整前 / 调整后 CDN 毛利 | −17,612 → **−49,959 元** |
| 另一日：当前模型 / 修正后毛利影响 | −6,793 → **−73,853 元（低估约 10 倍）** |

→ **结论 A（成立）**：当前口径在"掉量分摊"情形下**系统性少摊 CDN 成本**，方向与量级可信
（来自表格自身汇总 + 公式对平，不依赖下述分解）。

## 3. 标签可分性（单位建设带宽毛利 = 毛利 / 节点建设带宽Gbps）

**全量重算**（表 B 03+04，n=2,580；修正后毛利用恒等式 `毛利 − 成本调整金额`）：

| 样本 | eta²(业务) | eta²(机房) | 业务×机房 | 残差 |
|---|---:|---:|---:|---:|
| 表B 修正前 | 0.1648 | 0.2628 | 0.6385 | **0.3615** |
| 表B 修正后 | 0.1808 | 0.2592 | 0.6550 | **0.3450** |
| 表A 基线日 09-12 | **0.0091** | 0.2604 | 0.5067 | **0.4933** |
| 表A 掉量日 09-13 | 0.0723 | 0.0812 | 0.4210 | **0.5790** |

**排序影响（全量）**：负单G毛利占比 44.0%→50.2%；符号翻转 11.4%；行秩相关 **0.883**；业务级秩相关 0.930。

**安慰剂**（组内打乱归属、保持每机房合计=0，300 次）：残差 实际 0.345 vs 随机 0.747（0 分位）；
eta²(业务) 实际 0.181 vs 随机 0.022。

> ⚠️ **更正说明**：本诊断初版误用 `04_CDN追加承担_节点级` 的 `调整后_毛利金额`（**整列 1,528 行全空**），
> 只统计到 03 表 1,052 行（且全为非 CDN 业务），曾报出"残差→0.294、25% 符号翻转、Spearman 0.69"的虚高结论，**已作废**。

→ **结论 B**：归因修正对可分性/排序只有**温和影响**（残差 −4.5%，业务级秩相关 0.93），
且"提升可分性"这一点本身有**同义反复**（规则即机房级重分配）与**无外部锚**的限制，**不能作为标签变好的证据**。

## 4. 对建模的直接结论

1. **结构在"机房"层，不在"业务"层**：eta²(机房) 0.26 恒高于 eta²(业务) 0.01~0.18；
   表A 基线日 eta²(业务)=**0.0091**，与 `platform R²≈0.007` 吻合。
2. **残差始终 0.35~0.58** → 节点级单位毛利大部分方差无法由"业务/机房身份"解释，
   **节点级点预测不可行**；可行粒度是**机房(省×运营商)×业务 + 节点级区间/观察**。
3. 归因修正**能纠业务级成本归属水平**，**不能提升节点级可预测性**（总账恒 0，节点盘子不变）。

## 5. 局限与下一步

**局限**：仅 2 个日对；仅七牛CDN相关机房/业务（选择偏差）；口径为毛利（收入−成本），与 RJ「单位平台利润」需映射；
eta² 为一维无协变量解释度，仅说明"分组结构在哪"。

**下一步**：
1. 停止节点级平台点估计；平台侧聚合到 **机房×业务** 级 + 节点级区间/观察；
2. 若要继续修标签 → 向财务/RJ 要 **08-10~09-09 全窗口**同字段（收入/成本/计费带宽/节点建设带宽/调账）重建 V3 标签并双跑；
3. 若要证明修正"更对"，需 **外部锚**（结算单/发票），否则只能停在"规则自洽"。
