# 标签可分性诊断（新）

**工具**：`wcx2实验代码（新）/标签可分性诊断（新）.py`
**目标量**：单位建设带宽毛利 = `毛利金额 / 节点建设带宽Gbps`；用一维方差解释度 eta² 分解。

## 1. 结果

| 样本 | eta²(业务) | eta²(机房) | eta²(业务×机房) | 残差 |
|---|---:|---:|---:|---:|
| 表B 修正前（n=2,580） | 0.165 | 0.263 | 0.639 | **0.361** |
| 表B **修正后** | **0.270** | **0.453** | 0.706 | **0.294** |
| 表A 基线日 09-12（n=1,559） | **0.009** | 0.260 | 0.507 | **0.493** |
| 表A 掉量日 09-13 | 0.072 | 0.081 | 0.421 | **0.579** |

（eta²(节点) 分别为 0.905 / 0.999 / 0.591 / 0.340，因节点固定效应几乎吞掉全部自由度，属退化值，不解读。）

## 2. 读出的三件事

1. **修正确实让标签更有结构**：残差 0.361→0.294（相对降 ~19%），业务 eta² 0.165→0.270、机房 0.263→0.453
   → 印证"业务间成本归属错"是真实存在的标签问题，**且可修**。
2. **但残差仍高（0.29；表A 0.49~0.58）**，且**机房维度始终强于业务维度**（0.26~0.45 vs 0.009~0.27）
   → **单位带宽毛利的可预测结构主要在"机房/资源"层，不在"业务画像"层**。
3. **表A 基线日 业务 eta²=0.009 ≈ 0**：现有口径下"业务"几乎不解释节点级单位毛利
   → 与 `platform R²≈0.007` 完全一致；**平台模型弱不是拟合不出来，是这个量在业务层根本没什么可学的结构**。

## 3. 对建模粒度的直接结论

- ❌ **节点级平台利润点预测**：即使归因修正后，残差仍近 0.3、机房×业务仅解释 0.7
  → 单节点"预计赚多少钱"不可靠，应保持**区间 + watch**，不做点估计排序;
- ✅ **机房(或 省×运营商)×业务 级**是当前唯一有稳定结构的粒度（机房 eta² 0.26~0.45）
  → 平台侧若要进目标，应按此粒度聚合后再参与比选；
- ⚠️ **业务级单独**不可用（表A eta² 0.009，且同业务跨节点单G毛利 CV 最大到 16）。

## 4. 局限（必须标注）

- 仅 **2 个日对**、仅 **七牛CDN相关机房/业务**（选择偏差），非全网全窗口；
- **修正的机房 eta² 上升有一部分是"同义反复"**：该分摊规则本身就是**机房级**重分配，天然抬高机房解释度；
- 目标量是**毛利**（收入−成本），与 RJ「单位平台利润」口径不同；
- eta² 是**一维、无协变量的解释度**，不代表加入画像特征后无法提升，只说明"分组结构"在哪里。

## 5. 下一步（结论已足够支撑决策）

1. **不再投入"节点级平台点估计"**（残差与机房依赖已封顶）；平台输出改为**机房×业务级预测 + 节点级区间/观察**；
2. 若要继续修标签：向财务/RJ 要 **08-10~09-09 全窗口**的 收入/成本/计费带宽/节点建设带宽/调账，
   按 `成本归因口径校验（新）.md` 的规则重建 V3 标签，再双跑对照；
3. 矿主侧不受此结论影响（矿主 RMSE/R² 的改良来自时效权重，已采用）。
