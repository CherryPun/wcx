# 标签可分性诊断（新）

**工具**：`wcx2实验代码（新）/标签可分性诊断（新）.py`
**目标量**：单位建设带宽毛利 = `毛利金额 / 节点建设带宽Gbps`；用一维方差解释度 eta² 分解。
**样本**：表 B（09-05/09-06）节点级 03+04 全量 n=2,580；表 A（09-12/09-13）节点级 n=1,559。

> ⚠️ **更正说明**：本文件初版误用 `04_CDN追加承担_节点级` 的 `调整后_毛利金额` 列，
> 该列**整列为空（1,528 行 NaN）**，导致"修正后"只统计到 03 表的 1,052 行（且全为非 CDN 业务），
> 得出"残差 0.361→0.294、符号翻转 25%、Spearman 0.69"的**虚高结论**，现已作废。
> 下方为全量重算结果（修正后毛利一律用恒等式 `毛利金额 − 成本调整金额`）。

## 1. 结果（全量重算）

| 样本 | n | eta²(业务) | eta²(机房) | 业务×机房 | 残差 |
|---|---:|---:|---:|---:|---:|
| 表B 修正前 | 2,580 | 0.1648 | 0.2628 | 0.6385 | **0.3615** |
| 表B **修正后** | 2,580 | 0.1808 | 0.2592 | 0.6550 | **0.3450** |
| 表A 基线日 09-12 | 1,559 | **0.0091** | 0.2604 | 0.5067 | **0.4933** |
| 表A 掉量日 09-13 | 1,559 | 0.0723 | 0.0812 | 0.4210 | **0.5790** |

**残差变化：0.3615 → 0.3450（相对 −4.5%）**，eta²(业务) +0.016、eta²(机房) −0.004。

## 2. 结论

1. **归因修正对"可分性"几乎没有提升**（残差仅降 4.5%，业务/机房解释度基本不动）
   → 修正只改**业务间成本归属的水平**，不改变"哪个维度有结构"。
   ⚠️ 且该指标本身有**同义反复**：修正规则就是机房级重分配，天然抬高机房/业务解释度，
   所以"提升多少"不可作为标签变好的证据。
2. **结构在"机房"层，不在"业务"层**：eta²(机房) 0.26 恒高于 eta²(业务) 0.01~0.18；
   表A 基线日 业务 eta²=**0.0091**，与 `platform R²≈0.007` 完全一致。
3. **残差始终 0.35~0.58** → 节点级单位毛利的大部分方差无法由"业务/机房身份"解释，
   **节点级点预测不可行**；可行粒度是**机房(省×运营商)×业务**，节点级只能给区间/观察。

## 3. 安慰剂检验（修正的归属是否只是随机）

组内打乱成本调整归属（保持每机房调整合计=0），300 次：

| 指标 | 实际 | 安慰剂（均值 [p5,p95]） |
|---|---:|---|
| 残差 | **0.3450** | 0.7467 [0.576, 0.900] |
| eta²(业务) | 0.1808 | 0.0219 |

→ 实际归属**远优于随机（0 分位）**，说明研发的调整**内部一致、有方向**；
⚠️ 但这只证明"规则自洽"，**不能证明调整后标签更接近真实**（无外部锚：结算单/发票/机房总账）。

## 4. 排序影响（全量）

| 指标 | 值 |
|---|---|
| 负单G毛利占比 | 44.0% → 50.2% |
| 符号翻转行占比 | **11.4%**（非初版的 25%） |
| 行秩相关 Spearman | **0.8831**（非 0.69） |
| 业务级平均单G毛利秩相关 | 0.9298 |

→ 修正对排序是**温和影响**，不是"重排"。

## 5. 局限

仅 2 个日对；仅七牛CDN相关机房/业务（选择偏差）；口径为毛利（收入−成本），与 RJ「单位平台利润」需映射；
eta² 为一维无协变量解释度，仅说明"分组结构在哪"，不代表加特征后无提升空间。

## 6. 下一步

1. **不投入节点级平台点估计**（残差与机房依赖已封顶）；
2. 平台侧若参与目标 → 聚合到 **机房×业务** 级 + 节点级区间/观察；
3. 若要验证归因修正是否"更对"，需 **外部锚**（结算单/发票）或 **08-10~09-09 全窗口**同字段，否则只能停在"规则自洽"。
