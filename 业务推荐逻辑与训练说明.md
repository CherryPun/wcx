# 业务推荐逻辑与训练说明

## 目标

本项目当前只面向大节点做主流业务推荐。

- 新节点：输入节点 ID 或节点画像条件，输出 Top3 推荐业务、原因和风险。
- 存量节点：对在线、服务中、非机房大节点做重匹配，找出当前业务确实需要调整的节点。
- 业务范围：只统计 `mainstream_business_allowlist.csv` 内业务，非主流业务不进入训练和主统计。
- 决策目标：矿主收益和平台利润各占 50%。

## 当前口径

训练收益已经从旧的 7 天累计窗口改为日粒度。

- 训练粒度：`node_id + business + sample_day`。
- 训练窗口：`sample_day 2026-08-02..2026-09-01`。
- 每条样本只表示某节点在某一天跑某业务的收益，不再把多天揉成一条。
- 现网当前业务窗口：`2026-09-01` 单天。

为了兼容旧模型入口，训练文件里仍保留这些列名：

```text
cum_cost_7d
cum_revenue_7d
cum_profit_7d
```

但当 `outcome_window_days=1`、`outcome_grain=daily_node_business` 时，它们的真实含义是单日值：

```text
cum_profit_7d = 当日 revenue_finalAmount - 当日 cost_finalAmount
```

后续可以再把字段名正式升级成 `cost_1d/revenue_1d/profit_1d`，当前先用标记字段保证模型兼容和口径可追溯。

## 数据来源

训练收益来自 Superset 的 `test.node_day_ops_wide_full`。

- 收入字段：`revenue_finalAmount`
- 成本字段：`cost_finalAmount`
- 平台利润：`revenue_finalAmount - cost_finalAmount`

节点画像来自 Jarvis 表：

- `jarvis.node_analysis_data`
- `jarvis.node_join`
- `jarvis.dial_acc`

运行健康指标来自 Prometheus/VictoriaMetrics，主要用于风险判断。

## 业务范围

当前主流业务池共 60 个业务 ID。

- 七牛：21 个
- 字节：10 个
- 腾讯：8 个
- B站：7 个
- 百度：4 个
- 优酷：4 个
- 快手：4 个
- 爱奇艺：2 个

业务 ID 和中文名称在 `mainstream_business_allowlist.csv` 和 `v1_business_name_map_enriched.csv` 中维护。虚拟业务、签约业务和别名审计结果在 `business_binding_audit/` 中维护。

## 训练样本

最近一月大节点全业务日粒度样本：

- 大节点：1978 个
- 全业务日样本：135403 条
- 全业务 ID：82 个

过滤到主流业务池后：

- 主流日样本：125897 条
- 覆盖节点：1963 个
- 实际有样本的主流业务：45 个
- 被排除的非主流样本：9506 条

## 训练目标

日粒度下，模型在同一个比较单元内做相对比较。

当前比较单元：

```text
outcome_group_id = sample_day + node_id
```

矿主收益得分：

```text
miner_score_norm = 同节点同日内 cost 归一化
```

平台利润得分：

```text
operator_score_norm = 同节点同日内 profit 归一化
```

综合得分：

```text
combined_score = 0.5 * miner_score_norm + 0.5 * operator_score_norm
```

也就是说，现在不再用“某节点某业务上线后 7 天累计收益”作为训练目标，而是学习“什么画像的节点，在某一天跑什么业务的单日收益表现更好”。

## 推荐模型

当前主要使用 V2：`profile_segment_ranker`。

它会根据节点画像做分段统计和泛化推荐，核心画像包括：

- 省份、城市、运营商
- 资源类型：专线、汇聚等
- 带宽桶、实际带宽桶
- NAT 类型、拨号类型
- 交付类型、设备类型
- CPU、内存、磁盘、网络质量风险桶

V2 输出：

- Top1、Top2、Top3 推荐业务
- 推荐原因：命中的画像分段、历史支持度、期望得分
- 风险等级：low、medium、high
- 风险原因：质量风险、样本支持不足、节点画像超出该业务历史范围等

最新 1d 主流大节点模型离线验证：

- hit@1：0.2760
- hit@3：0.5571
- nDCG@3：0.4632
- 评估比较单元：10134 个
- 评估节点：393 个

## 收益率模型对照

已新增一版单位带宽收益率模型，不覆盖默认 1d 绝对收益模型。

训练目标：

```text
effective_bandwidth_mbps = max(actualbandwidth, bandwidth, bw, ...)
cost_per_bandwidth_mbps = cost / effective_bandwidth_mbps
profit_per_bandwidth_mbps = profit / effective_bandwidth_mbps
combined_score = 0.5 * 单位带宽矿主收益得分 + 0.5 * 单位带宽平台利润得分
```

收益率模型的 V2 排序权重也做了调整：

```text
source_rate=0.20
champion=0.10
expected_best_rate=0.20
expected_score=0.50
```

也就是说，这版更看重“这类节点跑这个业务的单位带宽收益率”，少看历史覆盖率。

收益率版离线验证：

- hit@1：0.2600
- hit@3：0.5218
- nDCG@3：0.3730
- observed_regret：0.0397

和默认绝对收益版对比：

```text
默认 1d 绝对收益版：hit@3=0.5571, nDCG@3=0.4632, observed_regret=0.1765
1d 单位带宽收益率版：hit@3=0.5218, nDCG@3=0.3730, observed_regret=0.0397
```

收益率版 Top1 分布明显不同，更偏向单位收益率高的业务：

```text
B站专线-盒子V2: 3581
七牛CDN-虚拟业务10000292: 1020
七牛特招-虚拟业务10000281: 810
字节架构汇聚V2: 752
百度小度: 447
```

当前建议：收益率版先作为对照模型和人工复核参考，不直接替换默认模型。原因是它会显著改变业务流向，尤其会把更多节点推荐到 B站 V2，需要先抽样确认真实可交付性和容量承接。

## 新节点推荐

新节点没有当前业务收益，所以使用 V2 画像模型直接推荐。

```bash
python3 v2_ranking_model.py recommend-node <node_id> \
  --nodes recent_month_large_mainstream_1d/multibusiness_nodes_large_mainstream_recent_1m.csv \
  --model recent_month_large_mainstream_1d/v2_large_mainstream_outputs/v2_ranking_model.json \
  --json
```

条件推荐：

```bash
python3 v2_ranking_model.py recommend-filter \
  --where province=江苏 \
  --where isp=电信 \
  --nodes recent_month_large_mainstream_1d/multibusiness_nodes_large_mainstream_recent_1m.csv \
  --model recent_month_large_mainstream_1d/v2_large_mainstream_outputs/v2_ranking_model.json \
  --limit 20
```

## 存量节点纠偏

存量节点已经有当前业务收益，所以不能只看历史画像推荐。

当前策略：

1. 当前业务未知：`current_business_unknown`。
2. 当前业务非主流：`review_switch_current_not_mainstream`，只单独记录为排除/复核。
3. 当前业务等于模型 Top1：`keep_top1`。
4. 当前业务利润和单位利润优于推荐目标业务现网中位值：`keep_current_realized_value`。
5. 当前业务盈利，但推荐目标风险是 high：`observe_current_profitable_high_risk`。
6. 当前业务在 Top3 但不是 Top1：`review_current_in_top3_not_top1`。
7. 当前业务收益确实差，且模型推荐更合理：`review_switch_candidate`。

这样可以避免类似 `B站专线-盒子V2 -> 百度小度` 被误判：如果 B站 V2 当前真实收益已经很好，就不会因为模型 Top1 不同而建议切走。

## 最新现网扫描

基于 `20260902` 节点快照和 `2026-09-01` 单日当前业务窗口：

- 扫描大节点：6885 个
- 当前有业务节点：6723 个
- 当前主流业务节点：6289 个
- 非主流当前业务节点：434 个
- 当前业务未知节点：162 个
- 模型原始判定当前非 Top1：5350 个
- 策略后当前主流业务仍需处理：1080 个
- 当前真实收益保护：1505 个
- 当前盈利但推荐高风险，转观察：2765 个
- 推荐越出主流业务池：0
- 在线、服务中、非机房、大节点过滤异常：0

动作分布：

```text
observe_current_profitable_high_risk: 2765
keep_current_realized_value: 1505
keep_top1: 939
review_switch_candidate: 834
review_switch_current_not_mainstream: 434
review_current_in_top3_not_top1: 246
current_business_unknown: 162
```

## 主要产物

- `recent_month_large_1d/multibusiness_outcomes_large_recent_1m.csv`：全业务大节点日粒度收益样本。
- `recent_month_large_mainstream_1d/multibusiness_outcomes_large_mainstream_recent_1m.csv`：主流业务日粒度训练样本。
- `recent_month_large_mainstream_1d/v1_training_pairs_large_mainstream_recent_1m.csv`：V1 训练样本对。
- `recent_month_large_mainstream_1d/v2_large_mainstream_outputs/v2_ranking_model.json`：V2 1d 主流推荐模型。
- `recent_month_large_mainstream_1d/v2_frontend_condition_business_report_large_mainstream_recent_1m.html`：前端条件推荐报告。
- `recent_month_large_mainstream_1d_unit_bw/v2_large_mainstream_outputs/v2_ranking_model.json`：V2 1d 单位带宽收益率推荐模型。
- `current_non_idc_large_scan_unit_bw/current_non_idc_large_recommendations_20260902.csv`：收益率版现网推荐明细。
- `current_non_idc_large_scan/current_non_idc_large_recommendations_20260902.csv`：现网 1d 推荐明细。
- `current_non_idc_large_scan/current_non_idc_large_mismatch_candidates_20260902.csv`：策略后仍需处理候选。
- `mainstream_data_statistics/mainstream_data_statistics_report_20260902.md`：主流业务统计报告。

## 复跑命令

重拉近一月 1d 大节点数据：

```bash
python3 -u build_recent_large_training.py \
  --output-dir recent_month_large_1d \
  --outcome-window-days 1 \
  --min-business-support 1 \
  --workers 4 \
  --candidate-chunk-size 800
```

重训 1d 主流业务模型：

```bash
python3 -u build_mainstream_large_training.py \
  --source-dir recent_month_large_1d \
  --output-dir recent_month_large_mainstream_1d \
  --min-business-support 1
```

训练 1d 单位带宽收益率模型：

```bash
python3 -u build_mainstream_large_training.py \
  --source-dir recent_month_large_1d \
  --output-dir recent_month_large_mainstream_1d_unit_bw \
  --target-mode unit_bandwidth \
  --min-business-support 1
```

重跑现网单日重匹配：

```bash
python3 -u scan_current_non_idc_large_mismatches.py
```

使用收益率模型重跑现网单日重匹配：

```bash
python3 -u scan_current_non_idc_large_mismatches.py \
  --output-dir current_non_idc_large_scan_unit_bw \
  --model recent_month_large_mainstream_1d_unit_bw/v2_large_mainstream_outputs/v2_ranking_model.json
```

重跑主流业务统计：

```bash
python3 -u build_mainstream_data_statistics.py
```
