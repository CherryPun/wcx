# 实验与历史记录（新）

本文件合并原多份实验/历史文档，仅保留结论与关键数据；细节脚本见 `wcx2实验代码（新）/`。
（对照基线：采用配置 `HL7+cap2.0+nthread1`，单线程确定性。）

## 1. M_时效权重实验（原）
- 近端时效权重：`样本权重 × clip(0.5^(样本龄/半衰期), cap)`；
- 单线程扫描：`HL7+cap2.0` 为帕累托最优点（矿 RMSE 0.2781→0.2002、集中度 max 0.5044→0.4178）；
- 已固化为默认；回退：`V5_RECENCY_HALF_LIFE=0`。

## 2. 模型改造否决记录（原）
| 方案 | 结果 |
|---|---|
| 量趋势特征 `V5_TREND=1` | 拟合略升、集中度恶化（max 0.6486）→ 否决 |
| 业务平衡 `V5_BUSINESS_BALANCE=0.5` | RMSE 0.571、集中度恶化 → 否决 |
| 分业务校准 `V5_CALIB=1` | 集中度降（max 0.3885）但命中大降（Top1 8.39%→4.80%）→ 否决 |

## 3. 时效折减实验（原）
对掉量业务折减分数后重排：**总溢出反升**（4,955→5,139 Gbps，负载转移到 1382680008）→ 否决。

## 4. 时效 hold 实验（原）
掉量 Top1 → 维持当前：总溢出 4,955→2,006 Gbps（-59.5%）、切换 -66%；
命中上升（hit@1 0.49%→12.80%）**是定义使然**，非质量提升 → 未采为默认。

## 5. 稀释与需求折减实验（原）
| 尝试 | 结果 |
|---|---|
| 逐节点池余量折减(M1M2) | 弱（溢出 -7.7%，余量重复使用） |
| 业务级需求折减 | 转移超限（B站 823、腾讯 812 Gbps）→ 否决 |
| V6 solver 累计约束 | 已存在（MILP，planned 溢出 0）；无需外层折减 |

## 6. 迭代拥堵定价结果（原，路线1）
按池利用率迭代折减分数：**不收敛（振荡于 ~3,000/~550/~380 Gbps）** → 路线1 否决；
建议改走"单一优化目标（凹边际收益）/需求曲线/硬配额"。

## 7. P1 字节系容量清单（原，已取代）
基于旧 cap1.5/非确定性口径的字节系容量清单（溢出 1382680008/10000224/10000096）；
**现口径见 `P1采用配置清单（新）.md`**（采用配置：切换 206、拦截 87、全切溢出 4,139 Gbps）。

## 8. 共性结论
- 后处理类改动（折减/平衡/校准/迭代定价）**均不划算或不收敛**；
- 唯一稳健增益 = **训练侧近端时效权重**（采用配置）；
- 容量/时序上，**需求总量是主要矛盾**（需扩容/客户需求），顺序只决定"谁拿到"。

## 9. P0–P5 改进探索（平台/需求方向；结论：**未改模型**）

> 目的：尝试把"平台利润"与"需求容量"做进模型；结论均为**负或"已到边界"**，故**未改动采用配置**。

| 阶段 | 内容 | 结论 |
|---|---|---|
| **P0 数据预检** | 扩窗 `2026-03-16~09-09`（178 天），业务×日 供给/流量聚合 | ✅ 数据就绪；门禁过 72 业务 |
| **P1 评估体系** | 分配级容量损失、稀释后期望分值、OPE 参考 | ✅ 基线：容量损失 **36.6%**；稀释分 0.5671 vs 名义 0.8413（-32.6%） |
| **P2 平台试点** | 综合分降权（0.5→0.8/1.0）、平台硬闸门 | ❌ 降权更集中、hit@1 更差；闸门把候选全拦 → **应重做平台模型**（归因+定价特征） |
| **P3 需求曲线** | 拟合 `T=D(1−e^(−S/S0))` | ❌ 高 R² 但 **`D` 不可辨识**（仅 1/25 稳定）→ 不可用 |
| **P4 简单上限** | 上限由"需求/0.7"收紧到"需求" | ❌ 容量损失 36.6%→**66.8%**，更差 |
| **P5 经验边际** | 按当前负荷所处供给桶折减分数 | ❌ 当前负荷未近饱和 → 因子全 1，无效果 |

**共同信息**：
1. 现行 `近端P75/0.7` 上限口径 + 排序，**已接近"需求约束下的可行解"**；
2. 真正要做"边际递减"，需在**求解器目标内**做**分段线性凹目标**（分段点用**经验分位**，因曲线不可辨识）；
3. 平台短板需**重做模型**（权威成本归因 + 定价特征），**不是调权重**。
