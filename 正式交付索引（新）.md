# wcx2 正式交付索引（新）

## 1. 交付基线

- 分支：`wcx2`（基于 RJ `e541ac2`）；`master` 为旧 wcx 交付（tag `wcx_v2_delivery`）。
- **采用配置**（已设为默认）：`V5_RECENCY_HALF_LIFE=7`、`V5_RECENCY_WEIGHT_CAP=2.0`、`V5_NTHREAD=1`（确定性）。
- 回退 RJ 基线：`$env:V5_RECENCY_HALF_LIFE="0"`。

| 指标（单线程，窗口 08-10~09-09） | RJ 基线 | 采用配置 |
|---|---:|---:|
| 矿主 RMSE / R² | 0.27810 / 0.01965 | **0.20020 / 0.02532** |
| 平台 RMSE / R² | 0.24255 / 0.00218 | **0.19225 / 0.00690** |
| Top1 集中度 max / HHI | 0.5044 / 0.3550 | **0.4178 / 0.2520** |
| V6 全切溢出 | （旧配置 4,955 Gbps） | **0 Gbps / 0 业务** |

## 2. 交付件

| 类别 | 文件 |
|---|---|
| 方案/口径 | `wcx2迭代方案（新）.md`、`数据口径与清单（新）.md`、`采用配置（新）.md`、`确定性修复与重评（新）.md`、`迭代收尾汇总（新）.md`、本文件 |
| 容量 | `P1采用配置清单（新）.md`、`P1字节系容量清单（新）.md`、`10000244容量核查（新）.md` |
| 诊断 | `P2基线度量（新）.md`、`P2召回审计（新）.md`、`P2错因分桶（新）.md`、`P2节点级regret（新）.md` |
| 时效 | `掉量时效核查（新）.md`、`时效折减实验（新）.md`、`时效hold实验（新）.md`、`稀释与需求折减实验（新）.md` |
| 模型 | `M_时效权重实验（新）.md`、`模型改造否决记录（新）.md` |
| 可视化 | `节点推荐与分布（新）.html` + `节点推荐与分布_data（新）.js`（RJ v4 模板四页） |
| 代码 | `v5_hybrid_recommendation.py`（改）、`.gitignore` |
| 实验脚本 | `wcx2实验代码（新）/`（一次性、审计留痕，见其中 `说明（新）.md`） |

## 3. 数据口径

- 窗口 `2026-08-10~09-09`；大节点=专线/汇聚、非 IDC；业务=主流 allowlist；
- 七牛统一到 RJ canonical `10000280（七牛CDN-ZJ月95）`；wcx 旧口径 `FQCDNV/10000292` 视为历史。

## 4. 复现（数据就绪后）

```powershell
$env:MULTIBUSINESS_SUPERSET_COMMON="...\skills\superset-sql-query\common"
# V5（默认即采用配置）
python v5_hybrid_recommendation.py build --pairs <pairs> --historical-profiles <nodes> \
  --latest-pressure-profiles <pressure> --current-nodes <nodes_v3> --current-business <cur> \
  --source-summary <v3_summary> --output-dir <out> --start-day 2026-08-10 --end-day 2026-09-09
# V6（复用 raw）
python v6_capacity_aware_allocation.py build --source-dir <src> --raw-input <raw> \
  --recommendations <v5_recs> --output-dir <out> ...
# 单测
python -m unittest -v test_v5_hybrid_recommendation.py   # 7/7
```

## 5. 边界与已知问题

- **可信度 low、禁止自动切**；Top1 平台利润区间跨零 >80%；
- hit 为**观察性口径**，非因果；数据截至 `2026-09-09`，需滚动重算；
- `current_business` 存在覆盖缺口（本地近似；同点全量版见 `current_business_同点全量（新）.csv`，本地产物）；
- 实验脚本**仅本地可跑、非复现入口**（已注明）；
- 平台利润模型弱（R²≈0.007）；`1382680008` 等字节系在旧口径下曾超容，新默认下溢出为 0。
