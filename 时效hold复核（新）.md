# 时效 hold 复核（新）

## 1. 做法

在 V5 时间外测试窗口（数据末尾 7 天：`2026-09-03~09-09`，node-day=68,737）上，
比较 **基线(V5 Top1)** 与 **hold(Top1=维持当前业务)**：
- `hit@k` = 推荐 Top1/Top3 是否等于**当日实际跑的干净业务**（V3 日账；按 `sample_weight` 加权）；
- 切换量 = Top1 与当前业务不一致的 node-day 数；
- ID 已归一（去除 CSV 浮点残留 `.0`）。

## 2. 结果

| 口径 | hit@1 | hit@3 | 命中@1数 | 命中@3数 | 切换 node-day |
|---|---:|---:|---:|---:|---:|
| 基线 | 0.49% | 5.24% | 207 | 3,656 | 11,387 |
| hold | **12.80%** | **14.50%** | 7,009 | 8,494 | **3,873** |

## 3. 关键解读（重要，勿误读）

1. **hold 的 hit 上升很大程度是"定义使然"，不是质量提升**：
   - 本指标的 ground truth 是"节点当日实际跑的业务"；hold 把 Top1 设成"节点最近在跑的业务"，
   - 而节点通常**继续跑原业务** → 于是 hold 天然与观测一致，hit@1 被机械抬高（0.49%→12.80%）。
   - 因此**不能据此认为 hold 推荐更准**。
2. 基线 hit@1 仅 0.49%，**明显低于 V5 自身口径（V2 主流约 30%）**，说明本脚本的"逐 node-day 对历史动作"与 V5 的评估口径不一致（V5 按节点/候选集/权重评估），**该绝对值不可与 V5 指标直接比较**，仅用于两方案**同口径相对**对比。
3. 可信的相对结论只有一个：**hold 把切换 node-day 从 11,387 降到 3,873（-66%）**，且容量总溢出从 4,955 → 2,006 Gbps（见 `时效hold联合容量结果（新）.md`）。

## 4. 结论

- **hold 的价值在"减少无效切换 + 降低全切溢出"**，而不是命中数字本身；
- 若要用命中/收益证明 hold 更好，需要：
  - 与 V5 相同的评估口径（节点级、候选集、权重）重算；或
  - **反事实/受控小流量 A/B**（离线模型无法单独证明"换业务更赚"）；
- 本脚本可作为**切换量与方向一致性的监控**，不作为效果结论。

## 5. 限制

- 评估窗口与 V5 训练/校准/测试的具体切分需严格对齐后结论才可比；
- ground truth 为历史动作，不代表因果最优；
- 数据截至 `2026-09-09`。

> 本文件为实验记录；明细/文本为本地产物，不入库。
