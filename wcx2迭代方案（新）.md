# wcx2 迭代方案（新）

## 0. 基座与约定

- 迭代分支：`wcx2`（RJ lineage）；`master` 冻结（tag `wcx_v2_delivery`，仅历史交付）；`machine-test` 远端只 fetch、不可 push。
- RJ 分支已全权交由本项目迭代，**不受 RJ 侧节奏约束**，数据与结论以本项目数据版本为准。
- 命名约定：**wcx2 内所有新生成文件（.md/.csv/.py/...）文件名一律追加「（新）」**；文档风格沿用 RJ 说明文档（中文分节 + 表格 + 口径/结果/限制/下一步）。

## 1. 目标与范围

- 目标：面向**大节点（专线 + 汇聚）**的业务推荐，并在推荐之后加**需求容量层**，避免把大量节点集中到同一业务。
- 范围（按 RJ，不自造）：
  - 节点：`node_size_type=large_node` 且**非 IDC**（排除机房与小盒子）；七牛 CDN **计入**专线/汇聚。
  - 业务：**RJ 主流 allowlist**。

## 2. 口径（全部沿用 RJ 原本口径）

| 维度 | 口径 |
|---|---|
| 粒度 | 日粒度（连续段权重） |
| 收益目标 | 矿主单位收益 `cost/buildBandwidth` + 平台单位利润 `profit/buildBandwidth`，综合分各 50% |
| 带宽 | 流量回退 `peak95 → analyzePeak95 → buildBandwidth × peak95Ratio / 100` |
| 虚拟业务 | RJ 统一 ID（如 `10000280/七牛CDN-ZJ月95`）+ NiuLink 绑定（`NIULINK_AUTH`）；拿不到权威绑定时用 RJ 现有映射并标注“待核” |
| 验证 | 时间序切分（训练 / 校准 / 最终测试），多随机种子 |

> 说明：仅“七牛主档 vs 虚拟业务”的归属定义 RJ 亦标注需与竹鑫对齐；**默认沿用 RJ 口径**，不另起标准。

## 3. 数据快照与时效

- 当前口径：wide `test.node_day_ops_wide_full` 截至 `2026-09-09`；`jarvis.node_analysis_data` 分区 `20260910`。
- 就用当前快照迭代，不等更新；文档注明数据版本。
- 时效约定：容量推演**滚动重算**，数据每更新一版重跑并更新结论；9/9 之后走势不追溯。

## 4. 迭代阶段

### 阶段 1：口径对齐 + 最新数据重建
- 复用 RJ 过滤：`scan_current_non_idc_large_mismatches.py` 的 `current_candidate_sql` + `node_size_type=large_node`。
- 重拉：`build_recent_large_training.py` → `build_mainstream_large_training.py`。
- 产出：`数据口径与清单（新）.md`。

### 阶段 2：需求容量层强化（核心）
- 接入 V6.2 四层容量：业务 / 业务+原运营商 / +调度类型+目标运营商 / +省份；近 14 日 P75 ÷ 0.70；coverage ≥ 0.80。
- 新增“掉量/时效”信号：训练窗口 vs 最近窗口量差折减或时效权重；对已超限业务（如 `10000244`）直接容量拦截。
- 接线：容量约束接在 V5 排序之后做批量分配，输出 `容量拦截清单（新）.csv`。
- 验收：证据充分容量池不超限；`10000244` 不出现在可执行 Top1。

### 阶段 3：排序与目标校准
- 训练窗口刷新到最新；目标维持矿主/平台各 50%。
- 掉量业务用时效 + 容量双重处理，不加排序硬压（放大惩罚已验证无效）。

### 阶段 4：验收指标与交付
- 指标：`hit@1/@3`、`nDCG@3`、`observed_regret`、90% 区间覆盖率、容量超限业务数 / 被拦截节点数。
- 交付：容量约束后的人工复核清单（新）+ 业务容量报告（新）HTML；**不自动切业务**。

### 阶段 5：工程化与协作
- 新模块带 unittest；产出复现说明（新）、数据 manifest（新）。
- 只推 `origin/wcx2`；与 RJ 同步用 `git fetch machine-test`。

### 里程碑

| 里程碑 | 内容 | 产出（新） |
|---|---|---|
| M1 | 口径 + 最新数据重建 | 数据口径与清单 |
| M2 | 容量层接入 + 10000244 拦截 | 容量拦截清单 |
| M3 | V5 → V6 全链验证 | 指标与超限报告 |
| M4 | 七牛定义落地 + 文档 | 待核映射、最终说明 |

## 5. 平台利润模型增强：是否列入及影响

现状：V5 平台利润 R² ≈ 0.026、86.5% 节点 90% 区间跨零 → 综合分里平台一侧近似噪声，排序实际由矿主单位收益主导。

| 方面 | 若列入的可能影响 |
|---|---|
| 排序结果 | 平台分数变稳后 Top1 可能从“矿主高、平台薄”的业务移开，业务更分散 |
| 矿主指标 | 权重此消彼长，`hit@1/@3` 可能小幅下降 |
| 校准 | 0.5/0.5 权重与可信度区间需重定，新旧不可直接比 |
| 依赖 | 需更可靠的成本归因/冲销口径；口径变更需重训且历史不可比 |
| 成本 | 新增平台侧特征与验证，训练/校准期拉长 |
| 与容量层关系 | 容量层是硬约束（吃不吃得下），平台模型是排序质量（排得准不准），**相互独立**；容量层不因平台增强而可省 |
| 风险 | 平台标签本身弱，增强不一定明显改善；可能把“当前超限业务”排更靠前，更依赖容量层兜底 |

**结论**：本轮**不列入**平台利润增强，优先做容量层；平台利润增强作为独立专项，前置条件是先定成本归因口径，并接受“矿主命中可能略降、业务更分散、需重校准权重”。

## 6. 待决事项

1. 七牛“主档 vs 虚拟”定义（与竹鑫对齐）——影响绑定与家族归并；
2. 业务范围是否在 RJ allowlist 上扩展；
3. 是否将平台利润模型增强列为独立专项（见第 5 节）。

## 7. 文件与命名约定

- 新增文档/数据/脚本：文件名追加「（新）」，例如 `数据口径与清单（新）.md`、`容量拦截清单（新）.csv`、`wcx2执行单（新）.md`。
- 文档风格：沿用 RJ 说明文档。
- 提交：只提交到 `origin/wcx2`。
