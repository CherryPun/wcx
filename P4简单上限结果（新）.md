# P4 简单上限结果（新）

## 1. 做法

把业务容量上限从现行口径 `近端流量P75 ÷ 0.70`（即在需求之上留 1/0.7≈1.43 倍建设冗余）
收紧为 `÷ 1.00`（上限≈需求本身），其余不变，重跑 V6 对照。

## 2. 结果

| 口径 | 规划切换 | 容量拦截 | 分配级容量损失 | 全切溢出(诊断) |
|---|---:|---:|---:|---:|
| 上限=需求/0.7（现行） | 206 | 87 | **36.6%** | 4,139 Gbps / 4 业务 |
| 上限=需求（tu=1.0） | **103** | **190** | **66.8%** | 5,427 Gbps / 6 业务 |

## 3. 结论（负）

1. **收紧上限使可切量腰斩（206→103）、拦截近乎翻倍（87→190）、容量损失从 36.6% 恶化到 66.8%**；
2. 原因：`0.7` 本身是"**目标利用率**"口径——它允许建设带宽略高于需求，以保证利用率≥70%；
   把上限压到"需求本身"等于**砍掉了这条冗余**，反而让大量本可执行的切换被拦；
3. （"全切溢出"上升是分母变小所致，属诊断口径，不作为判据；planned 溢出两版均为 0。）

## 4. 判断
- **P4"简单把上限收到需求"不可取**；现行 `P75/0.7` 已是合理的需求感知口径；
- 要真正体现"边际递减"，应改**目标函数**（P5：凹边际），而不是继续动"上限"。

> 运行目录：`recent_month_large_mainstream_v6_cap_tu10（新）/`（本地产物，不入库）。
