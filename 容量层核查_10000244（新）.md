# 容量层核查：10000244（新）

## 1. 目的

用 RJ V6 的容量计算链路（`v6_capacity_aware_allocation.py`）在最新数据上重算
`10000244 B站专线-盒子V2` 的业务容量，核对它是否超限、容量层是否会拦截。

## 2. 方法

```text
load_inputs（候选=recent_month_large_1d（新）的 large_candidate_node_ids_recent_1m.json）
→ prepare_capacity_facts（allowlist + 名称映射 + 绑定）
→ aggregate_capacity_pools（业务 / +原运营商 / +调度 / +省份 四层）
→ build_capacity_pool_summary（近 14 日 P75 ÷ 0.70；coverage ≥ 0.80；valid ≥ 7 天）
```

- 数据窗口：`2026-08-10` ~ `2026-09-09`（宽表截至 09-09）。
- 参数：`target_utilization=0.70`、`forecast_days=14`、`minimum_valid_days=7`、`minimum_bandwidth_coverage=0.80`。
- 执行脚本：`v6容量核查_10000244（新）.py`；产物在 `recent_month_large_mainstream_v6_capacity（新）/`。

## 3. 结果：业务整体（as_of 2026-09-09）

| 字段 | 值 |
|---|---:|
| 容量状态 | **仍有容量** |
| 有效容量天 | 31 |
| 近 14 日覆盖率 | 99.9% |
| 预测峰值 P75 | 1,052,400 Mbps |
| 容量上限 | **1,503,428 Mbps（≈1,503 Gbps）** |
| 当前建设带宽 | 1,009,358 Mbps（≈1,009 Gbps） |
| 当前估算峰值 | 738,755 Mbps |
| 当前利用率 | 0.7319 |
| 节点利用率中位 | 0.9909 |
| 可新增（headroom） | **494,070 Mbps（≈494 Gbps）** |
| 过建 | 0 |

各层级均为“仍有容量 / 接近目标”，无超限：

| 层级 | 状态 | 上限(Mbps) | 当前(Mbps) | 余量(Mbps) |
|---|---|---:|---:|---:|
| 业务整体 | 仍有容量 | 1,503,428 | 1,009,358 | 494,070 |
| 业务+移动 | 仍有容量 | 1,164,345 | 762,315 | 402,030 |
| 业务+联通 | 仍有容量 | 257,315 | 183,613 | 73,702 |
| 业务+电信 | 仍有容量 | 80,783 | 63,430 | 17,353 |
| 移动·本网本省·移动 | 接近目标 | 51,710 | 49,180 | 2,530 |
| 电信·本网本省·联通 | 接近目标 | 17,023 | 16,850 | 173 |

## 4. 若按 V2 主流近月推荐全切

V2 主流近月推荐文件里 Top1=10000244 的节点：**268 个**，合计建设带宽 **≈255.4 Gbps**。
对照业务富余 494 Gbps：**不发生溢出**，容量层不会因此拦截。

> 说明：V6 的“假设全切溢出”正式口径需 V5 推荐集；本节用 V2 主流推荐近似。

## 5. 结论与订正

- 在 `2026-09-09` 数据上，RJ V6 容量层判定 `10000244` **仍有容量（富余约 494 Gbps）**，**不超限、不拦截**。
- 这与本仓库早前文档 `B站专线-盒子V2（10000244）大节点容量核查（新）.md` 中“过建 12,181 Mbps”的结论**不一致**：早前用的是 `jarvis` 覆盖到的子集（1,279 节点）且分母口径不同；**以本节 RJ V6 官方口径为准**，早前文档已加勘误说明。
- 业务层仍有容量，但**节点利用率中位 0.9909**（多数节点接近满），且业务量近两周下滑 → 若后续继续掉量或大批量切换，仍可能转为超限，需滚动重算。

## 6. 下一步

- 跑通 V5 → V6 全链，得到正式的“假设全切 Top1 溢出 / 拦截节点”结论；
- 数据过 09-09 后重算容量，确认是否转为超限。

> 明细：`v6业务容量摘要（新）.csv`（全部业务一行）。
