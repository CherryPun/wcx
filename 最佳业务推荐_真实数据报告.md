# 最佳业务推荐真实数据报告

## 1. 结论

本次模型可以正常完成训练和推荐，但运营方向目前不能作为可上线结论：推荐业务在测试节点真实结果中的覆盖率只有 0.34%，大多数推荐业务没有在该节点的 7 天窗口内被观测到。矿主方向的已观测推荐条件 regret 为 3.88%，但覆盖率也只有 0.40%，因此不能把 96.12% 价值捕获外推到全部测试节点。

当前最重要的改造方向是提高“推荐业务在测试节点上可验证”的覆盖率，或建立业务分配实验/历史业务承接约束，再评价运营价值。

## 2. 数据口径

| 项目 | 本次实际使用 |
|---|---|
| 成本/收入 | `yzh-starrocks`，database_id=19，`test.node_day_ops_wide_full` |
| 粒度 | `(nodeId, customerId, day)`；业务 ID 使用 `customerId` |
| 成本 | `cost_finalAmount` |
| 收入 | `revenue_finalAmount` |
| 节点属性 | `jf-jarvis`，database_id=2，`jarvis.node_join` |
| 上线日 | 宽表首次出现日（本次所有结果来源为 `first_appearance`） |
| 7 天窗口 | `[online_day, online_day + 6]` |
| 抽样 | ID 末两位为 `00` 至 `0c`，约 5.08%，分后缀和上线日批量查询 |

宽表原始历史范围为 2026-03-16 至 2026-08-04；本次建模上线日范围为 2026-04-01 至 2026-07-28。`serviceTime` 查询路径已保留为可选配置，但由于 `node_join` 的节点条件查询耗时过高，本次没有启用，不能把本次结果描述成 serviceTime 口径。

## 3. 特征和目标

节点特征全部来自上线日当天 `node_join` 分区的最新小时快照，保证快照日不晚于上线日。

类别特征为：

`vendorid`、`deliverytype`、`resourcetype`、`dialtype`、`nattype`、`scheduleisps`、`regsource`、`customermode`、`province`、`isp`、`city`、`device_type`、`arch_type`、`isvm`、`qoskiller_status`。

数值特征为：`bw`。

目标为两个回归目标：

- 矿主目标：`cum_cost_7d`，选择预测成本最大的业务。
- 运营目标：`cum_revenue_7d - cum_cost_7d`，选择预测利润最大的业务。

低基数类别和业务 ID 使用 One-Hot；高基数类别使用训练集频率编码。没有使用上线后 7 天的带宽、质量或收入统计，避免目标泄漏。

## 4. 数据规模和质量

| 指标 | 数值 |
|---|---:|
| 节点×业务结果行 | 202,483 |
| 结果节点数 | 63,156 |
| 结果业务数 | 128 |
| 属性节点数 | 63,108 |
| 属性节点覆盖率 | 99.92% |
| 属性快照晚于上线日 | 0 |
| 成本和收入均为 0 的结果行 | 20,723 |
| 成本和收入总和均为 0 的节点 | 4,265 |
| 属性原始缺失单元格 | 253,690 |

属性缺失来自源表本身，例如 `scheduleisps`、QoS 状态、拨号和 NAT 字段。类别缺失作为缺失类别编码；数值缺失按模型编码器的既有规则处理，没有用未来快照补齐。

## 5. 时间回测

- 训练集：`online_day < 2026-07-05`，57,565 个节点，180,645 条训练行。
- 测试集：`online_day >= 2026-07-05`，5,309 个节点，20,099 条测试行。
- 全部合并后业务数为 126 个。
- 训练样本支持数至少 50 的候选业务为 69 个。
- 模型：两个 `HistGradientBoostingRegressor`，成本模型和运营利润模型各一个。

## 6. 测试结果

| 指标 | 矿主模型 | 运营模型 |
|---|---:|---:|
| hit-rate@1 | 0.38% | 0.30% |
| hit-rate@3 | 1.05% | 1.77% |
| 推荐真实覆盖率 | 0.40% | 0.34% |
| 已观测推荐条件 regret | 3.88% | 105.36% |
| 已观测推荐条件 value capture | 96.12% | -5.36% |

这里的 regret 和 value capture 只在推荐业务确实出现在该测试节点结果中的样本上计算；未观测业务不能假设成本或收入为 0，因此不计入条件 regret。低覆盖率意味着这些指标不能代表全体测试节点。

作为简单基线，训练集成本最佳业务中最常见的是 `10000182`。它在测试集的矿主命中率为 4.14%，覆盖率为 14.26%，条件矿主 regret 为 40.43%。运营基线的最佳值经常接近 0，regret 分母不稳定，结果为 66050.72%，仅作诊断，不作为业务结论。

## 7. 业务解释

节点静态属性可以帮助模型学习不同节点类型的业务价值差异，但当前数据是“历史已承接业务”的观察数据，不是随机业务分配实验。对没有承接过的节点×业务组合，模型是在做冷启动外推，测试集没有真实反事实结果可验证。

因此当前结果只能支持：

1. 矿主方向在少量可观测推荐上有一定价值排序信号，但全量覆盖不足。
2. 运营方向当前没有足够覆盖支持上线，尤其不能依据负 value capture 做业务决策。
3. 低 hit-rate 不能单独解释为模型失败，也不能用条件 regret 单独宣称模型成功；必须同时看真实覆盖率。

## 8. 后续改造建议

1. 建立“可分配业务集合”或业务承接约束，推荐只在节点可承接/历史可验证的业务中排序，并同时输出冷启动推荐和可验证推荐。
2. 通过线上 A/B 或准实验记录节点实际被分配到的业务，获得未观测组合的反事实结果。
3. 补充上线前可知的、更有区分度的库存和能力属性；Prometheus ZFS/SMART 指标在当前 node_id 映射未完全打通前不要强行 join。
4. 对运营目标增加稳定的金额阈值和分母规则，分别报告正利润节点、全体节点和已观测节点，避免接近 0 的利润导致 regret 爆炸。
5. 在覆盖率提高后，再比较排序模型、pairwise loss 和业务组合推荐。

## 9. 产物

- `outcomes_raw.csv`：真实 7 天成本/收入结果。
- `nodes.csv`：上线日当天 `node_join` 属性快照。
- `recommendations_real.csv`：5,309 个测试节点的推荐结果。
- `metrics_real.json`、`model_diag.txt`：机器可读指标和诊断。
- `build_outcomes.py`、`build_attributes.py`、`train_real.py`、`model.py`、`schema.py`：复现脚本。
