# M1 确认：V6 求解器已含累计容量约束（新）

## 1. 结论

**"稀释/累计占用"不需要在 V6 之外新增折减**——RJ V6 的分配器（`optimize_allocations`，`v6_capacity_aware_allocation.py:581`）
已用 **MILP** 实现"边分配边扣减池余量"。

## 2. 依据（代码事实）

```text
决策变量：每个 (节点, 候选) 是否被选
约束①：每节点最多选 1 个候选
约束②：每个容量池的累计负载 ≤ 容量上限
        upper = effective_cap_limit - fixed_load
        负载按节点建设带宽累加
目标：最大化 分增益（含置信度/排名微调）
节点始终含"维持当前(rank0, gain0)"选项 → 池满时自动落回
```

- 结果中 `planned_active_pool_overflow_mbps = 0`、`planned_change_nodes = 57`（远小于可切换节点）；
- `unconstrained_top1_overflow`（如 4,955 Gbps）仅为"若全切 Top1"的**诊断值**，不是最终方案。

## 3. 为什么此前"M1 外层折减"效果有限

- 逐节点折减是**每节点独立**使用同一份池余量 → 重复占用、偏松；
- 求解器是**全局累计**约束 → 正确做法已在其中；
- 实测外层折减只降约 7.7% 溢出，正说明它不如求解器。

## 4. 因此

- **不改 V6 求解器**（避免重复/无谓改动）；
- "真正的 M1"应落在**模型打分侧**（V5 的目标/特征加入需求与稀释感知），或**评估侧**（P2 度量）；
- 这两个都不会改变"分配器正确性"，而是改善"分数是否可信/是否最优"。

## 5. 可选（唯一有意义的小改）

把 `minimum_score_gain` 由常数改为**随池紧张度调整**（池越紧、切换门槛越高），属调参，收益有限，非必需。

> 本文件为结论记录，不含代码改动。
