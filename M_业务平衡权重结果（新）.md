# 业务平衡权重结果（新，排序改造实验）

## 1. 做法

在 V5 样本权重上按业务归一化，抑制少数业务对 loss 的支配（env `V5_BUSINESS_BALANCE=alpha`，默认关闭）：
```text
bfactor = (mean_business_total_weight / business_total_weight)^alpha  ，再归一
```
基础配置：`V5_RECENCY_HALF_LIFE=7`、`V5_RECENCY_WEIGHT_CAP=1.5`。

## 2. 结果（V5 时间外）

| 配置 | 矿主 R² | 矿主 RMSE | 平台 R² | 平台 RMSE | Top1 max占比 | HHI | 有效业务数 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 基线 | 0.0217 | 0.2778 | -0.0027 | 0.2431 | 0.4886 | 0.3296 | 3.03 |
| **HL7+cap1.5** | **0.0291** | **0.1998** | **0.0063** | **0.1923** | **0.3955** | **0.2454** | 4.07 |
| +bal0.5 | 0.0122 | 0.5671 | 0.0105 | 0.4687 | 0.6849 | 0.4813 | 2.08 |
| +bal1.0 | -0.0001 | 1.0544 | 0.0026 | 0.8332 | 0.7465 | 0.5691 | 1.76 |

## 3. 结论

- **业务平衡权重全面变差**：矿主 RMSE 0.1998→0.5671（bal0.5）→1.0544（bal1.0）；
  集中度也恶化（max 0.40→0.68→0.75），有效业务数下降。
- 原因：把业务权重强行拉平**破坏了标签尺度与观测分布**，低量业务主导梯度 → 预测更差、分数更偏。
- **否决，不采用**；最优仍为 **HL7+cap1.5（不带平衡）**。

## 4. 本轮"排序改造"裁决汇总

| 尝试 | 结果 |
|---|---|
| 近端时效权重 HL7+cap1.5 | ✅ 采用（RMSE↓、R²↑、集中度↓、V6 溢出 -41%）|
| 量趋势特征 | ❌ 拟合升但集中度恶化 |
| 业务平衡权重 | ❌ RMSE 大幅恶化、集中度恶化 |
| 外层需求折减 / 逐节点折减 | ❌ 转移超限 |

**结论**：在 RJ 现有建模路径下，**唯一稳健的增益来自训练侧近端时效权重**；
"抑制少数业务/加趋势"这类改动需更精细设计（如**校准层**而非重加权），或转做**评估与容量护栏**。

> 产物：`v5_hybrid_recommendation.py`（改：`V5_BUSINESS_BALANCE` 开关）、本文件；训练产物本地不入库。
