# 阶段对比：RJ 原分支 / 时间线前 / 时间线后（新）

## 0. 三个状态

| 代号 | 含义 | commit |
|---|---|---|
| **S0** | RJ 原分支 | `e541ac2` |
| **S1** | 本项目"做时间线前"（确定性修复 + 采用配置 + 基线对照 + 报告） | `426fa03` |
| **S2** | "做时间线后"（时间线/规则/四层池/拥堵定价探索） | `121af11` |

## 1. 仓库与代码改动

| 维度 | S0 (RJ) | S1 | S2 |
|---|---|---|---|
| 归属 | `yanzhuxin/machine-test` RJ | `CherryPun/wcx` wcx2 | 同 S1 |
| 相对 S0 文件变更 | — | **42 文件 +2,816 行** | S1 + **8 个新文件**（4 脚本/4 文档） |
| 核心代码改动 | — | `v5_hybrid_recommendation.py`（时效权重/上限/nthread/实验开关）、`.gitignore` | **无**（纯分析） |
| 模型默认 | recency 关；`nthread=6`（非确定） | `HL7+cap2.0`、`nthread=1`（确定） | 同 S1 |

## 2. 模型指标（同口径，单线程）

| 指标 | S0 (RJ 默认) | S1 = S2（采用配置） |
|---|---:|---:|
| 矿主 RMSE / R² | 0.27810 / 0.01965 | **0.20020 / 0.02532** |
| 平台 RMSE / R² | 0.24255 / 0.00218 | **0.19225 / 0.00690** |
| Top1 集中度 max / HHI | 0.5044 / 0.3550 | **0.4178 / 0.2520** |

> S1 与 S2 **模型指标完全相同**——时间线工作只增加分析与机制，不改模型。

## 3. 容量/时序能力与结论

| 维度 | S0 | S1 | S2 |
|---|---|---|---|
| 容量诊断 | RJ 自带 V6，但无本项目结论 | ✅ 采用配置 vs 基线同口径对照 | ✅ 维持 |
| V6（同点全量 current） | — | 切换 206 / 拦截 87 / 全切溢出 **4,139 Gbps**（基线 6,265，-34%） | 同 S1 |
| 时序模拟 | ❌ | ❌ | ✅ 业务层：切换 **1,477** / hold **811** |
| 四层池时序 | ❌ | ❌ | ✅ 切换 **1,004** / hold **1,284**（**-32%**） |
| 规则对比 | ❌ | ❌ | ✅ 顺序影响"谁"，总量 ~1,480–1,550（随机窄） |
| 拥堵定价（路线1） | ❌ | ❌ | ❌ 不收敛（振荡），已否决 |
| 可视化 | RJ 模板（无本次数据） | 本次数据四页报告 + 名称/金额 | 同 S1（当前业务覆盖 142 unknown） |

## 4. 结论层面的差异

- **S0 → S1**：从"RJ 原样"变为"**确定性 + 采用配置 + 同口径证据链**"；
  确立：准确率↑、集中度↓、全切溢出 -34%（6,265→4,139）；并发现/更正多处（cap1.5 误判、0 溢出假象、非确定性）。
- **S1 → S2**：在 S1 结论上补齐**"时序/可执行性"**维度：
  1. **可切上限被容量钉死**：业务层 1,477 / 四层 1,004；`10000096` 仅 ~200–240（推荐 956）；
  2. **hold 占 35%~56%**：多数节点在容量内不应动；
  3. **顺序 << 总量约束**：换排序只改"谁拿到"，不改可切总量；
  4. **需求缺口是主要矛盾**（10000096/10000183），需扩容/客户需求，非排序可解。

## 5. 一句话

- **S0 = RJ 原样**（弱模型、非确定、无本项目分析）；
- **S1 = 模型固定（采用配置）+ 证据链**（准确率/集中度/容量对照）；
- **S2 = 再加"时序可执行性"**（可切上限、hold、顺序作用、需求缺口），**模型指标不变**。

> 相关文档：`采用配置（新）.md`、`确定性修复与重评（新）.md`、`时间线模拟结果（新）.md`、`规则对比结果（新）.md`、`四层池模拟结果（新）.md`、`迭代拥堵定价结果（新）.md`。
