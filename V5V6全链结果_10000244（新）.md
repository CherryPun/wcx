# V5→V6 全链结果：10000244（新）

## 1. 目的

按 RJ 完整链路（V3 日粒度 → 压力画像 → V5 混合模型 → V6.2 容量分配）在最新数据上，
给出 `10000244 B站专线-盒子V2` 的正式容量/全切溢出/拦截结论。

## 2. 链路与输入（均在本机用最新数据重建）

```text
V3 日粒度加权：build_v3_daily_business_training.py（复用已拉 raw，窗口 2026-08-10~09-09）
压力画像：fetch_latest_node_pressure.py（snapshot 2026-09-09，覆盖 12,273 节点）
V5 混合模型：v5_hybrid_recommendation.py build（输出 v5_node_recommendations.csv）
V6.2 容量分配：v6_capacity_aware_allocation.py build（复用 raw + V5 推荐）
```

- 产物目录：`recent_month_large_mainstream_v3_daily_weighted（新）/`、`recent_month_large_mainstream_v5_hybrid（新）/`、`recent_month_large_mainstream_v6_capacity_full（新）/`（大文件不入库）。
- `current_business` 快照因现网扫描查询超时，改用 V3 日账最新日（2026-09-09）本地构造。

## 3. 结果

### 3.1 10000244 业务容量与全切压力（as_of 2026-09-09）

| 字段 | 值 |
|---|---:|
| 容量状态 | **仍有容量** |
| 容量上限 | 1,503,428 Mbps |
| 当前建设带宽 | 1,009,358 Mbps |
| 富余 | 494,070 Mbps |
| 全切 Top1 负载（unconstrained） | 1,011,882 Mbps |
| **全切 Top1 溢出** | **0** |
| 约束后负载 / 余量 | 1,027,858 / 475,570 Mbps |
| 规划切入 / 切出节点 | 7 / 0 |

**结论：10000244 在当前数据下不超限、不被容量拦截。**

### 3.2 真正全切溢出的业务（4 个）

| 业务 | 全切溢出 | 负载 / 上限 |
|---|---:|---:|
| 1382680008 字节CDN专线 | ≈1,957 Gbps | 3,354 / 1,397 |
| 10000224 字节CDN专线V4 | ≈1,634 Gbps | 1,750 / 116 |
| 10000096 字节架构专线V3 | ≈1,364 Gbps | 3,242 / 1,879 |
| 10000041 B站Web加速 | ≈0（微量） | 35 / 19 |

- 容量约束后：规划切换 57 个节点（Top1 52 / Top2 3 / Top3 2），拦截 11 个节点。
- 被拦截节点的模型 Top1 主要是 **10000096（10 个）** 与 10000183（1 个），与 10000244 无关。

### 3.3 与先前判断的关系

- 更早文档 `B站专线-盒子V2（10000244）大节点容量核查（新）.md` 第 3.2 节的"过建"结论已作废（子集口径）；
- 本文件与 `容量层核查_10000244（新）.md` 一致：**10000244 不构成容量约束对象**。

## 4. 解读

- `10000244` 的"扎堆/掉量"是**排序与准确性问题**（V2/V3 把它排得过多），**不是需求容量问题**；用容量层压不住它。
- 当前真正被容量约束的是**字节系 CDN/架构专线**（1382680008 / 10000224 / 10000096）：全切会显著超限，容量层已拦截相关节点。
- 因此：对 B站的处置应放在**排序/时效**层（如近期掉量业务的时效权重或真优校准），对字节系才是容量层的用武之地。

## 5. 限制与下一步

- `current_business` 为本地近似（非现网权威扫描，后者查询超时）；
- 数据截至 `2026-09-09`，需滚动重算；
- 下一步：对掉量业务做排序层时效处理（独立于容量层），并复核字节系容量拦截清单。
