# 业务容量推演说明

## 1. 为什么需要业务容量

节点业务推荐不能只回答：

> 这个节点跑哪个业务的预计收益更高？

还必须回答：

> 这个业务是否还有足够的真实流量，可以支撑新增节点的建设带宽？

一个业务可能在收益模型中适合很多节点，但客户业务量不是无限的。如果所有节点都交付到同一个
Top1 业务，而业务流量没有同步增长，新增节点只能共同分摊原来的流量，最终会导致：

- 节点利用率下降。
- 矿主实际收益低于模型预测。
- 平台产生大量闲置建设带宽。
- 推荐结果在模型上看起来很好，但在现网中无法兑现。

因此，当前系统将决策拆成两层：

```text
V5：判断节点适合什么业务
V6.2：从业务整体到省份、原运营商、调度类型和目标运营商组合，判断还能承接多少建设带宽
```

只有节点适配和业务容量同时满足，才进入人工交付复核。

## 2. 两个最重要的概念

### 2.1 建设带宽

建设带宽表示我们给一个业务配置了多少节点资源，可以理解为业务的流量供给能力。

例如：

```text
某业务当前建设带宽 = 1,000 Gbps
```

表示当前交付给该业务的节点，合计可以提供约 1,000 Gbps 建设带宽。

### 2.2 业务 95 峰值流量

95 峰值流量表示这个业务实际产生并由节点承载的流量，可以理解为真实业务需求。

例如：

```text
业务建设带宽 = 1,000 Gbps
业务95峰值流量 = 700 Gbps
```

业务整体利用率为：

```text
业务利用率
= 业务95峰值流量 / 业务建设带宽
= 700 / 1,000
= 70%
```

## 3. 为什么增加节点可能降低利用率

假设业务流量仍然是 700 Gbps，但又给这个业务增加了 500 Gbps 建设带宽：

```text
新增前：700 / 1,000 = 70%
新增后：700 / 1,500 = 46.7%
```

新增节点并不会自动产生新的客户流量。业务需求没有增长时，建设带宽越多，平均利用率反而越低。

所以：

```text
节点适合某个业务
不等于
这个业务可以无限接收节点
```

## 4. 容量上限如何计算

当前希望业务整体利用率保持在约 70%。

如果某业务近期可以稳定提供 840 Gbps 流量，那么合理建设带宽为：

```text
业务容量上限
= 预测业务流量 / 目标利用率
= 840 / 70%
= 1,200 Gbps
```

这意味着：

> 在业务流量约为 840 Gbps 的情况下，配置约 1,200 Gbps 建设带宽，可以让整体利用率维持在 70% 左右。

如果这个业务当前只有 800 Gbps 建设带宽：

```text
可新增容量
= 容量上限 - 当前建设带宽
= 1,200 - 800
= 400 Gbps
```

因此，这个业务还可以继续承接大约 400 Gbps 建设带宽。

## 5. 三种业务容量状态

| 业务 | 近期流量 | 当前建设带宽 | 推演容量上限 | 判断 |
| --- | ---: | ---: | ---: | --- |
| 业务A | 700 Gbps | 1,000 Gbps | 1,000 Gbps | 接近目标 |
| 业务B | 840 Gbps | 800 Gbps | 1,200 Gbps | 仍有 400 Gbps 容量 |
| 业务C | 700 Gbps | 1,500 Gbps | 1,000 Gbps | 建设带宽偏多 500 Gbps |

对应公式：

```text
可新增容量 = max(容量上限 - 当前建设带宽, 0)

建设带宽偏多 = max(当前建设带宽 - 容量上限, 0)
```

当前报告将业务分成：

- `仍有容量`：可以继续承接一定建设带宽。
- `接近目标`：剩余空间较少，需要谨慎增加。
- `建设带宽偏多`：当前资源已经超过历史流量推演值，不再净增加。
- `证据不足`：历史有效天数或流量覆盖不够，暂不新增。

## 6. 近期业务流量如何预测

系统不会只取某一天流量，也不会直接使用最近一个月最大值。

当前规则是：

```text
预测业务流量 = 最近14个有效业务日的估算总流量P75
```

P75 表示最近 14 个有效日中，约 75% 的日期不超过这个流量值。

选择 P75 的原因：

- 比平均值更能覆盖高流量日。
- 比最大值更不容易被单日异常影响。
- 可以为日常业务流量波动保留余量。

目标利用率 70% 和预测周期 14 天都是可调整的规划参数，不是模型自动学习出的固定常数。

## 7. 节点日流量如何获取

每个节点每天只保留一个干净的主流业务。流量按照以下优先级获取：

```text
peak95
-> analyzePeak95
-> 建设带宽 * peak95Ratio / 100
-> 缺失
```

其中：

- `peak95` 是首选的真实 95 峰值流量。
- `analyzePeak95` 是第二流量来源。
- 前两项缺失时，根据建设带宽和 `peak95Ratio` 重建流量。
- 单节点流量超过建设带宽 200% 时，视为异常并尝试下一来源。

### 七牛虚拟业务处理

七牛同一节点同一天可能出现多个虚拟业务 ID，而且这些行可能代表同一份节点流量。

系统统一执行：

```text
七牛规范业务名称 = 七牛CDN-ZJ月95

同节点 + 同一天 + 同一规范业务
只取一个优先级最高的有效流量
不累加重复虚拟业务行
```

否则同一份流量可能被重复计算多次，导致七牛容量被错误放大。

## 8. 流量缺失如何处理

缺少流量的节点不能直接按 0 计算，否则会把业务利用率人为压低。

系统先计算有流量观测节点的利用率：

```text
观测利用率
= 有流量节点的95流量之和
  / 有流量节点的建设带宽之和
```

再估算完整业务流量：

```text
估算业务总流量
= 观测利用率 * 全部业务建设带宽
```

这种方法假设缺失节点和有观测节点的平均利用率相近。因此系统还会输出带宽观测覆盖率；覆盖率低于
80% 的业务日不进入容量预测。

## 9. Top1 为什么会被容量拦截

假设一个 300 Gbps 节点的 V5 推荐是：

```text
Top1：业务A
Top2：业务B
Top3：业务C
```

对应业务剩余容量为：

```text
业务A：0 Gbps
业务B：400 Gbps
业务C：100 Gbps
```

判断过程为：

1. Top1 业务A没有剩余容量，不能继续增加300 Gbps。
2. Top2 业务B有400 Gbps剩余容量，可以承接该节点。
3. 最终容量方案选择业务B，供人工复核。

如果业务B只剩200 Gbps，业务C只剩100 Gbps，三个候选都无法承接该节点，则保持当前业务，不为了
追求模型Top1而制造容量超限。

## 10. 为什么要批量规划

容量不能逐节点孤立判断。

假设业务B剩余容量为400 Gbps，同时有两个节点：

```text
节点1建设带宽 = 300 Gbps
节点2建设带宽 = 200 Gbps
```

两个节点不能同时交付业务B：

```text
300 + 200 = 500 Gbps > 400 Gbps
```

系统需要全局判断：

- 哪个节点交付业务B可以获得更高综合收益提升。
- 另一个节点能否使用Top2或Top3。
- 每个业务最终建设带宽是否仍在容量上限内。

当前使用 MILP 整数规划，目标为：

```text
在所有生效的四层容量池上限内
最大化全部候选节点的V5综合适配分提升
```

约束包括：

- 一个节点只能选择一个业务。
- 每个业务整体容量池规划后建设带宽不能超过可操作上限。
- 每个业务+运营商容量池规划后建设带宽不能超过可操作上限。
- 有充分证据时，业务+原运营商+调度+目标运营商容量池不能超过可操作上限。
- 有充分证据时，业务+省份+原运营商+调度+目标运营商容量池不能超过可操作上限。
- 候选必须位于V5 Top3。
- 候选平台利润点估计必须大于0。
- 候选置信度至少为medium。
- 候选综合分比当前业务至少提高0.03。

## 11. 为什么不强制迁出现存节点

如果某业务当前建设带宽已经超过推演容量，系统不会自动要求迁出节点。

可操作容量定义为：

```text
可操作容量上限 = max(当前建设带宽, 推演容量上限)
```

这意味着：

- 建设带宽偏多的业务不能继续净增加。
- 现存节点可以保持不动。
- 节点迁出后，其他节点最多补回原有总量。
- 模型不会仅凭一次离线计算直接改变现网业务。

## 12. 当前真实数据结果

本轮统计窗口为 `2026-08-02` 至 `2026-09-01`：

| 项目 | 结果 |
| --- | ---: |
| 原始数据 | 505,615 条 |
| 干净节点日 | 279,442 条 |
| 有容量流量节点日 | 276,652 条 |
| 流量可用率 | 99.00% |
| 有容量统计的主流业务 | 26 个 |
| 容量证据充分业务 | 26 个 |

业务容量状态：

| 状态 | 业务数 |
| --- | ---: |
| 仍有容量 | 12 |
| 接近目标 | 6 |
| 建设带宽偏多 | 8 |
| 证据不足 | 0 |

如果将当前业务可识别的节点全部无约束切到V5 Top1：

| 业务 | 假设Top1负载 | 超出可操作容量 |
| --- | ---: | ---: |
| 字节架构专线V3 | 3,589.900 Gbps | 1,594.794 Gbps |
| B站专线-盒子 | 2,567.130 Gbps | 323.517 Gbps |
| 优酷专线 | 566.226 Gbps | 115.811 Gbps |
| B站专线-mp | 604.187 Gbps | 25.028 Gbps |

合计假设溢出约 `2,059.15 Gbps`。加入容量约束后，规划方案超容量为 `0 Gbps`。

当前有 462 个节点满足容量规划前置条件。四层容量约束后，335 个节点进入人工复核切换，其中 307 个
采用 Top1、17 个采用 Top2、11 个采用 Top3；其余 127 个保持当前业务。全部 1,454 个证据充分的
容量池均未超限。这 335 个节点只是容量规划候选，不是严格切换清单；V5 严格区间胜出节点目前仍为 0。

与只约束业务整体相比，共有 155 个节点改变了规划结果，其中 127 个不再切换、28 个改用其他候选。
这正是增加原运营商、调度类型、目标运营商和省份层级的价值：业务整体有容量，不代表某个局部网络组合仍有容量。

## 13. 当前方法没有解决什么

当前容量是根据历史流量反推的“建议建设带宽”，不是客户承诺容量，也不是服务器技术上限。

它还没有包含：

- 客户合同约定的容量上限。
- 客户需求单中的区域和运营商配额。
- 节假日、活动和版本发布等未来流量计划。
- 客户临时调量或商务策略。
- 客户提供的区域、运营商和调度正式配额。

因此当前容量结果用于人工规划，不允许直接触发现网业务切换。

## 14. 后续更完整的容量定义

后续接入客户需求单和合同容量后，可以形成双重约束：

```text
历史流量推演容量 = 最近真实流量 / 目标利用率

最终可交付容量
= 历史流量推演容量
  与客户需求或合同容量之间的保守值
```

当前已经分别建立：

```text
业务整体容量
业务 + 原运营商容量
业务 + 原运营商 + 调度类型 + 目标运营商容量
业务 + 省份 + 原运营商 + 调度类型 + 目标运营商容量
```

`scheduleISPs` 为空时，目标运营商取节点原运营商；非空时，按接口原始顺序只取第一个。
因此 `移动 + 异网出省 + 电信` 与 `移动 + 异网出省 + 联通` 是两个独立的细分容量池。

容量池至少需要 7 个有效日和 80% 的近期带宽观测覆盖率才生效。一个候选节点同时占用所有命中的
证据充分父子容量池；任一生效池容量不足，候选即被拦截。

细分容量池证据不足时采用最近父层回退：

```text
省份+原运营商+调度+目标运营商证据不足 -> 使用业务+原运营商+调度+目标运营商
业务+原运营商+调度+目标运营商证据不足 -> 使用业务+原运营商
业务整体或业务+原运营商证据不足 -> 不允许新增
```

本轮共建立 1,881 个容量池，其中 1,454 个证据充分：业务整体 26/26、业务+原运营商 69/70、
业务+原运营商+调度+目标运营商 240/273、业务+省份+原运营商+调度+目标运营商 1,119/1,512。

## 15. 一句话结论

```text
V5决定节点更适合什么业务。
V6.2判断业务整体及该节点所在“省份+原运营商+调度类型+目标运营商”网络组合有没有足够流量支撑这个节点。

只有“节点适合、预计收益合理、平台利润为正、业务容量足够”同时成立，
节点才进入人工交付复核。
```
