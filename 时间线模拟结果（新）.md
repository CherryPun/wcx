# 时间线模拟结果（新）

## 1. 规则（混合）

- **顺序**：紧迫度（当前业务平台利润越低越先）→ 边际增益密度（Δ分/带宽）→ 随机 tie-break（seed=42）；
- **逐节点分配**：Top1~3 中取第一个"分数增益≥0.03、平台利润>0、且目标业务池**剩余容量足够**"的业务；
- **容量**：业务层 `allocatable_headroom` 初始；切换时**释放原业务容量**；无可行则 hold；
- 数据：采用配置（cap2.0）+ 同点全量 current。

## 2. 结果

- 节点 **2,288**；**切换 1,477**；**hold 811**（无可行候选/门槛不满足）。
- **切换目标分布（前 8）**：
  `10000280` 403、`10000183` 281、`10000096` 197、`10000244` 186、`10000079` 102、`10000304` 84、`10000065` 66、`10000069` 62。
- **占用 vs 上限（Mbps）**：
  | 业务 | 占用节点 | 估算占用 | 上限 | 余量 |
  |---|---:|---:|---:|---:|
  | 10000280 | 998 | 3,584,469 | 5,466,045 | 1,520,310 |
  | 10000096 | 198 | 1,128,005 | 1,878,728 | **23（打满）** |
  | 10000183 | 297 | 908,759 | 1,987,582 | **173（打满）** |
  | 10000244 | 206 | 590,040 | 1,503,428 | 241 |
  | 10000079 | 183 | 910,694 | 7,028,311 | 710,057 |

## 3. 有价值的结论

1. **可执行的切换上限被容量钉死**：`10000096` 只能切 **~197 节点**（推荐 956）、`10000183` ~281（推荐 532）→ **需求缺口量化**；
2. **时序模拟天然无溢出**（planned 溢出 0），且不靠事后截断；
3. **hold 仍占 811/2,288≈35%**：容量/门槛约束下，多数节点**本期不应动**；
4. **分布比静态更分散**：切换目标不再单点集中（最大 `10000280` 403 次切换）；
5. **规则可对比**：同数据下换排序（历史先跑/纯价值/随机）可出"溢出-命中-集中度"权衡表；
6. **边界结论**：即使按最优顺序，需求不足的业务（10000096/10000183）**仍切不了更多** → 需**扩容或客户需求**，非排序可解。

## 4. 限制

- 容量只用**业务层**池（未含运营商/调度/省份四层）；
- **单批次**（无运营批次/到达节奏）；
- 依赖**弱模型分数**（R²≈0.025）与容量估计；
- 随机 tie-break 固定 seed；建议多 seed 出分布；
- 未做命中/收益的**因果验证**（需反事实/线上 A/B）。

> 脚本：`wcx2实验代码（新）/时间线模拟（新）.py`；结果文本本地产物。
