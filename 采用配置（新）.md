# 采用配置（新）

## 1. 已固化为默认

wcx2 的 V5 训练**默认启用近端时效权重**（原 RJ 默认关闭）：

```text
V5_RECENCY_HALF_LIFE 默认 7      （半衰期 7 天）
V5_RECENCY_WEIGHT_CAP 默认 1.5   （权重上限 1.5）
```

代码位置：`v5_hybrid_recommendation.py`（`prepare_training_window`）。
验证：不带任何 env 直接 build，指标与显式 `HL7+cap1.5` **完全一致**。

其余实验开关**默认关闭**：`V5_TREND`、`V5_BUSINESS_BALANCE`、`V5_CALIB`。

## 2. 采用依据（同窗口 `2026-08-10~09-09`，V5 时间外）

| 指标 | RJ 默认 | 采用配置 | 变化 |
|---|---:|---:|---|
| 矿主 R² | 0.0217 | **0.0291** | +34% |
| 矿主 RMSE | 0.2778 | **0.1998** | -28% |
| 平台 R² | -0.0027 | **0.0063** | 转正 |
| 平台 RMSE | 0.2431 | **0.1923** | -21% |
| Top1 集中度 max | 0.4886 | **0.3955** | ↓ |
| HHI | 0.3296 | **0.2454** | ↓ |
| Top1（观察口径） | 2.05% | **9.88%** | ≈4.8× |
| Top3（观察口径） | 30.62% | 27.91% | 略降 |
| V6 全切溢出 | 4,955 Gbps | **2,922 Gbps** | -41% |

## 3. 回退方式

```powershell
$env:V5_RECENCY_HALF_LIFE="0"   # 关闭时效权重 → 回到 RJ 基线行为
```

## 4. 边界与前提

- **可信度仍 low**（90% 节点低置信），仍**不允许自动切**；
- Top3 略降（27.9% vs 30.6%），非全面碾压；
- hit 为**观察性口径**（推荐 vs 节点实际跑过的最优），非因果；
- 结论基于**本次重建窗口**，数据更新后需重验；
- 该默认值使 **wcx2 默认行为不再等于 RJ 原分支**（属有意为之）。

## 5. 单测

`python -m unittest -v test_v5_hybrid_recommendation.py` → **7/7 通过**：
- 原 4 个（调度/连续段权重/节点效应/CLI）；
- 新增 3 个时效权重单测：
  1. 半衰期比例（相隔 7 天 → 2×；相隔 28 天 → 1/16）；
  2. `half_life=0` 回退（权重无差异）；
  3. 权重上限 `cap` 降低权重极差。

> 本文件为采用记录；配置改动见 `v5_hybrid_recommendation.py`。
