# 平台模型重做结果（新）

## 1. 做法

- 从 V3 日账提取**业务级**定价/结算特征（只用测试期前 10 天的窗口，避免泄漏）：
  `biz_price_bucket`（客户单价分位）、`biz_price_type`（价格项名称众数）、`biz_measure_bucket`、`biz_supply_bucket`；
- 由 env `V5_PLATFORM_FEATURES=1` 纳入 V5 编码字段（默认**关**），重训对照。

## 2. 结果

| 配置 | 平台 R² | 平台 RMSE | 平台90%覆盖 | 矿主 R² | Top1 max占比 | HHI |
|---|---:|---:|---:|---:|---:|---:|
| 基线（采用配置） | **0.00690** | 0.19225 | 0.908 | 0.02532 | **0.4178** | **0.2520** |
| + 业务级定价特征 | 0.00358 | 0.19258 | 0.907 | 0.02510 | 0.4506 | 0.2589 |

## 3. 结论（负）

1. **加业务级定价特征没有改善平台模型**（R² 0.0069→0.0036，反而略差），集中度也略升；
2. 说明平台利润的**方差并非由"业务级定价"解释**，更可能来自：
   - **成本归因/冲销口径未定** → 标签噪声；
   - 小分母（÷建设带宽）放大日粒度抖动；
   - 平台利润本身接近"不可由画像+业务统计预测"。
3. 因此：**平台模型重做的前置条件是拿到权威成本归因口径**；在口径未定前，继续加特征收益甚微。
4. 本改动**未采用**（env 默认关）；采用配置保持 `HL7+cap2.0`。

## 4. 建议
- 与 yzx/上游**先对齐成本归因与冲销规则**，重算标签；
- 若归因仍不可得，可考虑：**平台只作软展示/不做排序权重**（但 P2 已证简单降权会变差，需谨慎）；
- 或把平台利润改为**业务级先验收缩**（弱信号时只给业务层面区间，不给节点级点估计）。

> 脚本：`wcx2实验代码（新）/P6平台特征（新）.py`；代码开关 `V5_PLATFORM_FEATURES`（默认关）。
