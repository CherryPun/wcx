# P2 平台利润试点结果（新）

## 1. 做法

用现有候选的 `miner_unit_income_topk / platform_unit_profit_topk`（含当前业务）**按节点重新归一**，
测试综合分权重 `w`（矿主）× `1−w`（平台）：`w∈{0.5,0.8,1.0}`；
另试"平台硬闸门"（要求 `platform_low90>0` 才允许作为候选）。

> 说明：本试点按 top1~3+当前业务的候选重算归一，与 V5 内部归一（全候选集）不同，
> **绝对值不可与 V5 官方直接比**，仅用于**同口径下的相对比较**。

## 2. 结果

| 配置 | max占比 | HHI | Top1 可行 | hit@1 | hit@3 |
|---|---:|---:|---:|---:|---:|
| w=0.5（现权重） | 0.5179 | 0.3146 | 2162 | **0.0782** | 0.2758 |
| w=0.8 | 0.7351 | 0.5520 | 2160 | 0.0463 | 0.2758 |
| w=1.0（纯矿主） | 0.7194 | 0.5323 | 2152 | 0.0468 | 0.2758 |
| w=0.8 + 平台闸门 | — | — | **0（全部被拦）** | — | — |

## 3. 结论（重要，与预期相反）

1. **下调平台权重（0.5→0.8/1.0）反而更集中、hit@1 更差**：说明当前平台分虽弱，
   但其**归一化后的差异起到了分散作用**；去掉后矿主分独大，全部涌向高分大业务；
2. **平台硬闸门不可行**：绝大多数候选 `platform_low90 ≤ 0`（平台区间跨零 >80%），闸门会把**所有候选拦掉**；
3. 因此"**先降权**"这条路**在当前模型下不成立**；正确方向是**重做平台模型**：
   - 先拿**权威成本归因口径**重算标签；
   - 把**已有定价特征**（`miner_unit_price` 等）加入模型；
   - 用**分位数/分层**降低区间跨零比例，再谈闸门。

## 4. 限制
- 归一近似（top3+当前）导致绝对值与 V5 官方不同；
- 命中的是观察口径（非因果）。

> 脚本：`wcx2实验代码（新）/P2平台试点（新）.py`；结果文本本地产物。
