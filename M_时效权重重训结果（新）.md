# 时效权重重训结果（新，首次模型迭代）

## 1. 改动

在 V5 训练里加入**近端时效权重**（样本权重 = 连续段权重 × `0.5^(样本龄/半衰期)`，均值归一）：

- 代码：`v5_hybrid_recommendation.py`（改 `prepare_training_window`；由 env `V5_RECENCY_HALF_LIFE` 控制，**默认 0=关闭**，可回退）；
- 复用链：V3 日粒度 → V5 重训（同一窗口 `2026-08-10~09-09`）。

## 2. 指标对比（V5 时间外，官方摘要）

| 配置 | 矿主 R² | 矿主 RMSE | 平台 R² | 平台 RMSE | Top1 max 占比 | HHI | 可信度 |
|---|---:|---:|---:|---:|---:|---:|---|
| 基线(h=0) | 0.0217 | 0.2778 | -0.0027 | 0.2431 | 0.4886 | 0.3296 | low |
| **HL=7** | **0.0260** | **0.2103** | **0.0067** | **0.2030** | **0.4161** | **0.2553** | low |
| HL=14 | 0.0262 | 0.2051 | 0.0043 | 0.1978 | 0.5887 | 0.3829 | low |
| HL=30 | 0.0215 | 0.2815 | -0.0011 | 0.2463 | 0.5708 | 0.3837 | low |

- **HL=7 是帕累托改进**：矿主/平台 RMSE 各降约 **24% / 17%**，两目标 R² 转正（平台由 -0.0027→0.0067），
  且 **Top1 集中度下降**（max 0.49→0.42、HHI 0.33→0.26）。

## 3. 容量联动（V6，HL=7 推荐）

| 口径 | 基线 V5 | HL=7 |
|---|---:|---:|
| 全切溢出总量 | 4,955 Gbps | **3,015 Gbps（-39%）** |
| 溢出业务数 | 4 | 4 |

## 4. 结论

1. **首次模型迭代即取得净增益**：准确率↑（RMSE、R²）、集中度↓、容量溢出↓——同向改善；
2. **HL=7 优于 14/30**（14 RMSE 更低但集中度恶化；30 近似无效）；
3. 可信度仍为 **low**（门槛未变），**仍不允许自动切**；
4. 代码改动**默认关闭**，采用方式：设置 `V5_RECENCY_HALF_LIFE=7` 重训。

## 5. 后续

- 更细扫 HL（如 5/8/10）与**样本权重上限**，找准确率-集中度折中点；
- 叠加"量趋势特征"（近 7 天 vs 前 7 天）重训，看能否继续提升；
- 与 hold/容量约束叠加，出联合复核清单。

> 产物：`v5_hybrid_recommendation.py`（改）、`M_recency权重（新）.py`、本文件；训练产物本地产物不入库。
