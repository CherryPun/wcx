# 确定性修复与重评（新）

## 1. 背景（为何要重评）

发现 **V5 运行非确定**：同配置两次运行 Top1 有 **573/2288** 不同（XGBoost `nthread=6`）。
→ 之前"采用配置集中度更低"等结论**受噪声影响，不可信**。

## 2. 修复

- V5 XGB `nthread` 由 6 改为可配置，**默认 1**（env `V5_NTHREAD`）；
- 验证：同配置两次运行 **Top1 差异 = 0**。

## 3. 单线程重扫 HL×cap（同 current business）

| 配置 | 矿RMSE | 矿R² | 台RMSE | 台R² | Top1 max占比 | HHI |
|---|---:|---:|---:|---:|---:|---:|
| 基线 | 0.27810 | 0.01965 | 0.24255 | 0.00218 | 0.5044 | 0.3550 |
| HL5c1.5 | 0.20016 | 0.02563 | 0.19196 | 0.00998 | 0.7045 | 0.5122 |
| HL7c1.2 | 0.27793 | 0.02082 | 0.24296 | -0.00115 | 0.4331 | 0.3115 |
| HL7c1.5（原采用） | 0.20020 | 0.02526 | 0.19205 | 0.00905 | **0.7041** | **0.5119** |
| **HL7c2.0（新采用）** | **0.20020** | **0.02532** | **0.19225** | **0.00690** | **0.4178** | **0.2520** |
| HL10c1.5 | 0.20014 | 0.02421 | 0.19209 | 0.00694 | 0.4908 | 0.3012 |
| HL14c1.5 | 0.27802 | 0.02019 | 0.24285 | -0.00028 | 0.4677 | 0.3153 |

## 4. 结论

1. **原采用 HL7+cap1.5 需修正**：准确率确实提升，但 **Top1 集中度升到 70.4%（high）**；
2. **真正帕累托最优 = `HL7 + cap2.0`**：
   - 矿主 RMSE 0.2781→**0.2002**、R² 0.0197→**0.0253**；
   - 平台 RMSE 0.2426→**0.1923**、R² 0.0022→**0.0069**；
   - **集中度同时下降**：max 0.5044→**0.4178**、HHI 0.3550→**0.2520**；
3. `HL10c1.5` 次优（RMSE 更小但集中度更高）；`HL5c1.5` 集中度过高；`HL7c1.2/HL14c1.5` 准确率基本无增益。

## 5. 新的默认与回退

```text
V5_RECENCY_HALF_LIFE = 7      （不变）
V5_RECENCY_WEIGHT_CAP = 2.0   （由 1.5 改为 2.0）
V5_NTHREAD = 1                （确定性，默认）
```

回退基线：`$env:V5_RECENCY_HALF_LIFE="0"`。

## 6. 待跟进

- **勘误**：早前"cap2.0 全切溢出 0"是 **current business 文件缺失**（全部 unknown → V6 无合格节点）造成的假象；
  用同点全量 current business 后，溢出为 **4,139 Gbps / 4 业务**（`10000096` 3,764 为主），切换 206、拦截 87（见 `P1采用配置清单（新）.md`）。
- 与基线对比需在**同一 current business** 下重跑 V6；
- 此前 hold/趋势/平衡/校准/折减等对比**需在单线程下复核**（已补：趋势/平衡/校准，见 `模型改造否决记录（新）.md`）；
- 可视化与 `采用配置（新）.md` 已同步为新默认。

## 7. 同点全量 current 下的正式对照（基线 vs 采用）

> 同一 current business（`current_business_同点全量（新）.csv`，覆盖 2,146/2,288），单线程确定性。

**V5：**

| 配置 | 矿RMSE | 矿R² | 台RMSE | 台R² | Top1 max | HHI |
|---|---:|---:|---:|---:|---:|---:|
| 基线 | 0.27810 | 0.01965 | 0.24255 | 0.00218 | 0.5044 | 0.3550 |
| **采用 cap2.0** | **0.20020** | **0.02532** | **0.19225** | **0.00690** | **0.4178** | **0.2520** |

**V6：**

| 配置 | 规划切换 | 容量拦截 | 全切溢出 | 溢出业务数 |
|---|---:|---:|---:|---:|
| 基线 | 94 | 63 | 6,265 Gbps | 5 |
| **采用 cap2.0** | 206 | 87 | **4,139 Gbps** | 4 |

**结论**：同点全量 current 下，采用配置在**准确率、集中度、全切溢出**上全面优于基线（溢出 -34%）；
代价是规划切换/拦截数更多（更多节点进入可动作集合），需人工复核消化。
