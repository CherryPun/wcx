# 分配器 v0（新）

**工具**：`wcx2实验代码（新）/分配器v0（新）.py`；**测试**：`test_allocator_v0.py`（3/3）。
**范围**：C3-v0（无外部依赖版）：**池×业务份额** + 分段线性凹目标（经验分位切点）+ 容量约束，LP 松弛。
对照基线 = 现行"先排再卡容量"（greedy）。

## 1. 模型

```
决策 x[p,b] ≥ 0          # 池 p 分给业务 b 的 Mbps（连续份额，替代 0-1 整节点切换）
约束 Σ_b x[p,b] ≤ pool_cap[p]        # 池容量（v0 = 池内节点建设带宽合计）
     Σ_p x[p,b] ≤ biz_ceiling[b]     # 业务级上限（v6 业务级 ceiling）
目标 max Σ_b 凹分段价值(X_b) + ε Σ g[p,b]·x[p,b]   # ε=0.1 池偏好项
     凹分段：slope = g_b × (1.0 / 0.6 / 0.3)，切点 = 该业务供给的 P50 / P90（经验分位，替代不可辨识的需求曲线）
```

指标：容量损失 / planned 溢出 / hold 占比 / 扩容缺口。

## 2. 验证：合成用例（边际递减下 LP 应优于贪心）

| 方案 | 分配 | 凹价值 |
|---|---|---:|
| LP 份额 | 业务1 50 + 业务2 150 | **170** |
| greedy（按单位收益排序吃满） | 业务1 200 | 140 |

→ **PASS**：在边际递减场景下，凹目标 + 份额把量分给"边际更高"的业务，贪心做不到。
测试同时覆盖：**容量约束不被突破**、**planned 溢出 = 0**（`test_allocator_v0.py` 3/3）。

## 3. 真实数据运行（池=省×运营商；81 池 / 496 组合 / 26 业务上限）

**口径修正（本次）**：池容量从"候选带宽求和"（会把同一节点在 top1–3 中重复计入，25.7 Tbps）
改为 **headroom 口径**：`池内唯一节点带宽 × 0.7 − 当前占用`（6.0 Tbps）。

| 资源模型 | 方案 | 总容量(Gbps) | 已分配 | 容量损失 | planned溢出 | 扩容缺口(Gbps) | 命中业务数 | **凹目标值** |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| nodesum（旧） | LP / greedy | 25,729 | 14,605 | 0.432 | 0 | 11,124 | 14 / 14 | — |
| **headroom（新）** | LP 份额 | 6,003.4 | 6,003.4 | **0.0** | 0 | 11,124.2 | 9 | **533,883** |
| **headroom（新）** | greedy（现做法） | 6,003.4 | 6,003.4 | **0.0** | 0 | 11,124.2 | 12 | **489,788** |

→ headroom 口径下容量被**打满**（损失 0），两者差异体现在**分配结构**：**LP 目标值 +9.00%**（把量集中给边际更高的业务，9 个 vs 12 个）。

## 4. 结论（诚实版）

1. **口径是决定性的**：旧 nodesum 口径下凹目标与贪心完全同解（差异被"池容量虚高"淹没）；
   换成 headroom 后出现 +9% 目标值差异 —— 说明**先修资源口径，再谈算法**；
2. **但 +9% 不是"业务收益"**：它是在**假设的凹曲线**（切点=供给 P50/P90、段斜率 1.0/0.6/0.3）
   下的建模差异，不代表真实增收；换真实需求分位（B2）后需重跑；
3. **真正的瓶颈仍是容量**：扩容缺口 **11,124 Gbps**，远大于分配优化空间 —— 与"可切总量由需求决定"一致；
4. **架构目标是达成的**：份额变量、凹目标、LP、决策指标、回归测试（3/3）齐备，且对照实验可复现。

## 5. 下一步

1. **本地可继续**：把池容量接入 v6 的池级 `allocatable_headroom_mbps`/`operational_capacity_ceiling_mbps`（替代 0.7 近似），并做敏感度分析（段斜率、切点）；
2. **要外部**：多业务**节点级份额**语义（上游）+ **真实需求**（B2）→ 切点换需求分位后重跑同一对照；
3. **要产品决策**：扩容缺口 11.1 Tbps 是"扩容工单"议题，不是算法议题。
