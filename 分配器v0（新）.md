# 分配器 v0（新）

**工具**：`wcx2实验代码（新）/分配器v0（新）.py`；**测试**：`test_allocator_v0.py`（3/3）。
**范围**：C3-v0（无外部依赖版）：**池×业务份额** + 分段线性凹目标（经验分位切点）+ 容量约束，LP 松弛。
对照基线 = 现行"先排再卡容量"（greedy）。

## 1. 模型

```
决策 x[p,b] ≥ 0          # 池 p 分给业务 b 的 Mbps（连续份额，替代 0-1 整节点切换）
约束 Σ_b x[p,b] ≤ pool_cap[p]        # 池容量（v0 = 池内节点建设带宽合计）
     Σ_p x[p,b] ≤ biz_ceiling[b]     # 业务级上限（v6 业务级 ceiling）
目标 max Σ_b 凹分段价值(X_b) + ε Σ g[p,b]·x[p,b]   # ε=0.1 池偏好项
     凹分段：slope = g_b × (1.0 / 0.6 / 0.3)，切点 = 该业务供给的 P50 / P90（经验分位，替代不可辨识的需求曲线）
```

指标：容量损失 / planned 溢出 / hold 占比 / 扩容缺口。

## 2. 验证：合成用例（边际递减下 LP 应优于贪心）

| 方案 | 分配 | 凹价值 |
|---|---|---:|
| LP 份额 | 业务1 50 + 业务2 150 | **170** |
| greedy（按单位收益排序吃满） | 业务1 200 | 140 |

→ **PASS**：在边际递减场景下，凹目标 + 份额把量分给"边际更高"的业务，贪心做不到。
测试同时覆盖：**容量约束不被突破**、**planned 溢出 = 0**（`test_allocator_v0.py` 3/3）。

## 3. 真实数据运行（池=省×运营商；81 池 / 496 组合 / 26 业务上限）

| 方案 | 总容量(Gbps) | 已分配(Gbps) | 容量损失 | planned溢出 | 扩容缺口(Gbps) | 命中业务数 |
|---|---:|---:|---:|---:|---:|---:|
| LP 份额 | 25,729.0 | 14,604.8 | 0.4324 | 0 | 11,124.2 | 14 |
| greedy（现做法） | 25,729.0 | 14,604.8 | 0.4324 | 0 | 11,124.2 | 14 |

**两者完全一致** —— 即：**在当前资源模型下，凹目标没有可操作空间**。原因（已定位）：

1. **池容量口径不准**：v0 用"池内节点建设带宽合计"，而候选行是 top1–top3，同一节点带宽被重复计入 →
   池容量被高估（25.7 Tbps），真正的瓶颈落在**业务级 ceiling**，与"如何在池间分摊"无关；
2. **没有真实需求**：凹切点用的是供给 P50/P90（经验分位），并未改变任何一条约束的松紧；
3. 因此在"排序 vs 分摊"这类自由度上，LP 与贪心退化为同一解。

## 4. 结论（诚实版）

- **架构目标达成**：分配器现在能表达**份额**与**凹目标**，有可跑的 LP、可复现的评测指标与回归测试；
- **性能收益无法在本地证明**：与 P4/P5 的历史结论一致——**只换目标形态、不换右端数据，收益≈0**；
- 要让 C3 真正产生价值，需要两项**非本地**输入：
  1. **真实需求/释放计划（B2）**：决定凹切点与约束右端；
  2. **多业务份额语义与池资源口径（上游）**：节点级份额与"池容量"必须来自真实资源账，而不是节点带宽求和。

## 5. 下一步（按依赖）
1. 与上游确认**多业务份额**与**池容量口径**（否则 C3 永远只能停在 v0）；
2. B2 数据到位后，把切点从"供给分位"换成"需求分位"并重跑同一对照；
3. 池资源模型改为：`pool_cap = 该池可分配 headroom`（取 v6 `allocatable_headroom_mbps` 的池级聚合），
   而不是节点带宽求和——**这一步本地可做**，但需要 v6 产出池级明细。
