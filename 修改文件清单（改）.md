# 修改文件清单（改）

本清单列出 **wcx2 相对 RJ 原分支（`e541ac2`）修改过的文件**（区别于"新增（新）"文件）。

> 说明：按要求应给修改文件加"（改）"后缀，但下列文件**不能改名**（会破坏导入/CI/Git/GitHub 约定），
> 故采用**本清单登记 + 文件头部标记**的等价做法。

| 文件 | 修改内容 | 不能改名的原因 | 标记位置 |
|---|---|---|---|
| `v5_hybrid_recommendation.py` | 近端时效权重（默认 HL7+cap2.0）、`V5_NTHREAD`（默认1，确定性）、趋势/平衡/校准 env 开关 | 被 v4/测试/CLI/实验脚本 import | 文件头注释 `[wcx2 修改（改）]` |
| `test_v5_hybrid_recommendation.py` | 新增 3 个时效权重单测（半衰期/回退/上限） | unittest 命名与命令约定 | 文件头注释 `[wcx2 修改（改）]` |
| `README.md` | 顶部"wcx2 变更提示"（新默认/回退/指向） | GitHub 入口固定名、多文档引用 | 顶部提示块 |
| `.gitignore` | 忽略 `*（新）.csv`、`*（新）.txt` 等本地产物 | Git 强制文件名 | 注释 `wcx2 新增本地产物…` |

## 新增文件
其余 47 个均为**新增（新）**文件（文档/可视化/实验脚本），见 `正式交付索引（新）.md`。
