> **已取代**：本清单基于旧 cap1.5/非确定性口径；现采用配置见 P1采用配置清单（新）.md。保留作历史记录。

# P1 字节系容量清单（新）

## 1. 目的

对**全切 Top1 会溢出**的字节系业务，给出容量诊断与处置建议（拦截 / 扩容），供人工复核。

数据：`recent_month_large_mainstream_v6_capacity_full（新）/`（窗口 `2026-08-10~09-09`，参数 0.70/14/7/0.8）。

## 2. 业务容量与缺口

| 业务 | 当前建设带宽(Mbps) | 容量上限(Mbps) | 全切负载(Mbps) | 全切溢出(Mbps) | 扩容需求(Gbps) | 规划切入 |
|---|---:|---:|---:|---:|---:|---:|
| 1382680008 字节CDN专线 | 1,248,524 | 1,396,795 | 3,353,726 | **1,956,931** | **≈1,957** | 1 |
| 10000224 字节CDN专线V4 | 3,900 | 115,603 | 1,749,796 | **1,634,193** | **≈1,634** | 0 |
| 10000096 字节架构专线V3 | 768,700 | 1,878,728 | 3,242,258 | **1,363,530** | **≈1,364** | 39 |
| 10000041 B站Web盒子 | 34,690 | 18,603 | 35,040 | 350 | ≈16 | 0 |

- 这几个业务业务层状态显示"仍有容量/建设带宽偏多"，但**全切 Top1 后会溢出**——说明溢出源自"拟切换的节点规模"，不是当前存量。
- **扩容需求 = 全切负载 − 容量上限**：即若要承接全部拟切换节点，需要新增的建设带宽。

## 3. 处置建议（拦截 / 扩容）

1. **拦截（先做，零成本）**：对上述超容池"负载不可增"，把拟切换节点挡回（下一候选或维持当前）。
   - 现状已拦截 11 个节点（模型 Top1：10000096×10、10000183×1）；复核切换 57 个（10000096×39、10000079×9、**10000244×7**）。
2. **扩容（需业务/资源决策）**：仅当**近端真实需求**确实高于当前建设带宽时，按目标 0.7 利用率给出新增建设带宽（上表"扩容需求"）。
3. **迁移（可行时优先）**：同业务换到有余量的运营商/调度/省份池（本清单层级池证据不足，未列出；需在 V6 细层输出后补）。

## 4. 与 10000244 的关系

- `10000244 B站专线-盒子V2` **不在溢出名单**（全切溢出 0、富余 ≈494 Gbps）——**P1 不涉及它**；
- 复核切换里它有 7 个节点，属正常规划，不是容量拦截对象。

## 5. 结论

- 真正需要容量处置的是 **1382680008 / 10000224 / 10000096**；
- 建议：**先拦截（已具备）→ 再评估扩容（需业务确认需求）→ 可行时迁移**；
- 拦截只是止损；**扩容才能解决需求性超容**。

## 6. 明细

- `P1字节系容量清单（新）.csv`（本地产物，不入库）；脚本 `P1字节系容量清单（新）.py`。
