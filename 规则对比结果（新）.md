# 规则对比结果（新）

## 1. 做法

同一数据（采用配置 cap2.0 + 同点全量 current）、同一分配机制（业务层池、容量消耗/释放、增益≥0.03、平台利润>0），
只改变**节点处理顺序**：

- `value`：边际增益密度（Δ分/带宽）降序；
- `urgency`：当前业务平台利润低者优先；
- `history`：节点最早上线日优先；
- `random`：随机，20 个 seed 取分布。

## 2. 结果（切换目标口径）

| 规则 | 切换 | Top1 max占比 | HHI | Top3 目标 |
|---|---:|---:|---:|---|
| value | 1,482 | 0.2510 | 0.1788 | 10000183(372)、10000096(362)、10000280(271) |
| urgency | 1,477 | 0.2729 | 0.1574 | 10000280(403)、10000183(281)、10000096(197) |
| history | 1,552 | 0.2384 | 0.1626 | 10000280(370)、10000183(320)、10000096(310) |
| random（20 seed） | 均值 1,482 | 均值 0.2500 | 均值 0.1580 | 切换范围 1,460~1,499；max 0.239~0.266 |

## 3. 结论

1. **顺序对"总量"影响有限**：四种规则的切换量都在 **~1,480~1,550**（随机分布很窄），说明在业务层容量约束下，**可切总量主要由需求上限决定，而非顺序**；
2. **顺序主要影响"谁拿到"**：
   - `history`（先上线优先）**切换最多、集中度最低**（max 0.238）；
   - `urgency`（减亏优先）**HHI 最低**（0.157）但单业务最高（0.273）——更均衡但头部略尖；
   - `value`（效率优先）**无明显优势**（max 0.251、HHI 0.179），且依赖弱模型；
   - `random` 稳定居中，适合做**公平基线/不确定性评估**。
3. **没有单一最优规则**：若重视"少集中"选 `history`；重视"减亏"选 `urgency`；重视"效率"理论上是 `value` 但收益甚微；`random` 作对照。
4. **重要提醒**：本对比是**业务层池**；接入**运营商/调度/省份四层池**后，可切总量会**进一步下降**，但"顺序影响小于总量约束"的结论预计不变。

## 4. 限制

- 单批次、业务层池、弱模型（R²≈0.025）与容量估计；
- 未计入命中/收益的因果验证。

> 脚本：`wcx2实验代码（新）/规则对比（新）.py`；文本结果本地产物。
