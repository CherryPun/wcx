# 采用配置容量清单（新）

## 1. 说明

采用配置 = **`V5_RECENCY_HALF_LIFE=7`、`V5_RECENCY_WEIGHT_CAP=2.0`、`V5_NTHREAD=1`**（单线程确定性）。
V6.2 容量分配（窗口 `2026-08-10~09-09`，参数 0.70/14/7/0.8）。

> 注：旧 `cap1.5` 版本（集中度 70.4%）已作废；本文件为 `cap2.0` 重跑结果。

## 2. 总览

| 指标 | 值 |
|---|---:|
| 报告节点 | 2,288 |
| 规划切换节点 | **0**（Top1/2/3 均 0） |
| 容量拦截节点 | **0** |
| planned 溢出 | **0** |
| **全切溢出（诊断）** | **0 Gbps / 0 业务** |
| 复核切换节点 | 0 |

## 3. 结论

- **`cap2.0` 下全切 Top1 不再造成任何业务超容**（溢出 0）——模型分布更分散（HHI 0.252），
  容量层无需拦截；
- 同时**没有任何节点满足切换门槛**（增益/平台利润约束），故 `planned_change=0`——本期在容量与门槛双重约束下**无建议切换**；
- 定位不变：**仅人工规划、不自动切**；若需"有动作"的清单，需放宽 `minimum_score_gain` 或改进平台利润模型（另议）。

> 明细：`recent_month_large_mainstream_v6_capacity_cap20（新）/`（`v6_capacity_report.html`、`v6_node_report.html`、`v6_capacity_review_nodes.csv`、`v6_capacity_blocked_nodes.csv`；本地产物，不入库）。
