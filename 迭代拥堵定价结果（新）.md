# 迭代拥堵定价结果（新，路线1 原型）

## 1. 做法

离线原型（不改 solver）：对 V5 候选 Top1~3（含当前业务）按**业务层池利用率**折减分
`factor = min(1, 上限/负载)`，分配→重算负载→再折减，迭代（含负载 EMA 阻尼），观察是否收敛、是否降低溢出。

## 2. 结果：**不收敛（振荡）**

| 轮次 | 溢出(Gbps) | 溢出业务 |
|---|---:|---:|
| 无定价 | 3,038 | 1 |
| 迭代2 | 3,038 | 1 |
| 迭代3 | 545 | 3 |
| 迭代4 | 3,038 | 1 |
| 迭代9 | 379 | 2 |
| 迭代26 | 2,712 | 1 |

- 同步/阻尼更新都在 **~3,000 Gbps、~550 Gbps、~380 Gbps** 之间反复跳（2~3 周期振荡），**不收敛**；
- 最终分布仍集中（10000183 723、10000280 427…），问题未被稳定改善。

## 3. 结论：否决"迭代拥堵定价（最佳响应式）"

- 原因：节点**最佳响应**是离散的（选哪个业务），多池相互挤压时会产生**多重均衡/循环**，
  简单的"分配↔折价"迭代无法稳定；EMA 阻尼只能减幅、不能收敛。
- 注：本原型的"无定价溢出 3,038/1 业务"是**业务层池 + 仅 Top3 候选**口径，与 V6 官方
  （多层池、4,139 Gbps/4 业务）不可直接比，仅用于检验收敛性。

## 4. 由此得到的方向

要真正把"需求"放进决策，需**结构化方案**，而非启发式迭代：
1. **路线3：单一优化目标**——在 V6 的 MILP 目标里让"边际期望收益随池内已分配量递减"
   （分段线性/凹函数），一次求解得到最优分配；这是可收敛的正确形态。
2. **路线2：需求响应曲线**——拟合"流量 vs 供给"饱和曲线，把期望（稀释后）收益作为目标。
3. **路线4：硬需求配额**——接入客户容量/需求，直接做上限（需平台数据）。

## 5. 建议

- **停止启发式迭代定价**（已证不收敛）；
- 若继续，直接做 **路线3（MILP 内凹边际收益）** 或 **路线2（需求曲线）**，并在
  "溢出下降且不转移 + 期望收益不降 + 命中不退化"三指标下验收。

> 本文件为实验记录：路线1 被证伪。
