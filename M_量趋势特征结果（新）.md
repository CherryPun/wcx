# 量趋势特征结果（新，模型迭代②）

## 1. 改动

- 为训练对加入**过去式业务量趋势**特征 `business_trend`（严格无泄漏）：
  `trend = 该业务前 7 天量 / 前 7~14 天量`（量=capacity_peak95_mbps），离散为 `down/flat_down/flat_up/up/unknown`；
- V5 侧由 env `V5_TREND=1` 把 `business_trend` 纳入编码字段（**默认关闭**）；
- 推断期用"每业务窗口内最新一天"的趋势；训练期用逐行过去式。
- 组合本次基础配置：`V5_RECENCY_HALF_LIFE=7`、`V5_RECENCY_WEIGHT_CAP=1.5`。

## 2. 指标对比（V5 时间外）

| 配置 | 矿主 R² | 矿主 RMSE | 平台 R² | 平台 RMSE | Top1 max占比 | HHI |
|---|---:|---:|---:|---:|---:|---:|
| 基线 | 0.0217 | 0.2778 | -0.0027 | 0.2431 | 0.4886 | 0.3296 |
| HL7+cap1.5 | 0.0291 | 0.1998 | 0.0063 | 0.1923 | **0.3955** | **0.2454** |
| **+量趋势** | **0.0307** | **0.1996** | **0.0082** | **0.1921** | 0.6718 | 0.4670 |

## 3. 结论

1. **量趋势特征确实提升拟合**：矿主 R² 0.0291→**0.0307**、平台 R² 0.0063→**0.0082**（RMSE 基本持平）。
2. **但集中度显著恶化**：Top1 max 占比 0.3955→**0.6718**、HHI 0.2454→**0.4670** —— 模型更偏向少数"量上行"业务。
3. 因此**不是帕累托改进，暂不采用**（当前的验收同时看准确率与集中度）。

## 4. 可能原因与后续

- 趋势桶把大量样本可预测性提高，但也强化了"追涨"（量上行业务被放大）；
- 可尝试：
  1. **更细/更稳健的趋势**（对数比值、连续值、按业务标准化）；
  2. 趋势与**容量/集中度约束联合**（趋势只在有 headroom 时给正权重）；
  3. 放弃趋势，转向**召回质量**或其他特征。
- 结论：**采用仍维持 HL7+cap1.5（不带趋势）**。

> 产物：`v5_hybrid_recommendation.py`（改：`V5_TREND` 开关 + 趋势映射）、`M_量趋势特征（新）.py`、本文件；训练产物本地不入库。
