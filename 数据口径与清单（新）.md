# 数据口径与清单（新）

## 1. 数据版本

| 项 | 值 |
|---|---|
| 业务宽表 `test.node_day_ops_wide_full` | 截至 `2026-09-09` |
| 节点画像 `jarvis.node_analysis_data` | 分区 `20260910` |
| 样本窗口 `sample_day` | `2026-08-10` ~ `2026-09-09`（31 天） |
| 账粒度 `outcome_window_days` | `1`（日粒度） |
| 目标 `target_mode` | `absolute`（默认；单位带宽版需另跑 `--target-mode unit_bandwidth`） |

## 2. 节点范围（大节点，排除机房/小盒子）

按 RJ 规则（`build_recent_large_training.py` / `scan_current_non_idc_large_mismatches.py`）：

```text
state = online 且 stage = inService
且 supplysidedeliverytype ∉ {机房, 小盒子}
且 nodedeliverytype ∉ {droid, h618, aml, n1, openwrt}
且满足其一：supplysidedeliverytype ∈ {汇聚, 专线}
        或 resourcetype ∈ {aggregation, dedicated}
        或 max(bandwidth, actualbandwidth) ≥ 100
        或 (max(bandwidth, actualbandwidth) ≥ 1000 且 corenum ≥ 8)
```

| 指标 | 值 |
|---|---:|
| 候选节点（含非大节点） | 41,951 |
| **大节点** | **2,288** |

## 3. 业务范围（主流 allowlist）

| 指标 | 值 |
|---|---:|
| allowlist 业务数 | 50 |
| 观测到的 allowlist 业务 | 36 |
| 未观测到的 allowlist 业务 | 14 |
| 源账行数 | 148,733 |
| 过滤后（主流）节点 | 2,261 |
| 过滤后（主流）账行 | 134,834 |
| 排除的非主流业务 / 行数 | 47 / 13,899 |

> `10000244 B站专线-盒子V2` 在 allowlist 内（`mainstream`）。

## 4. 产物清单（本地路径，大文件不入库）

```text
recent_month_large_1d（新）/
  ├─ large_candidate_node_ids_recent_1m.json
  ├─ multibusiness_nodes_large_recent_1m.csv
  ├─ multibusiness_outcomes_large_recent_1m.csv
  ├─ v1_training_pairs_large_recent_1m.csv
  ├─ v2_large_recent_outputs/（v2_ranking_model.json、v2_node_recommendations.csv 等）
  └─ recent_large_training_summary.json

recent_month_large_mainstream_1d（新）/
  ├─ multibusiness_nodes_large_mainstream_recent_1m.csv
  ├─ multibusiness_outcomes_large_mainstream_recent_1m.csv
  ├─ v1_training_pairs_large_mainstream_recent_1m.csv
  ├─ v2_large_mainstream_outputs/（v2_ranking_model.json、v2_node_recommendations.csv 等）
  └─ mainstream_large_training_summary.json
```

> 目录约 256 MB + 222 MB，已被 `.gitignore` 的 `recent_month_large*/` 排除，**不入库**。

## 5. 离线验证结果

主流大节点 1d（窗口 08-10~09-09）：

| 指标 | 值 |
|---|---:|
| eval groups | 11,061 |
| 唯一评估节点 | 453 |
| hit@1 | 0.3097 |
| hit@3 | 0.6434 |
| nDCG@3 | 0.5302 |
| top1 observed coverage | 0.4695 |
| observed regret | 0.1291 |

Top1 分布（主流大节点，节选）：

```text
10000279 = 3186
10000096 = 1437
10000281 = 1346
10000244 = 1267   ← B站专线-盒子V2 仍在前列
10000278 = ...
```

## 6. 限制与下一步

- 数据截至 `2026-09-09`，之后走势未知；容量结论需滚动重算。
- 本次为 `absolute` 目标；V5/V6 链路使用单位收益目标，后续需补跑 `--target-mode unit_bandwidth`。
- `10000244` 仍进入 Top1 前列 → 阶段 2 用需求容量层对其做容量拦截。
