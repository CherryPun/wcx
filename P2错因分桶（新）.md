# 排序错因分桶（新）

## 1. 口径
- 真值A：节点窗口内实际跑过业务中**单位收益最高**者；
- 真值B：节点内 min-max 归一后**0.5 矿主 + 0.5 平台 单位收益**最高者（贴近 V5 目标）；
- 超容业务集合：`1382680008 / 10000224 / 10000096 / 10000041`。

## 2. 结果（n=2,146）

| 口径 | Top1 | Top3 |
|---|---:|---:|
| 真值A（观察单位收益） | 2.05% | 30.62% |
| 真值B（模型 0.5/0.5） | 2.61% | 31.64% |
| **剔除超容业务后（A）** | **25.77%** | 28.80% |

- 未命中 A：n=1,489；**被挤掉的 Top1 分布**：
  `10000096` 666 / `10000224` 431 / `1382680008` 277 / `10000244` 54 / 其余零散；
- **未命中中"挤掉者是超容业务"占 92.8%**；
- 未命中中的真优分布：`10000280` 638、`10000079` 232、`10000304` 179、`10000069` 139、`10000183` 72…；
- 挤掉者带宽更大的占比仅 0.3%（不是简单"追大带宽"）。

## 3. 结论（关键）

1. **排序被少数超容业务主导**：`10000096/10000224/1382680008` 这三个既**超容**、又**占据了绝大多数模型的 Top1**；
2. 但**它们并非多数节点的历史最优** → 一旦把这三个从候选里去掉，**Top1 命中 2.05% → 25.77%（≈12.6×）**；
3. 真值 A 与 B 差异很小（2.05 vs 2.61%）→ **目标定义不是主因，排序偏差才是**；
4. 即：**容量冲突与排序偏差是同一现象的 two sides**——模型过度偏好少数高量业务，既压低了命中，又造成全切超容。

## 4. 含义与下一步

- **不该用"硬惩罚/折减"**（前面已验证会转移超限），而应从**排序本身**减少对少数业务的过度偏好：
  1. **校准到命中/收益**（不仅 RMSE）：如按业务分层校准、单调约束、对高预测量业务做收缩；
  2. **样本/权重**：限制单一业务对损失的支配（分组加权、按业务归一 loss）；
  3. **候选/容量联合**：在排序阶段考虑容量余量（残余有效）。
- 验收：在**容量可行集内**看 Top1/Top3 是否提升、且集中度不恶化。

> 产物：`P2错因分桶（新）.py`；明细文本本地产物不入库。
