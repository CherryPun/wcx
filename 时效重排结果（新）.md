# 时效重排结果（新）

## 1. 做法

在 V5 输出上对候选 Top1~3 做**再排序**（不改训练）：
对持续掉量业务按其近端量比折减 `combined_score`，再取最高分为 Top1。

```text
折减因子 = max(FLOOR, 1 + cost_chg)   （cost_chg<0 且近7天成本基数 >= 1000 才折减；否则=1）
FLOOR = 0.5；掉量口径=近7天 vs 前7天（见 掉量时效核查（新）.md）
```

脚本：`时效重排（新）.py`；明细：`时效重排明细（新）.csv`（本地产物，不入库）。

## 2. 结果（节点数 2,288）

- **Top1 改变 1,277 个（55.8%）**——重排幅度很大。

| 业务 | 重排前 Top1 | 重排后 Top1 |
|---|---:|---:|
| 10000096 字节架构专线V3（-38.5%） | 1,118 | **0** |
| 1382680008 字节CDN专线 | 516 | **1,365** |
| 10000224 字节CDN专线V4（-93.2%） | 445 | 381 |
| 10000244 B站专线-盒子V2（-7.8%） | 82 | 52 |
| 10000183 B站专线-盒子（-29.3%） | 44 | 8 |

## 3. 关键问题（结论）

**单纯按"掉量"折减会把推荐整体推向另一个容量已超限的业务：**

- 折减把 `10000096` 几乎清空（1,118→0），但流量集中转向 `1382680008`（516→1,365）；
- 而 V6 显示 `1382680008` **全切溢出 ≈1,957 Gbps（最大）**，`10000224` 仍溢出 ≈1,634 Gbps；
- 即：不用容量约束兜底，时效折减只是"把扎堆挪个地方"，甚至挪到更紧的业务。

## 4. 结论与建议口径

1. **时效处理必须与 V6 容量约束联合**：先时效重排、再过容量分配，二者缺一不可。
2. **折减要更温和、更有针对性**：
   - 建议 `FLOOR` 抬到 `0.8~0.9`，并只对 `cost_chg <= -20%` 的业务折减，避免把 -7.8% 的 `10000244` 也一起打；
   - 或改为**训练侧近端加权**（在样本权重上体现时效），而非对最终分数硬折减。
3. **优先对象仍是 `10000096` / `10000224`**：这两个既掉量又容量超限；`10000244` 在 V5 下仅 82 节点、-7.8%、容量有富余，**不是时效处理的首要目标**。
4. 任何时效改动都需**重跑 V6 容量**看溢出是否下降，不能只看 Top1 分布。

## 5. 下一步

- 选参：`FLOOR=0.85`、阈值 `cost_chg<=-20%`，重跑时效重排 + V6，比较：
  - 全切溢出业务数/溢出量是否下降；
  - 10000096/10000224 是否被有效抑制且未转移到其他超限业务；
- 或改为训练侧近端加权方案评估。

> 本文件只报告一次实验，不代表最终方案。
