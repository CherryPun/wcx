# M1 需求折减结果（新）

## 1. 做法

在 V5 打分侧按**业务级需求/供给稀释因子**折减候选综合分，再交 V6：

```text
稀释因子 = min(1, 容量上限 / 全切Top1负载)   （取 V6 基线业务层）
adjusted = combined_score × 稀释因子
```

## 2. 四方案对比（V6）

| 口径 | 溢出业务数 | 总溢出(Gbps) | 拦截 | 切换数 |
|---|---:|---:|---:|---:|
| 基线(V5) | 4 | 4,955 | 11 | 57 |
| 时效 hold | 2 | 2,006 | 10 | 23 |
| M1M2（逐节点） | 3 | 4,573 | 8 | 60 |
| **需求折减 M1** | 3 | **1,786** | 17 | **16** |

各方案溢出业务：

- 基线：1382680008(1,957) / 10000224(1,634) / 10000096(1,364) / 10000041(0)
- hold：1382680008(2,006) / 10000041(0)
- **需求折减 M1：10000244 B站(823) / 1382649737 腾讯专线(812) / 10000065 B站mp(151)**

## 3. 关键结论（否决）

1. **需求折减把字节系的溢出"转移"到了新业务**：`10000244 B站` 全切负载升到 2,326,128 > 上限 1,503,428（溢出 823 Gbps），另有腾讯专线 812。
   → **违反验收标准"总溢出下降且不转移"**，不可采用。
2. **根因：业务级静态稀释因子用"旧的全切负载"算，重排后负载变大，因子没更新** → 低估，B站被吸走大量节点后反而超容。
   → 静态一次折减**不成立**，需要**迭代/均衡定价**（分数随累计占用动态调整）。
3. **注意：所有方案的"planned"解都由 solver 卡在容量内（planned 溢出 0）**；
   `unconstrained_top1` 只是"若全切"的诊断值。so 需求折减的实际收益无法用该诊断值证明，需**均衡 + 反事实/A-B** 验证。
4. 切换数大降（57→16）说明折减确实抑制了切换，但代价是把量转移并制造新超容。

## 4. 结论

- **静态业务级需求折减：不做**（会转移超限）；
- 若要"真 M1"，应实现**边际/均衡定价**：随池累计占用动态下调候选分（等价于把容量作为价格的迭代），
  并用"总溢出不转移 + 逆向评估"验收；
- 在缺反事实/在线验证前，**推荐维持 solver 现有硬约束（planned 溢出 0）**，不引入外层分数折减。

> 本文件为实验记录，代表一次被否决的方案。
