> **确定性更正**：本文件的指标对比在**非确定性运行**下完成（Top1 约 25% 会变）；集中度/Top1 类结论以 确定性修复与重评（新）.md（单线程）为准。

# 稀释与需求折减实验（新）

合并原 `M1M2稀释感知结果（新）.md`、`M1需求折减结果（新）.md`、`M1确认_solver已含累计约束（新）.md`。

## 1. 三种尝试与结果

| 尝试 | 做法 | 全切溢出 | 结论 |
|---|---|---:|---|
| 逐节点折减(M1M2) | 每节点按池余量折减分数 + hold | 4,573（-7.7%） | 弱：池余量被逐节点重复使用 |
| 业务级需求折减 | `score × min(1, 上限/全切负载)` | 1,786 | **转移超限**：B站(823)/腾讯(812) 新溢出 |
| solver 累计约束确认 | 查 `optimize_allocations` | — | **已存在**（MILP，planned 溢出 0） |

## 2. 关键结论

1. **V6 求解器已含"累计占用"约束**（每池累计负载 ≤ 上限 + 每节点≤1候选 + 目标最大分增益），
   节点恒有"维持当前"选项 → 池满自动降档/hold；**不需要外层折减**。
2. **外层折减（逐节点或业务级）都不成立**：前者太弱，后者转移超限。
3. 如要做"真 M1"，应实现**边际/均衡定价**（随累计占用动态调分），且需反事实/A-B 验证。

## 3. 采用

- **不改 V6 solver**；不做外层折减。
