# 客户视图对账（新）

**目的**：验证"能否用 Superset 的 5 分钟客户视图修掉 `current_business` 覆盖缺口（142 unknown）"。
**结论：不能。它是可信的镜像，但覆盖仅约一半且偏差明显；unknown 大部分是真实情况，不是数据缺口。**

## 1. 数据源（Superset，db19 schema test）

| 表 | 内容 |
|---|---|
| `jarvis.dws_jarvis_customer_5min_view` | `day / ts / node_id / customer_id / vendor / in / out / virtual_cid`，5 分钟粒度；09-14 前约 **14.1 亿行** |
| `lbattle.jarvis_customer` | `record_id / name / is_virtual / associated_customers / sign_id / id_new`（虚拟→真实绑定） |
| `jarvis.dws_jarvis_business_5min` | node×biz 5 分钟 `in/out/net_in/net_out/deudp_*` + 省/运营商/资源类型（高频状态可用） |

## 2. 实测结果

| 检查 | 结果 |
|---|---|
| `customer_id` 语义 | **已是真实业务 ID**（`000b55c2…`：视图 10000034 = V3 audit selected_business 10000034）→ 无需虚拟映射即可对齐 |
| 一致性（clean 节点） | 视图覆盖到的 **30/30 节点-日主业务与 audit 完全一致（100%）** |
| 覆盖（09-13 全量） | 视图 **80,591** 节点 vs 宽表 **170,673** → **≈47%** |
| 抽样（09-01~09-09，每日 400 节点） | 每天仅命中 **~50 个节点** |
| **缺失与收入的关系** | 节点**在**视图：归因收入>0 占 **11.4%**、均值 20.2；**不在**视图：**81.5%**、均值 **150.9** → **缺失的恰是高收入节点** |

## 3. 能否修 142 unknown：不能

被视图覆盖的 **511 个** unknown 节点-日，其业务全部为 **allowlist 之外**：
`10000175`(189)、`10000177`(97)、`1380389889`(77)、`10000014`(19)、`10000303`(10)…（这些节点 `active_nonmainstream_count` 均值 = 2.0）。
→ 视图**证实**这些节点在跑非主流/未纳管业务，**不是数据缺口，无法"回收"**。

## 4. 技术注意
- `in` / `out` 为**保留字**，必须反引号；
- `virtual_cid` **全空**，虚拟映射需走 `lbattle.jarvis_customer.associated_customers`；
- node_id 存在带供应商前缀形式（如 `ant…`）；
- 大表必须带 `day`（+节点前缀）过滤，否则引擎 500。

## 5. 结论与后续

| 判断 | 结论 |
|---|---|
| Superset 能否替代 NiuLink 做权威绑定 | ❌ 只能做**镜像/交叉校验**；官方口径仍需 NiuLink 与其**收录规则** |
| 能否修 current_business 覆盖缺口 | ❌ 覆盖 ~47% 且偏向低价值节点 |
| unknown 的性质 | ✅ **大部分是真实情况**（非主流业务），不是缺口 |
| 有价值的产品问题 | 若希望纳管这些业务（10000175/10000177/1380389889…），那是**扩 allowlist** 的产品决策，可用 5 分钟视图直接支撑数据 |

**给 RJ 的问题**：`dws_jarvis_customer_5min_view` 的收录口径是什么（为何仅约一半节点、且缺失的是高收入节点）？是否可作为 `current_business` 的补充/校验源？
