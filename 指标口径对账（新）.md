# 指标口径对账（新）

**触发**：RJ 文档 `节点业务推荐模型V5全流程说明.md` §10.3 报平台 R² = **0.0263**、矿主 RMSE = **0.0470**（V4 基线）/0.0438（V5），
而我们在 `recent_month_large_mainstream_v5_sw_7c2.0（新）` 同窗复算是平台 **0.0069**、矿主 RMSE **0.2002** —— 差 3–4 倍。

## 1. 对账结论：差异来自样本口径，不是模型

用 V3 日粒度 pairs 的 `construction_bandwidth_mbps` 对测试期预测逐行分层，重算加权 RMSE/R²：

| 建设带宽下限 | n | 矿主 RMSE | 矿主 R² | 平台 RMSE | 平台 R² |
|---|---:|---:|---:|---:|---:|
| 不过滤（全样本） | 39,379 | 0.2002 | 0.0253 | 0.1923 | **0.0069** |
| ≥100 Mbps | 39,177 | 0.0766 | 0.0889 | 0.0482 | 0.0293 |
| **≥500 Mbps** | 33,484 | **0.0341** | **0.2370** | **0.0284** | **0.0363** |
| ≥1,000 Mbps | 26,092 | 0.0361 | 0.2332 | 0.0294 | 0.0560 |
| ≥2,000 Mbps | 18,681 | 0.0348 | 0.1935 | 0.0285 | **0.0719** |

对照 RJ 文档：矿主 RMSE 0.0470 / R² 0.2825、平台 RMSE 0.0492 / R² 0.0263。
→ **bw ≥ 500 时两边完全同量级**（我们矿主 0.0341/0.237、平台 0.0284/0.0363）。

**根因**：我们的评估**未设建设带宽下限**，样本含最小 50 Mbps 的节点（bw < 1,000 占 11.7%、bw < 500 约 5,900 行），
`金额 / Mbps` 在小分母下产生极端值（这些行的 y 的 p99 达 **19.49**，正常行仅 0.14），极端值主导 SSE → R² 塌陷。

## 2. 对既有结论的影响

| 结论 | 影响 |
|---|---|
| "平台 R²≈0.007、节点级几乎不可预测" | ⚠️ **被夸大**：那是**混杂口径**产物。大节点子集（bw≥500）平台 R² ≈ **0.036–0.072**，矿主 R² ≈ 0.19–0.24 |
| 平台标签弱、不能做节点级点预测 | ✅ 方向不变（0.04–0.07 仍很低），但量级描述需改 |
| `成本归因与标签诊断（新）.md`（总账恒 0、残差 0.35–0.58、业务 eta² 0.009） | ✅ **不受影响**（数据源为 CDN 毛利下钻，非本 pairs） |
| 采用配置相对基线的改进（矿主 RMSE −28%） | ⚠️ 需**限定口径**重述；全样本下基线/采用同口径可比，但绝对值受小带宽样本污染 |

## 3. 必须做的口径修正

1. **V5/V6 指标一律声明建设带宽下限**（建议 ≥500 Mbps，与"大节点"语义一致）；或直接在大节点子集上报告；
2. `交付RJ说明（新）.md` / `分析报告（新）.md` / `正式交付索引（新）.md` 里的 R²/RMSE 补口径标注；
3. 复算"采用 vs 基线"在 bw≥500 下的差值（基线预测文件已随清理删除，需重跑一次 baseline build）；
4. 压测特征本轮回填覆盖率为 **0.0**（见 summary `latest_pressure_training_coverage`），
   即**我们的指标不含静态压测泄漏**；风险仅在"启用该特征（RJ 口径静态复用）"时才出现——交付文档措辞需据此更正。

## 4. 待办
- [ ] 在评估脚本加带宽下限参数（`--min-build-bandwidth`），默认 500；
- [ ] 重跑 baseline 与采用配置，在 bw≥500 下重新出对照表；
- [ ] 更新交付文档指标口径。
