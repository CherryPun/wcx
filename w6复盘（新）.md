# w6 复盘（新）

**背景**：8 窗滚动回测中 w6（测试窗 **2026-08-02~08-05**）明显走弱：矿主 RMSE 0.0681 / R² 0.1166、平台 RMSE 0.0673 / R² 0.0222，为 8 窗最低。

## 1. 定位：不是模型退化，是极少数极端标签

| 检查 | 结果 |
|---|---|
| 业务维度 | w6 矿主误差 **83.8% 来自业务 `10000280`（七牛CDN-ZJ月95）**；其他窗口该项仅占 13–31% |
| 行维度 | **9 行（占样本 0.03%）贡献 80.4% 的加权 SSE** |
| 值域 | 这 9 行实际单位收益 0.50–**5.00** 元/Mbps/天，而 `10000280` 典型值 0.078 |

**两类异常（性质不同）**

| 节点 | 特征 | 判断 |
|---|---|---|
| `dfe5b08f…`（bw=1000） | 07-28~08-09 **每天** 0.66–0.76（约典型值 9 倍，持续） | **持续性口径/归属异常**（小带宽节点长期承担大额成本） |
| `00c47917…`（bw=810） | 08-02 单日 **5.0009**，前后两天 0.0125 / 0.0071 | **单日尖峰**（疑似冲正/补记） |

## 2. 根因：评估用原始真实值，而训练用 winsorize 标签

训练侧按业务做 P01–P99 winsorize（`clip_target`），但**评估侧用的是未裁剪的真实值** →
个别极端行（真实值 5.0 vs 预测 0.07）单项就产生 ≈ 24 的平方误差，直接吞掉整个窗口。
这与"小带宽尾部"是**同一类口径问题**（详见 `指标口径对账（新）.md`）。

## 3. 修复：评估侧也按业务 P01–P99 winsorize（已实现）

新增 `--eval-winsorize`（默认 **1**）。修复前后对比：

| 窗口 w6 | 矿主 RMSE | 矿主 R² | 平台 RMSE | 平台 R² |
|---|---:|---:|---:|---:|
| 修复前 | 0.0681 | 0.1166 | 0.0673 | 0.0222 |
| **修复后** | **0.0294** | **0.4226** | **0.0258** | **0.1293** |

8 窗整体（修复后，bw≥500 + winsorize）：

| 汇总 | 均值 | 最差 | 最好 | std |
|---|---:|---:|---:|---:|
| 矿主 R² | **0.3915** | **0.2610** | 0.5501 | 0.0958 |
| 平台 R² | **0.0963** | 0.0272 | 0.1709 | 0.0507 |

→ w6 不再是异常窗（矿主 R² 0.423，高于均值）。

## 4. 仍需跟进的真实问题

winsorize 只是**评估口径修复**，掩盖不了业务侧的事实：**`10000280` 存在长期异常值域节点**
（如 `dfe5b08f…` bw=1000 却持续承担 ≈0.7 元/Mbps/天，是典型值的 9 倍）。
这属于 `成本归因与标签诊断（新）.md` 的同一议题：**小带宽节点的成本归属是否合理**。
建议：拿到财务口径后核查该节点是否真的产生该成本；若是真实成本，则应作为"低价值节点"参与决策，
而不是被 winsorize 掉。
